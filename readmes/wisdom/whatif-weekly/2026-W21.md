# WhatIf — Week 2026-W21 (May 18–24, 2026)
**Posted:** 2026-05-18

---

## Claude's Items

| ID | Domain | Question / Observation | Confidence | Resolve By | Outcome | Accurate | Worthwhile |
|----|--------|----------------------|------------|------------|---------|----------|------------|
| C-W21-1 | SU | The first test of animation from map.json will show vehicles moving on inter-station beams but with visible teleport jumps at station entry and exit — the gap flags will appear but the vehicles will not stop at the flags, they will skip over the station-internal segment and resume on the next inter-station beam. | High | 2026-05-25 | | | |
| C-W21-2 | SU | `generate_map_json` will generate correct `length_mm` for inter-station segments but incorrect (or zero) lengths for station-internal `features` lines — because the station-internal geometry is synthetically generated and the mm measurement path has not been verified on that geometry type. | Med | 2026-05-31 | | | |
| C-W21-3 | SU | Archiving the 27 console tasks will reveal within one session that at least one of them is still called indirectly — a method called from a kept task will reference an archived task's lambda or call a method that only existed in the archived task's run block. | Low | 2026-05-25 | | | |
| C-W21-4 | RT | The Route-Time simulator will produce a different travel time estimate for the same O-D pair as the SketchUp map.json `length_mm` — not because either is wrong, but because Route-Time uses straight-line distance while map.json uses arc-walked distance along the Bezier-approximated beam path. The divergence will be detectable and should be documented as expected. | Med | 2026-06-07 | | | |
| C-W21-5 | SYS | The console cleanup from 43 to 16 tasks will cause no user complaints — Bill will find the lean list easier to navigate and will not ask for any of the 27 archived tasks to be restored within the next 4 weeks. | High | 2026-06-15 | | | |

---

## Allie's Items

*(Allie posts Monday 2026-05-19 via allie-whatif.py)*

---

## Assessment Log

**2026-05-20:** TripPlanner v2 complete — C-W21-1 and C-W21-2 are now testable. Pipeline: Build → generate_map_json v2 → TripPlanner v2 → Animate. Vehicle animator handles v2 Hash segments. Run this sequence in SketchUp to assess both items.

---

## Why These Items

**C-W21-1 (teleport jumps):** The station-internal gap is the largest known defect in the
map.json animation architecture. High confidence because gap logging already confirmed this
in build_pods output.

**C-W21-2 (length_mm for features):** Feature lines are generated from platform stub geometry
— a different code path than inter-station bezier segments. The mm measurement has not been
explicitly verified on this path.

**C-W21-3 (indirect references to archived tasks):** Archiving by removal is clean, but
console tasks sometimes call shared module methods that were only ever exercised through
the archived task. Low confidence because the kept tasks are tightly scoped and unlikely
to have this dependency.

**C-W21-4 (RT vs map.json length divergence):** Two different systems measuring the same
network for different purposes. Worth documenting as expected behavior rather than a bug.

**C-W21-5 (no restore requests):** The 27 archived tasks were all diagnostic or exploratory.
The core workflow (Build → Validate → Animate) is complete in the 16 kept tasks.
