# WhatIf — Week 2026-W24 (June 8–14, 2026)
**Posted:** 2026-06-07

---

## Claude's Items

| ID | Domain | Question / Observation | Confidence | Resolve By | Outcome | Accurate | Worthwhile |
|----|--------|----------------------|------------|------------|---------|----------|------------|
| C-W24-1 | SU | The other three templates (station_line_end, station_thru_dip, traffic_circle7) will each need a vector_in component added to their `gw_cp_in_*` tracks — and all three will clear Noelle RED FLAG on first Extract Template run after the component is added. No code change needed. | High | 2026-06-14 | | | |
| C-W24-2 | SU | The `allie-agent-brief.py` output for JPods_station_parking will show the vector_in demand as a high-frequency observation (it fired many times across revisions 1–16) and will need `_normalize_text` tuning before the brief is useful — the per-run rev numbers in the flag text will produce many distinct "unique" flags that are actually the same underlying issue. | Med | 2026-06-14 | | | |
| C-W24-3 | SU | The `trial5` template folder has no lines.json and will turn out to be either a student test model or a Bill experiment — it will be deleted or moved to an `_archive/` subfolder within one session of being noticed. It is not a future template. | High | 2026-06-14 | | | |
| C-W24-4 | SU | The proximity threshold of 5000mm (5m) for matching vi_entities to gw_cp_in endpoints will prove too loose for templates where two CPs are close together — traffic_circle7 has 4 CPs and some are ~10m apart, but a parking-style template with CPs at opposite ends of a 73m platform could have a vi_entity match the wrong CP. A tighter proximity (2000mm) or a nearest-wins strategy will be needed. | Med | 2026-06-21 | | | |
| C-W24-5 | SYS | The agent observation JSONL files will contain enough data after running all 6 templates to produce a meaningful first teaching brief — but the brief will surface that Noelle's "arc Z span" check fires on traffic_circle7 repeatedly and has never been resolved, making it the highest-frequency unresolved demand. | Med | 2026-06-21 | | | |

---

## Allie's Items

*(Allie posts Monday 2026-06-09 via allie-whatif.py)*

---

## Assessment Log

*(assessments added as resolve dates pass)*

---

## Why These Items

**C-W24-1 (other templates clear on first try):** The vi_entity scan and `_vi_component_direction`
are template-agnostic. Once Bill adds the vector_in component to each gw_cp_in instance in the
other templates, the code finds it via proximity. No per-template adjustment needed.

**C-W24-2 (normalize_text tuning):** The brief generator strips per-run variable data, but the
flag messages include the specific formation name and CP index. Revision numbers in error context
strings (from the `(rev N)` extraction log) would not be in flags, but the CP-specific text
("CP0: gw_cp_in_0 missing...") normalizes correctly. The real risk is that 16 revisions of
RED FLAG for the same root cause appear as 16 separate observations with slightly different wording.

**C-W24-3 (trial5 cleanup):** Any template folder without lines.json will cause the batch
Extract Template run to emit a `no lines.json` warning and fail silently. That's not harmful but
is noise. trial5 is almost certainly from a prototyping session.

**C-W24-4 (5m proximity too loose):** The threshold was set for JPods_station_parking where the
two CPs are ~74m apart. For a closer arrangement, a 5m bubble could overlap two different CPs.
The correct fix: nearest-wins (sort by distance, assign the closest vi_entity to each cp_in,
no vi_entity used twice).

**C-W24-5 (arc Z span demand):** The `gw_uturn_*` Z-span check in Noelle fires whenever the arc
has a slight Z variation (flange artifact). This has been noted as cosmetic in prior sessions but
has not been fixed at the model level. After 6 templates × multiple revisions it will be the
highest-frequency observation in the JSONL.
