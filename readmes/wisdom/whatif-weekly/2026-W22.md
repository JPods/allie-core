# WhatIf — Week 2026-W22 (May 25–31, 2026)
**Posted:** 2026-05-23

---

## Claude's Items

| ID | Domain | Question / Observation | Confidence | Resolve By | Outcome | Accurate | Worthwhile |
|----|--------|----------------------|------------|------------|---------|----------|------------|
| C-W22-1 | SU | The Trip Simulator end-to-end test (book a trip → animate → arrived) will surface a mismatch between the trip_id format in the booking response and what the status poller expects — either the format is right but the field name is wrong (`nora_id` vs `vehicle_id`) or the trip_id comparison uses string vs symbol keys in the Ruby hash. | Med | 2026-05-28 | | | |
| C-W22-2 | SU | `natalie_dispatch_idle` (written 2026-05-23, commit 944316c, never tested) will fail on first live test because the idle_reserve vehicles are placed in the model but not indexed into `@@pods` correctly — the dispatch function will attempt to call `.animate_step` on an object that was added without the full pod state hash. | Med | 2026-05-28 | | | |
| C-W22-3 | SU | Camera follow (`follow_camera_tick`, 0.25s repeating timer) will work but feel slightly wrong — the pod's world position will be correct but the camera orbit will be jittery when the pod is in a tight U-turn at a station platform, because the look-at target jumps by more than one guideway segment per tick. | Med | 2026-05-31 | | | |
| C-W22-4 | SYS | Within one week of the Station Names feature going live, Bill will assign meaningful names to at least 3 stations and use those names when describing a network layout — the feature will shift the vocabulary from "S044" to place names. | High | 2026-05-31 | | | |
| C-W22-5 | SU | The `self.class` bug pattern (module method calls silently failing inside proc blocks) is present in at least one other location in the plugin codebase that was not fixed in this session — it will surface when a different dispatch path is tested. | Low | 2026-06-07 | | | |

---

## Allie's Items

*(Allie posts Monday 2026-05-26 via allie-whatif.py)*

---

## Assessment Log

*(assessments added as resolve dates pass)*

---

## Why These Items

**C-W22-1 (trip_id format mismatch):** The booking response was built to return `nora_id` but the index.html wrappers normalize to `vehicle_id`. One side of that bridge has not been tested against live Ruby code. Mismatches at response field names are the most common first-run failure in new API wiring.

**C-W22-2 (natalie_dispatch_idle not tested):** The function adds to `@@pods` mid-animation — a code path that has never been exercised. Adding to a live animation array while the timer is ticking is likely to have an off-by-one or missing-field issue that only appears at runtime.

**C-W22-3 (camera follow jitter in U-turns):** The 0.25s tick rate was chosen without measurement. U-turn geometry has tight curvature; the camera's look-at target can move sharply between ticks at station approach speed. This is a feel issue, not a crash — worth noting before demo conditions.

**C-W22-4 (station names shift vocabulary):** The feature's real value is reducing cognitive load in multi-station networks. High confidence because Bill already thinks in place-name terms; S### is a tool artifact.

**C-W22-5 (self.class pattern elsewhere):** The bug was fixed in `dispatch_server.rb` mount_proc blocks. Other module proc patterns exist in the plugin (console callback wrappers, observer blocks). Low confidence because those paths have been tested, but the pattern is architecturally endemic.
