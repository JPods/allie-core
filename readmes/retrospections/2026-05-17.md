# Retrospection — 2026-05-17

## What Was Done

1. **Single source of truth for trips** — Identified and eliminated three independent trip-building paths:
   - `TripPlanner` (trip.json) — correct station-internal routing
   - `build_platform_round_trip` in `jpod_platform.rb` — graph traversal over line IDs, skipped station internals
   - `build_station_loop_trip` in `jpod_natalie_bridge.rb` — same problem

   `TripPlanner` is now the single source. `build_platform_round_trip`, `build_platform_one_way_trip`, and `build_station_loop_trip` all delegate to `TripPlanner.lookup` / `TripPlanner.lookup_round_trip`.

2. **`ROUTES_1CP_SIMPLE` corrected** — Bill reviewed trip.json and flagged S048→S050 arrival as wrong. Root cause: `gw_far_out` was coded as the platform destination for `1cp_simple` stations, but it is not. The correct arrival routes through `gw_platform_in → gw_platform_parking → gw_platform`. Departure is `gw_platform → gw_platform_out → gw_stub_pair_0_out` (no u-turn).

3. **`TripPlanner.generate_from_path`** added — bypasses `model.path` empty-path failure. Now callable with an explicit followme.json path without a saved model.

4. **`TripPlanner.lookup` + staleness check** — reads trip.json, regenerates if stale (followme.json newer than trip.json).

---

## Lessons for Allie

1. **`gw_far_out` is a departure track segment, not the platform** — `gw_far_out` appears only in `1cp_line_end` departure paths (`gw_platform → gw_uturn_1 → gw_far_main → gw_far_out → gw_stub_pair_0_out`). It is never the platform destination. `1cp_simple` stations reach their platform via the same tags as all other station types: `gw_platform_in → gw_platform_parking → gw_platform`. Domain: [SketchUp] [MeshMobility]

2. **The distinction between `1cp_line_end` and `1cp_simple`** — Both are end-of-line single-CP stations. The difference is in the departure path: `1cp_line_end` needs a reversal loop (`uturn_1 + far_main + far_out`) to face outbound again. `1cp_simple` exits directly via `platform_out → stub_pair_0_out`. If you see `gw_uturn_1` and `gw_far_main` tags, it is `1cp_line_end`. If you see only stub/platform tags, it is `1cp_simple`. Domain: [SketchUp]

3. **Two sources of truth for the same computation is a design defect, not a bug** — `build_platform_round_trip` and `TripPlanner` were computing trips independently. Bill named this directly: "There should only be one source of truth." The fix was not to patch the outputs to match, but to make one call the other. When two code paths produce the same output from the same input, one must be eliminated. Domain: [Cross-domain]

4. **Bill will review trip.json segment-by-segment and mark errors with "wrong"** — Format: he prefixes the bad record with `"wrong"` then provides the correct record. Read these as axiomatic corrections — the routing tables in `TripPlanner` must match them exactly. Domain: [SketchUp]

---

## Scars

- **`gw_far_out` as platform destination** — coded this as the platform for `1cp_simple` based on the comment "far_out contains the platform." That was wrong. `gw_far_out` is a track section in the departure loop — not named for containing the platform. Cost: two regenerate cycles.

- **Connection IDs synthesized as bidirectional** — `adjacency` created reverse edges by reusing the forward connection ID. followme.json has separate entries for each direction (`seg_S048_cp1_S050_cp0` and `seg_S050_cp0_S048_cp1`). Synthesizing the reverse edge with the forward ID put the wrong guideway in the trip. The fix is one line: remove the reverse-edge push in `adjacency`. Cost: one regenerate cycle.

- **`1cp_simple` departure** — first fix set it to `platform_out → stub_pair_0_out` (no u-turn), based on a wrong assumption that S050 was simpler than S051. The physical network shows both need the same departure loop. Cost: one more regenerate cycle.

---

## For Noelle

Noelle's station type detection feeds `TripPlanner.station_type`. The 1cp_simple/1cp_line_end distinction:

- `station_line_end` (S050, S051): arrival is `gw_stub_pair_0_in → gw_platform_in → gw_platform_parking → gw_platform`
- Departure: `gw_platform → gw_uturn_1 → gw_far_main → gw_far_out → gw_stub_pair_0_out`
- Both S050 and S051 use the same template and both include `gw_platform_parking`. The `1cp_simple` / `1cp_line_end` distinction was a false split — there is only one 1-CP terminus type in this network.

---

# Retrospection — 2026-05-17 (Part 2 — Animation Engine)

## What Was Done

5. **Route overlay direction fixed** — `show_route_followme_overlay` rewrote from scratch. Old code dropped all `{STATION}.`-prefixed segments and used `station_trail_chain` for the destination end (chained through all structure tracks including u-turns). New code iterates `trip_segs` directly, resolves only non-structure fm_paths. Diagnostic output added.

6. **gw-only axiom** — Established as a first-class rule: only gw entities (non-structure fm_paths with a `connection_id` attribute) define travel paths. Any fallback that touches structure tracks is a Build pipeline gap, not something to bridge in the animation engine.

7. **`jpod_vehicle_anim.rb` written** (340 lines) — complete replacement for the old BFS-based animation loop. Architecture: NoraPod (pure state machine) + JPodVehicleAnim (timer owner). Propose/commit pattern from ene_railroad. `JPodGuideway.start_animation` delegates to it.

8. **Destination assignment tools** — Console tasks `list_vehicles` and `set_vehicle_destination` let the user see all vehicles and assign destinations without touching Entity Info.

9. **CW/CCW bug root cause identified** — `jpod_network.rb:561` proves `track_idx=1` is the from→to direction. `candidates.first` with no track_index check picks the wrong beam. Fix deferred to animation retool.

10. **Animation retool plan opened** — Bill to write requirements; session ended here.

---

## Lessons for Allie (Part 2)

5. **`track_idx=1` = from→to travel direction** — Confirmed in Build pipeline code comment. When `prev_last` is nil in `resolve_gw_segs`, always prefer `track_idx=1` candidate. Picking `candidates.first` causes CW motion because track_idx=0 has reversed points. Domain: [SketchUp]

6. **gw-only is a hard constraint, not a preference** — Every attempt to bridge Build gaps through structure tracks produced wrong segment insertions. The gap is real and Noelle must fix it in the Build pipeline. Animation code must log and skip, never bridge. Domain: [SketchUp]

7. **Segment name encodes direction** — `seg_S048_cp1_S050_cp0`: first station = from, second station = to. This is parseable. Combined with `track_idx=1`, it gives two independent direction signals. If they conflict, the Build pipeline has a bug. Domain: [SketchUp]

---

## Scars (Part 2)

- **Bridge through structure tracks always contaminates** — Added `feature_bridge_chain` to fill the 3,583mm gap at S050. It found `gw_uturn_0` at dist=0.0 and inserted it as a bridge segment. Then `gw_stub_pair_0_in` from trip_segs created a backward detour. Plotted=9 for trip_segs=8. Cost: one diagnostic cycle. Rule: never bridge through structure tracks.

- **`Array#index_by` is a Rails method** — Ruby stdlib uses `each_with_object`. Cost: would have been a runtime error on first animation run.
