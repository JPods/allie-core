# Retrospection — 2026-07-17

## What Was Done

### WC3 Go-Live Preparation

- **Cleaned up Python venv** — removed `venv 2` (broken) and `venv312` (dead symlink), only `venv/` remains

### pdfme Print System (React2025)

- **Built pdfme PDF generation service** (`services/pdfme/generateCommercePdf.ts`) — client-side PDF generation replacing browser `window.print()`. Supports all transaction types.
- **Created proposal-specific template** with WC2 heritage: Accept/Use columns, Base/Disc%/Unit pricing, dual signature block, "use items only" footnote. Harvested from actual WC2 `ExamplePaperProposal.pdf` and `Proposal1.xml`.
- **Created three invoice variants** from WC2/JIT/JPods heritage:
  - `InvoiceStandardPrint.tsx` — Ord/Ship/B-O columns, bordered empty rows, serial numbers
  - `InvoiceShippingPrint.tsx` — 4-column header, DownPayment, Contract Detail
  - `InvoiceServicePrint.tsx` — Job#, Instructions, simple "Total Due" for international
- **Fetched pdfme playground starter templates** (invoice, quote) from GitHub — JSON templates with `table` schema, `multiVariableText`, `staticSchema` for footers
- **Built template service** (`templateService.ts`) — load/save pdfme templates from WC3 Report model
- **Built field registry** (`fieldRegistry.ts`) — exposes 120+ WC3 fields (scalars, JSON paths, computed) to the template system
- **Rewrote PdfDesigner.tsx** — template chooser, load/save from Report model, preview PDF, export JSON, submit for review, live data preview from real records
- **Wired Download PDF button** in `PrintPreviewModal` to use pdfme when `pdfData` provided
- **Added `printTypes.ts` fields**: `basePrice`, `use`, `accepted`, `qtyBackordered`, `jobNumber`, `orderNumber`, `instructions`, `daysValidFor`, `daysLeadTime`, `repId`

### Collaborate_WebClerk Setting

- **Created Setting #444** (`seed_collaborate_settings.py`) — 14 categories for Alice_local ↔ Alice_WCHQ collaboration, all independently toggleable
- **Added submission flow** — users submit templates for WCHQ review via Document records
- **Added library access** — browse, preview, install from WCHQ curated library
- **Added contribution controls** — opt-in, anonymized, approval-gated sharing back

### Agent Team

- **Created Andi** — Allie's sister, production observer on IT15 (always-on)
  - `facets/andi/facet.json` — 9 watches: Alice, Noelle, Nora, Sally, physical cross-correlation, sync, submissions, system health, security
  - `readmes/agents/andi.md` — full agent file with Celery beat schedule, systemd service, notes to other agents
  - `thoughts/andi/` — directory for nightly reflections
  - Nora/Sally telemetry: baselines not thresholds, 14-day rolling windows, drift tracking, cross-correlation

### Red / Orange / Yellow Flag System

- **Created `readmes/agents/agent-flags.md`** — complete flag system:
  - 🔴 Red: stop everything (chaos, unhappiness — that's the point)
  - 🟠 Orange: known problem, fix by date (every orange has a `fix_by`)
  - 🟡 Yellow: don't understand (honest signal, learning input)
  - Hard rule: every flag → Alice Action + human owner, no exceptions
  - Escalation rules, MQTT topics, resolution protocol
  - Five retrospection questions after every red flag
- **Updated ouch list** — clarified distinction from flags, added "why long-tail" and "what proves us wrong" required fields

### Alice Report Coach

- **Built `report_coach.py`** — compares forms within each model, finds duplicates, generates descriptions, tracks usage patterns
- **Built `audit_report_quality` management command** — `--fix` (deactivate duplicates), `--coach` (Alice generates descriptions), `--actions` (create Action records)
- **Added usage tracking** to `report_view.py` — `metadata.flow.use_count`, `last_used_utc`, `used_by[]`, `first_used_utc`
- **Cleaned 14 duplicate/orphan/test reports**, auto-generated 37 descriptions
- **Usage-based coaching**: NEVER_USED, SINGLE_USER_REPORT, SAME_USERS_BOTH_REPORTS findings

### Barn Cleaner

- **Built `BarnCleaner.tsx`** — floating window that walks through unapproved reports
- Four decisions: 🗑 Trash, ✕ Deactivate, ✏️ Restructure, ✓ Keep (reason required)
- WCHQ Library access button for replacements
- Alice coaching banner: "Weed aggressively. The cost of clutter is paid daily."
- 🧹 button added to DataBrowser toolbar

### DataBrowser / Reports Dialog Fixes

- **Fixed ReportsDialog filter** — `model_name` parameter collision was returning all 273 reports instead of model-specific. Fixed with client-side filter.
- **Added ✏️ Edit button** to each report row — opens PDF Designer with report template + live record data
- **Font size scale** — changed from S=12/M=14/L=16 to S=16/M=18/L=20/X=24, default M

### Wisdom Layer

- **`fear-and-delight.md`** — fear of change vs delight in excellence, applies to all domains
- **`the-loop.md`** — define metrics → try now → measure → adjust → loop → inspect frame. Constrained variability before frame inspection. Fear of scars is fear of wisdom.
- **`bend-to-perception.md`** — the user is more connected to the data than the program. Program bends to her perception. Trellis and rose.

### Vector Stores

- All wisdom files indexed into Claude store (1,910 chunks) and Alice store (13,555 chunks)
- Andi store will be seeded at IT15 deployment

## What to Polish

- **Server-side model_name filter for Reports** — client-side filter works but fetches all 261 reports then filters. Add `model_name_filter` param to wcapi GET endpoint for efficiency.
- **PdfDesigner live data preview** — `populateTemplateWithRecord` does flat field matching. Needs smarter mapping for nested JSON fields and line items table population.
- **Barn Cleaner preview** — uses server-side HTML endpoint for preview. Should render via pdfme client-side for consistency with the Designer.
- **Proposal 1-4 names** — still need human review to articulate what makes each different. Alice Action #346 is open.
- **Celery beat schedule for `audit_report_quality`** — command exists but not yet in Celery beat config for nightly runs.
- **Andi's `andi-reflect.py`** — facet and agent file created but reflection script not yet written.

## WhatIf Bucket

- Could the barn cleaner pattern apply to items, contacts, and saved searches — not just reports?
- Should Alice track which report layouts produce the most customer complaints or reprints?
- Could pdfme Designer support A/B testing — render two layouts side by side with same data, user picks?
- Would a "report genealogy" view help — show which WC2 form each report descended from?
- Should Andi's physical machine cross-correlation feed back into Noelle's network design rules?
