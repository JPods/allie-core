# Retrospection — 2026-06-07

## What was done

Completed the agent learning layer for JPods template validation (Noelle/Natalie/Sally).
Primary arc: resolving vector_in detection for `gw_cp_in_*` components.

Full session log: `sessions/2026-06-07.md`

---

## Lessons for Allie

1. **`Geom::ORIGIN` is not accessible inside lambdas in SketchUp Ruby** — use
   `Geom::Point3d.new(0,0,0)`. The constant `Geom::ORIGIN` fails in lambda/block scope
   inside the JPods module. Every future use of the origin point inside a lambda must use
   the explicit form. Domain: [SketchUp]

2. **ene_railroad solid geometry: no standalone edges** — FollowMe-extruded solids have
   edges only via `face.outer_loop.edges`. Any indicator geometry that code must find
   (direction edge, reference edge) must be a standalone edge NOT welded to face geometry.
   Add this to station model authoring rules. Domain: [SketchUp]

3. **When SketchUp makes a component from geometry, local origin ≠ edge endpoint** —
   Do not require `vertex.distance(Geom::ORIGIN) < tol` for externally placed components.
   Use the component's world insertion point to determine the "from" end of the indicator edge.
   Domain: [SketchUp]

4. **Explicit datums must be visible to humans** — Axiom 10 requires explicit model datums
   over derived references. But a datum hidden inside solid geometry is not truly explicit —
   it's hidden from the designer who must maintain it. Placing the vector_in indicator as a
   visible component outside the track group but inside its envelope solves both the
   human-visibility problem and the code-accessibility problem simultaneously. Domain: [Cross-domain]

5. **Diagnostic condition must check the right entity attribute** — `_diag` was false for
   3 iterations because it checked `entity.definition.name` (the ene_railroad component name)
   rather than `entity.layer.name` (the JPods tag). When a diagnostic doesn't fire, check
   the condition first, not the search logic. Domain: [SketchUp]

---

6. **Sally platform→parking check must be BFS, not direct-successor** — station_line_end
   routes from `gw_platform` to parking via a multi-hop hold loop (8 tracks). A direct
   successor check will always flag this as broken even when it is correct. BFS through
   the lines.json successor graph is the right check for reachability.
   Domain: [SketchUp]

7. **Pass-through topologies have no platform/parking — and that is correct** —
   traffic_circle7 is a pure routing station; pods never park. Sally's parking requirement
   must check for topology type before flagging. Marker: `pass_chains` present AND
   `hold_loop_chain` absent in lines.json. Generalizes to any future routing-only template.
   Domain: [SketchUp]

8. **Proof checks should read from extracted.json, not re-scan geometry** — Proof had its
   own `_find_direction_vector` call for the `gw_cp_in_*` vector_in check, independent of
   what `populate_from_open_template` had already determined. Any check Proof makes about
   a property that was computed during extraction should read `extracted.json['that_field']`,
   not re-derive it. Avoids drift between extraction result and proof verdict.
   Domain: [SketchUp]

---

## Scars

- Three hours on vector_in detection through nested geometry before arriving at the correct
  architecture. The indicator must be a visible model-level component. Hidden geometry is
  not an explicit datum — it's implicit geometry that happens to have the right length.

---

- The platform→parking BFS fix was initially coded as a false-positive suppression.
  Bill caught that station_line_end really does have parking reachable via the loop
  before the code was tested. Read the topology first; don't assume.

---

## WhatIf

Posted to `readmes/wisdom/whatif-weekly/2026-W24.md` — 5 items spanning SU, SYS domains.

---

# Retrospection — 2026-06-07 (session 2)

## What was done

- Wrote TFTS and Axiom 19 for the "one source of truth / do the math" arc
- Fixed Natalie block: `guideway_group?` now recognizes inter-station seg_ groups via `seg_guideway=true`
- Fixed path.json to capture seg_ beam paths (actual track positions, ±1.75m from centerline)
- Fixed Show Route: now draws on specific navigated track, not centerline between two tracks
- Removed all 222mm references from code and comments; `detect_cps_from_top_level_cp` simplified to use rail edges only

## Lessons for Allie

9. **`guideway_group?` was the invisible wall for FollowMe export** — `e.name == 'JPods Guideway'` is correct for intra-station groups but inter-station seg_ groups are named by their connection_id. The `seg_guideway=true` attribute is the reliable flag. Any future scan that needs ALL guideways must check both. Domain: [SketchUp]

10. **The CP hub is the construction centerline, not a vehicle path** — the cp_marker hub vertex is equidistant between the two 1750mm rails (midpoint of the gate face). It defines the center of the Bezier Plan. The actual vehicle tracks are ±1.75m from this hub. map.json pts = centerline; path.json pts = actual track. Never use map.json pts for vehicle position display. Domain: [SketchUp]

11. **222mm was a legacy tangent edge, not a Z offset — but it caused Z confusion** — the 222mm tang_edge in the cp_marker was used to determine the CP outward tangent direction. It contributed nothing to CP center Z. The Z discrepancy (~222mm) between seg_ and gw_ endpoints is a separate model authoring issue: cp_marker hub vertex Z must be 0 in definition-local space, placed by the model author at the correct gate height. Domain: [SketchUp]

12. **path.json and FollowMe export had the same blindspot** — both scanned for `e.name == 'JPods Guideway'` and both missed inter-station seg_ groups. Two parallel bugs from the same assumption. When fixing one geometry scan, always check if other scans share the same group identity predicate. Domain: [SketchUp]

## Scars

- The Natalie block ("0 guideway connections") persisted across two trials because the root cause was in `guideway_group?` — a shared predicate used by both FollowMe export and path.json. The fix was three lines. The diagnosis took the full session.

## Open

- S003 / S004 SEVERE end_delta: needs re-extraction via Generate Template Data
- S003 lines.json "no implicit conversion of String into Integer" — uninvestigated
- cp_marker hub vertex Z: needs model authoring verification
- traffic_circle7 chains approved_by not set
