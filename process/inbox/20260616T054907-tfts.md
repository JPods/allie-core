# TFTS — 2026-06-16T05:50:00Z

problem:   Animator reads model.json but BFS fails + _build_route_json RED FLAGs with v5 lines.json
fault_ref: (no separate FAULT file — identified from console output during Step 4 testing)
domain:    SU

arc:
  - try:      Add load_model_json + model_json_to_map_compat to jpod_vehicle_anim.rb (commit e8535f9)
    result:   model.json loaded correctly; pts count correct (80+32=112).
              BFS fails: "no connected path from s006 to s007 in 114 segments"
              RED FLAG: "No departure tracks from S006 CP0 (formation=station_thru_dip)"
    revealed: Two independent bugs: (1) routing_graph in model.json only has pass-through
              successors — landing branch gw_near_main_1→gw_platform_in missing, and
              gw_cp_out_N has no inter-station segment successor.
              (2) _departure_tracks/_arrival_tracks/_passthrough_tracks read chain keys
              at lj[] top-level but v5 lines.json puts them under lj['natalie'].

  - try:      noelle.rb: add Pass 2 (chain pairs from lines.json) and Pass 3 (inter-station
              segment links) to generate_network_json. jpod_animator.rb: hoist lj['natalie']
              to top-level in _load_formation_lines. (commit f404789)
              Run Build to regenerate model.json, then Animate.
    result:   succeeded
    revealed: Both fixes needed together. noelle.rb fix produces complete routing_graph so
              BFS finds full topology; jpod_animator.rb fix makes _build_route_json fallback
              work correctly for v5 templates if BFS ever fails.

principle: model.json routing_graph must be built from THREE sources:
           (1) st['tracks'] successors (pass-through chains from map.json)
           (2) consecutive pairs from ALL chain sequences in lines.json natalie section
               (landing, exit, pass, originating) — this is where branching connections live
           (3) inter-station links: cp_out_N→seg_id and seg_id→cp_in_N from connections_out
           Without (2), gw_near_main_1 has only the pass-through successor and BFS cannot
           reach gw_platform_in. Without (3), BFS cannot leave the station.
           Also: v5 lines.json moves all chain sections under 'natalie' sub-key.
           Any consumer that reads originating_chains/exit_chains/landing_chains/pass_chains
           must handle v5 by looking under lj['natalie'] or by normalizing on load.
