# TFTS — 2026-06-13T05:28:08Z

problem:   Park maneuver starts 23865mm from pod position after hold_loop landing — cp_gap=severe
fault_ref: (no separate fault file — gap detected in trip-report-NORA_0003 during Shuffle Test)

arc:
  - try:      Assume gw_platform's first point (gw_pts_first) is the entry end.
              Sally builds slot positions from gw_pts_first; park maneuver starts there.
    result:   failed — cp_gap=23865mm on every hold_loop landing.
              Pod arrives at x=20096 (gw_pts_last) but park_start=x=-3769 (gw_pts_first).
    revealed: SketchUp stores gw_platform's edge running exit→entry (last point = entry, first = exit).
              Sally's assumed orientation was exactly backward.

  - try:      Use gw_near_main as entry-end anchor in init_from_model Pass 3.
              gw_near_main spans the full width of the station; proximity to gw_platform
              first/last was expected to identify the entry side.
    result:   failed — gw_near_main connects at both ends of the station (it runs from the
              gw_cp_in_lead_0 side at 9 o'clock all the way to gw_cp_out_lead_1 at 3 o'clock).
              Both endpoints are equidistant from gw_platform's first and last points in y,
              so the proximity signal was ambiguous. No flip occurred; orientation stayed wrong.
    revealed: The anchor track must connect directly and exclusively to gw_platform's entry end.
              A bypass track that spans the full station cannot distinguish the two ends.

  - try:      Add gw_platform_in2 and gw_platform_in1 to the front of the anchor priority list
              in init_from_model Pass 3. gw_platform_in2 feeds directly into gw_platform at
              its entry end — one of its endpoints is at exactly x=20096 (the entry point).
    result:   succeeded — Sally detects d_last < d_first (gw_pts_last is near gw_platform_in2),
              reverses pts, builds ps1 at x=18846 (entry side).  Park maneuver starts at x=20096
              where the pod actually is.  Gap = 0.

principle: The entry-end anchor for gw_platform must be the track that physically feeds INTO
           gw_platform at the entry — gw_platform_in2 (or gw_platform_in1 as fallback).
           Never use a bypass track (gw_near_main) as the anchor because it spans both ends
           of the station and gives no directional signal when the parking edge direction
           is reversed relative to flow direction.

           CCW orientation must be stated explicitly in any template that has a reversed
           parking edge: gw_platform pts[0] = EXIT end (3 o'clock / outbound),
           pts[-1] = ENTRY end (9 o'clock / inbound).  This is not a bug in the model —
           SketchUp edge direction is arbitrary — but every tool that reads gw_platform
           pts must be aware of it and use gw_platform_in2 proximity to orient.

domain:    SU
