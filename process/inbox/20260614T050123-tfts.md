# TFTS — 2026-06-14T05:01:23Z

problem:   station_thru_dip platform_shuffle: pod moves backward onto gw_platform_parking after tag fix
fault_ref: (no fault file — symptom observed during live test run)

arc:
  - try:      Fix model mis-tag — rename gw_platform_parking tag from 'gw_platform' to 'gw_platform_parking'
    result:   New failure: pod now drives backward onto gw_platform_parking instead of parking on gw_platform
    revealed: The fix that was correct in the model exposed a bug in Trip 2 dispatch code

  - try:      Hypothesize on_maneuver_complete not triggering hold_loop_return state
    result:   Wrong — hold_loop_return phase IS set at dispatch time (line 2823). sequencing? returns FALSE
              after :land promotion (pod_states deleted). The issue is not in Sally's state machine.
    revealed: The problem is DOWNSTREAM of Sally — in Natalie's Trip 2 dispatch code

  - try:      Trace what parking track Trip 2 selects after the tag fix
    result:   Found the bug: Trip 2 at line 2971 searched for gw_platform_parking in lookup_cache first.
              Before fix: gw_platform_parking not in cache (mis-tagged) → fell through to gw_platform ✓.
              After fix: gw_platform_parking IS in cache → used as parking track → drove pods backward.
    revealed: The Trip 2 comment ("Trip 2 drives pod INTO gw_platform_parking") was wrong for
              station_thru_dip. gw_platform_parking is the approach siding; gw_platform is the parking zone.

  - try:      Fix Trip 2 to use Sally.parking_chain_for(arrive_sid) as the authoritative parking track.
              For station_thru_dip: parking_chain=['gw_platform'] from lines.json.
              Added Sally.parking_chain_for method. Removed gw_platform_parking priority.
    result:   PENDING CONFIRMATION (code written, not yet tested)
    revealed: n/a

principle:  parking_chain in lines.json is the authoritative parking track for each template.
            The lookup_cache must NOT be used to infer the parking track — presence of
            gw_platform_parking in the cache does not mean that it is the parking zone.
            Always use Sally.parking_chain_for(station_id) for Trip 2 track selection.

domain:    SU

files_changed:
  - jpod_sally.rb: added Sally.parking_chain_for(station_id) method
  - jpod_vehicle_anim.rb: Trip 2 track selection (line ~2976) uses parking_chain_for
