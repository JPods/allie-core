# TFTS — 2026-06-05T03:54:41Z

problem:   Three routing systems (Natalie BFS, TripPlanner, show_route BFS) produced inconsistent trip formats; route.json was stale and generated incorrect routes
fault_ref: (prior session — TripPlanner S002 missing 'originating.out_cp1')
arc:
  - try:      Fix TripPlanner to handle CP1 chains for S002
    result:   failed — TripPlanner also had map key case mismatch (uppercase vs lowercase seg IDs); only generated 2 of needed routes; architecture was fundamentally broken
    revealed: TripPlanner could not route via live model topology; it depended on pre-computed network.json declarations that didn't cover all actual CPs in the model

  - try:      Delete TripPlanner entirely, isolate BFS from show_route_followme_overlay into _build_route_json, rewire all callers
    result:   succeeded — single canonical BFS over live seg_ groups in model; all routing goes through one engine; route format unified

  - try:      Replace seg_N numbered keys ("seg_1": ["seg_s002_cp1_s003_cp1"]) with actual connection_id as key (value=null)
    result:   succeeded — self-describing format: "seg_s002_cp1_s003_cp1": null; no redundancy

  - try:      Fix JSON button (cmd_trip_json_to_output) to produce same route object format as Show Route
    result:   succeeded after fixing: (1) destination_station_id not set by set_vehicle_destination, (2) platform suffix regex /_P\d+$/i → /[_.]P\d+$/i, (3) midpoint formula for multi-hop round trips

  - try:      Purge show_trip_path_by_id, Show Routes console action, cmd_show_trip_detail, cmd_show_trip_path, generate_random_closed_trip
    result:   succeeded — 321 insertions / 713 deletions; route-list panel, CSS, JS callbacks all removed

principle: When three systems produce the same output, delete two. Route.json is never generated again — BFS over live model is the authority. The canonical format (connection_id as key, null value, station.cp keys for tracks) is self-describing and requires no external lookup.
domain:    SU
