# TFTS — 2026-06-12T04:15:31Z

problem:   Multi-hop trips through intermediate stations had empty gw_* track sequences + pass-through collision risk

arc:
  - try:      Trip JSON showed s003.0:[] and s004.1:[] — _passthrough_tracks returned empty
    result:   failed
    revealed: pass_chains section missing from station_thru_dip and JPods_station_parking lines.json

  - try:      Author pass_chains from lines{} successors topology (cp0→cp1 and cp1→cp0 paths)
    result:   succeeded — _passthrough_tracks now returns correct track lists
    revealed: Noelle also needed to read pass_chains during Build/Validate to store in tmpl_behaviors and warn when absent

  - try:      Sally @@passing registry with NoraPod maneuver callbacks for collision avoidance
    result:   latent — callbacks not wired; and architecture mismatch: Sally is not the authority
    revealed: "Natalie knows where every vehicle is on the network. She is instructing their trips.
               In su she is plotting their animation. My view is Natalie owns the animator." — Bill

  - try:      Natalie fleet scan in _dispatch_next_sequential — check @@pods for any pod whose
              maneuver id matches "#{sid}.gw_near_main*" or "#{sid}.gw_far_main*"
    result:   succeeded — Natalie uses her existing fleet knowledge; no unimplemented callbacks needed
    revealed: The maneuver id already encodes station + track ("s003.gw_near_main_1"); Natalie can
              determine mainline occupancy from current_maneuver[:id] alone

principle: Natalie owns the animator and knows every pod's position. For SketchUp collision
           avoidance, scan @@pods directly using maneuver ids — do not delegate to a separate
           registry that requires external callbacks to stay current. Sally's @@passing registry
           is the physical-network authority (MQTT events), not the SketchUp authority.

domain: SU
