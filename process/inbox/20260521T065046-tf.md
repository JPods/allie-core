# TF — 2026-05-21T06:50:46Z

summary: beam_path is the single source of truth for all path geometry — followme and animation must read it directly, not reconstruct via proximity stitching.

code:    jpod_animator.rb :: show_trip_path_by_id, JPodVehicleAnim.start, stitch_structure_followme_paths

context:
  Show Routes drew wrong paths through station interiors because structure_followme_paths
  used proximity matching to stitch intra-station tracks. The stitch had no knowledge of
  route order and connected gw_stub_pair_0_in → gw_uturn_0 (> 90° wrong turn).
  Animation had the same defect via build_full_animation_path.

  The fix is simple: every JPods Guideway group already stores beam_path — the exact
  Bezier + PathBuilder output that FollowMe used to build the physical geometry.
  That attribute IS the path. Reading it directly is authoritative and requires no
  proximity guessing.

principle:
  If a canonical record exists (beam_path on every built guideway group), read it.
  Never reconstruct by proximity what can be retrieved by lookup.
  Proximity stitching is a last resort, not a primary algorithm.

domain: SU
