# TFTS — 2026-06-18T02:20:00Z

problem:   Cross-station animation (s006→s007) failing — "no connected path" BFS failure
           in 2_thru_dip network model after Z_CONTINUITY fixes.

fault_ref: (no separate fault file — error reported in SketchUp console during animation)

arc:
  - try:      Add proximity guard (next if adj[nxt].include?(lid)) to prevent backward
              gw_platform_parking→gw_platform_in link; add Pass 2.5 in noelle.rb to read
              designer.tracks successors into routing_graph; add originating_chains to
              Pass 2 chain reading
    result:   Animation still failed. Proximity guard was correct. Pass 2.5 changes
              require REBUILD to take effect — routing_graph in network.json still lacked
              gw_platform_parking→gw_platform link.
    revealed: All noelle.rb changes require a REBUILD to update network.json. The
              animation code reads network.json at start — stale network.json = stale
              routing_graph regardless of code changes.

  - try:      Rebuild 2_thru_dip model to regenerate network.json with Z_CONTINUITY-
              correct pts and Pass 2.5 routing_graph entries.
    result:   Succeeded. network.json now has 40 routing_graph entries (was 36).
              s006.gw_platform_parking→s006.gw_platform and
              s007.gw_platform_parking→s007.gw_platform added. Animation works:
              6 pods routing s006↔s007, Sally parking queue functioning, trips
              completing with minor_defects verdicts (1 CP gap of 2.6mm per trip).
    revealed: The missing link was gw_platform_parking→gw_platform. This is the final
              step of the landing sequence (end of landing_chain → gw_platform). It
              was declared in designer.tracks.successors in lines.json but was not
              being read into the routing_graph. Pass 2.5 (generate_network_json) now
              reads designer.tracks successors after chain reading.

principle: Build artifacts are authoritative for animation. Any noelle.rb change that
           affects routing_graph, geometry pts, or successor links requires REBUILD
           before animation will see the change.

           The landing sequence ends at gw_platform_parking. The originating sequence
           starts at gw_platform. The gw_platform_parking→gw_platform link (declared
           in designer.tracks.successors) is required to complete the BFS path from
           departure platform to arrival platform. Without this link, BFS exhausts
           at gw_platform_parking and fails.

           Follow-up items from this arc:
           1. Reversed pts: gw_platform_out, gw_near_main_1, gw_platform_in,
              gw_platform_parking have pts stored in wrong direction in network.json.
              Animation reverses them at runtime (fwd_gap vs rev_gap check). Fix:
              normalize pts direction in generate_network_json based on routing flow.
           2. Stale uppercase connections (seg_S006_cp0_S007_cp0 etc.) in
              network_definition cause Noelle review to block. Delete from model.
           3. 130 proximity fallbacks per BFS — extra geometry tracks (gw_0_in,
              gw_c_0_0, etc.) have no routing_graph entries. Proximity runs for all.
              Noisy but not blocking.

domain:    SU
