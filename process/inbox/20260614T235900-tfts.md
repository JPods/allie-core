# TFTS — 2026-06-14T23:59:00Z

problem:   CW direction regression after bezier arc fix; plus vehicles jump instead of
           following trip.json sequentially

fault_ref: 20260614T230000-tfts.md (downcase fix session)

arc:
  - try:      After .downcase fix landed, run animation to confirm CCW bezier arcs.
    result:   Bezier arcs correct (13-14 pts) but vehicles running CW.
    revealed: Path.json is generated from physical SketchUp model geometry. The ene_railroad
              arc centerlines store pts in CW-physical order. Path.json records those pts.
              The edge merge condition '&& json_pts.size <= 2' blocked the 14-pt ene_railroad
              arcs from overriding the 13-pt path.json arcs. So path.json CW arcs were used.
              The proximity flip in build_maneuvers_from_ids does NOT universally fix CW pts —
              it orients each segment relative to prev_end_pt, but the FIRST segment has no
              prev_end_pt so it takes stored direction. This chains into wrong orientation
              for the full ring.

  - try:      Remove the '&& json_pts.size <= 2' guard from the edge merge condition so
              the 14-pt ene_railroad arc centerlines can override 13-pt path.json arcs.
              Also fix rev_ok: when edge endpoints are reversed relative to json chord,
              reverse the edge pts before storing (prevents CW storage).
    result:   14-pt ene_railroad arcs now win over 13-pt path.json arcs. Vehicles follow
              correct CCW direction through the traffic circle. Bezier arcs preserved. ✓
    revealed: The ene_railroad stored 'path' attribute (followme_track_centerline_from_attributes)
              is the authoritative source for arc shape AND direction. path.json records
              physical geometry which may be CW. Edge should always win when it has more pts
              and endpoints align.

  - try:      Add line-transition logging: @current_line in NoraPod, _log_line_change,
              seq_plan in trip plan JSON.
    result:   Each gw_/seg_ transition logged to <model>/trip_reports/<model>-line-transitions.jsonl.
              Trip plan now includes seq_plan: flat sequential list for easy reading. ✓
    revealed: n/a

principle: ene_railroad stored 'path' attribute is the ground truth for arc centerline
           geometry (shape AND direction). path.json records physical model geometry which
           can be CW. The edge merge must allow ene_railroad to override ANY existing entry
           (not just 2-pt chords) when it has more pts and endpoints align.
           Rule: edge_entry[:pts].size > json_entry[:pts].size (no size <= 2 restriction).
           Rule: rev_ok → reverse pts before storing so orientation matches json chord.
           The coord log (anim-coords.jsonl) source field shows 'edge_attr' when ene_railroad
           wins; 'geometry' when path.json wins. After the fix, gw_c_ long arcs should show
           'edge_attr' not 'geometry'.

domain:    SU
files:     jpod_vehicle_anim.rb: edge merge condition, rev_ok fix, @current_line,
           _log_line_change, seq_plan in _build_plan_json
commit:    6e8f4e3
