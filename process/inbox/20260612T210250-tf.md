# TF — 2026-06-12T21:08:00Z

summary: Z_CONTINUITY Δ=105mm persists after baseline reset because two code paths use different
         CP-center sources: cp.json (synthesis) vs entity attribute (seg_ endpoints). When cp.json
         is stale the delta is non-zero. Fix: noelle.rb synthesis must call StructurePlacer.connection_point
         for cp_gate_world — the identical source used by generate_map_json for seg_ endpoints.

code:    noelle.rb:2220 (cps_data.each loop — cp_gate_world assignment)
