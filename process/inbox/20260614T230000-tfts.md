# TFTS — 2026-06-14T23:00:00Z

problem:   gw_c_* arc segments animate as straight-line chords — 1m bezier approximation violated
fault_ref: (coord log 20260614T220645-anim-coords.jsonl — all s001.gw_c_* show 2 pts)

arc:
  - try:      Trace why gw_c_* tracks only have 2 pts in animation despite path.json
              having 12–13 segments (932mm spacing) for each.
    result:   Path.json data IS correct (12 segments, ~932mm each). Edge lookup
              generates 14-pt arc entries. But they never reach the route path.
    revealed: Two separate key namespaces: lowercase (s001.*) from map.json/path.json,
              uppercase (S001.*) from edge_lookup. They never merge.

  - try:      Inspect the coord log (20260614T220645-anim-coords.jsonl) for gw_c_ entries.
    result:   Found both: s001.gw_c_0_0 (2 pts) and S001.gw_c_0_0 (14 pts).
              The edge lookup adds S001.* entries independently because json_entry is nil
              for that key. Then id_lookup upgrade looks up lowercase key → gets 2 pts.
    revealed: Root cause: jpod_animator.rb line ~2050:
              sid = ...get_attribute('JPods', 'structure_id')...strip.UPCASE
              All edge_lookup keys built as "S001.gw_c_0_0" — uppercase. Never matched
              against "s001.gw_c_0_0" (lowercase from map.json). id_lookup upgrade
              looks up lowercase → chord only → 2 pts used for animation.

  - try:      Change .upcase → .downcase on line 2050 of jpod_animator.rb.
    result:   Edge_lookup now builds "s001.gw_c_0_0" keys (lowercase) — matches
              map.json keys exactly. Merge replaces 2-pt chord with 14-pt arc.
              id_lookup upgrade finds dense pts. Animation follows arc. ✓
    revealed: n/a

principle: Every lookup key in the animation system must use lowercase station IDs.
           The structure_id attribute is stored as uppercase ("S001") in .skp entity
           attributes (from next_structure_id: "S%03d" % n), but all map.json,
           path.json, and lookup keys use lowercase. Any code that reads structure_id
           and builds lookup keys must call .downcase (not .upcase or unmodified).
           The coord log (anim-coords.jsonl) is the diagnostic tool — compare
           pts_count for s001.* (lowercase) vs S001.* (uppercase) entries. Both
           appearing is the symptom; the cause is always a .upcase somewhere.

domain:    SU
files:     jpod_animator.rb line ~2050: .strip.upcase → .strip.downcase
