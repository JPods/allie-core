# TFTS — 2026-06-13T06:00:00Z

problem:   compact_platform_idle builds park maneuver in wrong direction for pods near exit end
fault_ref: (observed in Shuffle Test log: "maneuver gw_platform_park_ps9 start=(-3769,-1039) end=(17481,-1039) len=836.6in")

arc:
  - try:      compact_platform_idle (animate path) orients gw_platform pts using pod_pos
              proximity: if pod is closer to pts.last than pts.first, reverse the polyline.
    result:   failed — a pod at ps8 (x≈-154mm, near exit end) is closer to pts.first
              (x=-3769, exit) than pts.last (x=20096, entry), so the condition
              dist_rev < dist_fwd is false. No reversal. p_pts stays exit-first.
              _pts_up_to_dist then measures from the exit end; park maneuver starts at
              x=-3769 and ends at x=17481 (21250mm from the wrong origin).
    revealed: Pod-position proximity cannot distinguish entry from exit when the pod is
              already parked near the exit end. The proximity signal inverts exactly when
              the pod is at the half where the answer matters most.

  - try:      Replace raw pod_pos proximity with _platform_pts_entry_first, which uses:
              (1) gw_platform_parking endpoint proximity (most reliable),
              (2) Sally ps1 slot_position as anchor (Sally's own reference frame),
              (3) pod_pos as last fallback only (unreliable, retained for legacy).
    result:   succeeded — p_pts is now entry-first (pts[0]=20096, entry/9 o'clock end).
              slot_dist for ps9 = (9-0.5)*slot_spacing_in ≈ 836.7in measured from entry.
              Park maneuver traverses entry→exit, clipped by seed_pos to pod's actual
              position. No full-platform traversal in wrong direction.

principle: Pod-position proximity to orient a platform polyline is unreliable when the
           pod is parked at or near the exit end — the very case where compaction operates.
           The correct anchor is either gw_platform_parking (a dedicated reference geometry
           at the entry end) or Sally's ps1 slot_position (computed from the same
           entry-first frame used in init_from_model).
           Rule: any code that orients gw_platform pts must call _platform_pts_entry_first,
           not implement its own pod_pos proximity check.

domain:    SU
