# TFTS — 2026-06-12T17:20:00Z

problem:   Z_CONTINUITY faults — multiple sessions of wrong-architecture fixes;
           Bill corrected: "It concerns me that you are referencing stubs we removed
           2 weeks ago." and "No. Look at github su_jpods_claude from yesterday"

arc:
  - try:      stub_tip_world reading geometry.json for seg_ endpoint Z
    result:   wrong architecture — stubs were removed 2 weeks ago
    revealed: the remote branch (origin/su_jpods_claude 6f69a7e) already solved
              Z_CONTINUITY with a simpler approach; we drifted 29 commits away

  - try:      cp_marker rail_tips — read rail endpoint vertices from cp_marker
              component to get outer tip Z for seg_ startPoint
    result:   Bill rejected: "No. Look at github su_jpods_claude from yesterday"
    revealed: the correct fix does not read geometry.json, stubs, or rail_tips AT ALL;
              it synthesizes gw_cp from cp_center using tangent × z_up (horizontal perp)
              so out_gate.z = cp_center.z — same source as seg_ startPoint Z
              → Z_CONTINUITY delta = 0 trivially

  - try:      Restore 4 files (jpod_network.rb, jpod_noelle_bridge.rb, noelle.rb,
              jpod_structure_tool.rb) to origin/su_jpods_claude baseline (6f69a7e)
    result:   succeeded — build pipeline now uses:
              compute_anchor_zs: from_z = from_cp[:center].z + Constants::BEAM_DEPTH
              BeamExtruder: stub_tips parameter vestigial, NO Z pinning
              noelle.rb seg_ Z: cp_center.z * mm (no geometry.json, no stub_tip_world)
              noelle.rb gw_cp synthesis: out_gate.z = cp_center.z (horizontal perp)
              Z_CONTINUITY: delta = 0mm because both sides derive from cp_center.z

principle: Z_CONTINUITY is satisfied trivially when both gw_cp outer tip and seg_
           startPoint are synthesized from the same datum: cp_center.z.
           The remote branch achieved this by making gw_cp synthesis compute the
           lateral offset in XY only (tangent × z_up = horizontal perpendicular),
           so the Z of the synthesized gw_cp outer tip equals cp_center.z exactly.
           Do not read geometry.json, stubs, or rail_tips to get Z — the cp_center
           IS the correct Z source, and math keeps all derived positions consistent.
           The 187.5mm discrepancy in the prior TFTS (20260612T122340) existed because
           geometry.json stores gw_cp at a DIFFERENT Z than cp_center (it is the actual
           geometric outer tip of the stub model). The synthesis approach avoids this
           discrepancy entirely by never consulting geometry.json for Z.

domain: SU
