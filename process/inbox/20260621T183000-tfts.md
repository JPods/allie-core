# TFTS — 2026-06-21T18:30:00Z

problem:   SU v2 animation simulates flat-speed, counter-only ezone behavior — physical systems have curve-radius speed caps, time-window gap-finding, three-zone ToF spacing, and session telemetry that feeds dispatch tuning. SU produces none of this.

arc:
  - try:      Forward/backward review of v2 code — found 3 bugs (Noelle dedup inverted, Sally double-registration, Compute succ_of overwrite) but no physical-realism issues surfaced
    result:   Code correct but simulation behavior diverges from physical
    revealed: Review finds interface bugs but not behavioral gaps — need to compare against physical code to find those

  - try:      Survey all 4 physical codebases (JPodsSM_RPi, JRobots_4WD, JRobots_FullScale, JRobots_SkyRide) and add su-real comments to all 8 v2 modules
    result:   Identified 3 high-value behavioral gaps between SU and physical
    revealed: Physical motor.py has speed priority stack (blocked > ezSpeedAdj > speedSet), mapSM.json has maxLateralG=0.3 + curveRadius per ezone, ezone.py uses time-window gap-finding not linear scaling, physical logs both pod IDs per stop-wait

  - try:      Implement curve-radius speed cap (Nora propose + Animation circumradius calc), rich ezone stop-wait logging (pod pairs + timing), session summary JSON output (per-station throughput + ezone data)
    result:   succeeded
    revealed: Three concrete bridges from SU to physical — speed cap makes ezone timing realistic, pod-pair logging identifies which routes create conflicts, session summary feeds physical Natalie dispatch tuning

principle: Simulation must model the physical constraints that affect timing — curve-radius speed limits and ezone time windows — or the simulation's throughput predictions are meaningless for physical deployment. The bridge is data files: session summaries, trajectory JSONL, ezone pair logs.

domain:    [SU]
