# TFTS — 2026-06-18T00:00:00Z

problem:   path.json was the primary geometry source at animation start, causing 34 gw_ track pts to be overridden by inferior (world_xf-only, no inner_xf) coordinates, 9 reversals, and 154 proximity fallbacks
fault_ref: (inline discovery — no separate FAULT file)

arc:
  - try:      Add track_directions to network.json at Build; pre-orient gw_ pts in model_json_to_map_compat using departure direction vectors
    result:   No change to reversals or fallbacks
    revealed: path.json load happens after model_json_to_map_compat; path.json's 34 gw_ track entries override the pre-oriented network.json pts before build_maneuvers ever sees them (log: "34 gw_ tracks overridden")

  - try:      Inspect what network.json already contains for connections
    result:   Discovered that designer.connections already has full bezier pts (108 pts for seg_s006_0_s007_0, 29 pts for seg_s006_cp1_s007_cp1) — written by Build
    revealed: path.json is redundant for geometry. network.json already has everything. The only unique content in path.json is the defect summary (chain_breaks, blocking_chain_breaks, name_mismatches)

  - try:      Remove path.json from animation read path; stamp defect counts into network.json build section at Build; use map_lookup (network.json) as the single geometry source
    result:   succeeded
    revealed: gw_ pts were always correct in network.json (inst_xf*inner_xf); the merge was actually degrading them. seg_ pts were already in network.json with full bezier resolution. path.json was a legacy file that silently imposed the wrong coordinates for gw_ tracks on every animation start.

principle: When a file is written at Build time and "merged" into a primary source at runtime, check which direction the merge actually runs. If the secondary source overrides the primary (even for a subset), the secondary source is the actual primary — and the data authority is wrong. Fix the authority, not the merge logic.

domain: SU
