# TFTS — 2026-06-16T06:47:20Z

problem:   advance SKIPPED: path too short — pods stuck at ps3, cannot advance to ps4

fault_ref: (no explicit FAULT file — symptom was repeating log line from prior session)

arc:
  - try:      Investigated SystemClock — hypothesis was "clock not initiated"
    result:   failed (wrong hypothesis)
    revealed: SystemClock.register calls _start immediately; clock IS running

  - try:      Traced init_sequencers_from_model — found `formation_id` attribute absent on
              2_thru_dip station entities → count=0 → no sequencers initialized
    result:   identified root cause
    revealed: Without sequencer, ptrack falls back to st[:parking_track] = 'gw_platform_parking'
              (Pass 1 sets this because that track exists in lookup_cache for thru_dip template).
              Slot positions are then computed on gw_platform_parking (12000mm track).

  - try:      Traced advance_pod_slot — found hardcoded `gw_key = "#{sid}.gw_platform"`
    result:   identified why positions don't match
    revealed: from_pt (on gw_platform_parking) is NOT collinear with gw_platform segment.
              _pts_from_point returns [from_pt] → advance_pts.size=1 → "path too short" every tick.

  - try:      Three-part fix:
              1. init_sequencers_from_model reads template from network.json/model.json
                 when formation_id entity attribute is absent
              2. init_from_model stores actual ptrack as st[:slot_track]
              3. advance_pod_slot uses Sally.slot_track_for(sid) instead of hardcoded gw_platform
    result:   succeeded
    revealed: The formation_id attribute is not being written to station component instances
              in 2_thru_dip. The network.json stations[sid]["template"] is the fallback authority.

  - try:      Refactor Sally's two core arrays to ParkingSlot + PlatformVehicle Struct objects
              with broadcast to Noras on every state change
    result:   succeeded
    revealed: Clean object model eliminates the confusion between position-definition (slot) and
              occupancy-tracking (who is in the slot). The invariant (ParkingSlot.occupant_id ==
              PlatformVehicle.slot) is now checkable and auto-corrected by _check_slot_consistency.

principle: |
  Sally's ptrack (which track slot positions are computed on) must match the track
  advance_pod_slot clips. If they diverge, every advance fails with "path too short".
  The slot track is stored as st[:slot_track] and exposed via Sally.slot_track_for(sid).
  
  Separately: when formation_id is absent on entity, read template from network.json
  (stations[sid]["template"]) — this is always current after Build, entity attrs may lag.
  
  Sally's two arrays (parking_slots, platform_vehicles) must maintain the invariant:
  ParkingSlot.occupant_id ↔ PlatformVehicle.slot. Structs make this checkable.
  Broadcast after every state change so Nora can enforce "occupied slot = refuse instruction."

domain: SU
