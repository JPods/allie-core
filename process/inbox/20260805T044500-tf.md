# TF — 2026-08-05T04:45:00Z

summary: All bulk data enters WC3 only through bundles — never raw import parsers. Alice owns the transformation in her own database (alice_conversion).
code: apps/conversion/services/converter.py — full pipeline
domain: WC3

The principle: WC3 records clean data. It does not parse messy data. The noise of data conversion — column mapping, oddity resolution, format normalization — lives in Alice's separate database. What arrives at /sync/receive/ is already correct.

Built today: apps/conversion/ Django app with 6 models (ConversionProject, SourceFile, ColumnMap, Oddity, StagingRow, PassLog), Claude Haiku integration for intelligent column mapping, multi-pass conversion pipeline, and CLI management command.

Removed today: import_items_tsv.py, import_bom_tsv.py, import_actions_from_csv.py, ImportExportModelAdmin from 3 admin classes. Archived to archive/import_removed_2026-08-04/.

Readmes written: data-conversion-framework.md, bundle-import.md

WC2 test data at /Volumes/TempFiles/zzzDataDump/ ready for first real test once Anthropic API key is configured. Item.json: 3,315 records, 142 fields. Known oddities already documented.

Actions: DEV-31139 (review framework), DEV-31140 (run WC2 conversion) — both due 2026-08-25.

Supplier WC3 copy concept: give data suppliers a free WC3 with the conversion app so they produce valid bundles on their own workbench. Their noise stays on their side.
