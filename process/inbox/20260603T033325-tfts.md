# TFTS — 2026-06-03T03:33:25Z

problem:   S004 hold_loop run — six defect classes identified from trajectory JSONL + proof JSON
fault_ref: 20260602T225850-fault.md (gw_uturn_0 arc), 20260602T225857-fault.md (parking shuffle)

arc:
  - try:      Ran hold_loop animation on S004 (station_line_end). Reviewed
              trajectory JSONL (20260603T032632-trajectory.jsonl) and proof
              JSON (20260603T032632-proof-S004.json).
    result:   Pod completed loops and landed at slot 3. Animation ran but
              with six identifiable defects visible in the logs.
    revealed: Trajectory JSONL + proof JSON together give a complete picture
              of what the pod actually did vs. what lines.json declared.
              Offline log review found defects that are invisible to the eye
              in the SketchUp viewport.

  - try:      Cross-referenced proof end_delta values against track lengths.
    result:   Five tracks show end_delta ≈ full track length:
              gw_far_main (19906mm delta, 20442mm length),
              gw_uturn_1 (3502mm delta, 4873mm found_len),
              gw_platform (7683mm delta, 7682mm length),
              gw_lift (6124mm delta, 6124mm length),
              gw_lift_parking (6265mm delta, 6265mm length).
    revealed: end_delta ≈ track_length is the signature of a direction reversal.
              The proof compares declared-end to found-end; when the track is
              reversed, declared-end IS found-start. Fix: regenerate lines.json
              from current model geometry (populate_from_open_template).

  - try:      Checked Z values across all tracks in proof.
    result:   All WARN tracks show exactly 62.5mm Z offset:
              declared Z=5937.5 → found Z=6000,
              declared Z=7937.5 → found Z=8000.
              CP tracks (gw_cp_in_0, gw_cp_out_0, leads) have no offset.
    revealed: Station was re-placed at a different terrain elevation after
              lines.json was last populated. 62.5mm offset is consistent and
              systematic — not random drift. Fix: same as direction reversal —
              regenerate lines.json from current placed instance.

  - try:      Read trajectory pts for landing maneuver (hold_loop[5/5]).
    result:   pts show zig-zag: [-34880] → [-32381] → [-34880] → [-37380].
              Pod travels outward on gw_cp_in_0 then immediately reverses.
    revealed: landing_chains.in_cp0 starts at gw_cp_in_0 — correct for an
              external arrival (pod coming from seg_* via outer CP). Wrong for
              a hold_loop promotion — pod is already INSIDE the station at end
              of gw_cp_in_lead_0. start_near orients gw_cp_in_0 outward because
              the pod's position (-34880) matches gw_cp_in_0's inner end. Chain
              then reverses on gw_cp_in_lead_0. Fix: Sally should use to_platform
              for hold_loop promotions, not landing_chains[cp].

  - try:      Checked gw_uturn_0 in proof.
    result:   ARC status: pts_count=2 in lines.json, found_pts=56, found_len=25172mm
              vs declared 5497mm. end_delta=4901mm.
    revealed: Known defect. lines.json only has chord endpoints for gw_uturn_0.
              Pod cuts straight across 3.5m diameter. Fix: run Generate Template
              Data from open station_line_end template to regenerate arc pts_mm.

  - try:      Checked pts array in hold_loop[6/6] maneuver.
    result:   pts[16] and pts[17] are identical [1750,-78230,5387] — a
              zero-length degenerate segment at the gw_far_main/gw_far_main_2
              junction.
    revealed: build_maneuver_from_tracks concatenates pts without deduplicating
              the shared junction point. simplify_collinear removes collinear
              interior points but not zero-distance duplicates. Fix: strip
              consecutive duplicate points during concatenation.

  - try:      Code fix — Sally: use to_platform for hold_loop promotion instead
              of landing_chains[cp]. Code fix — build_maneuver_from_tracks:
              strip consecutive duplicate pts at track junctions.
    result:   (pending — Bill will re-run)
    revealed: (pending)

principle:  Two-source review (trajectory JSONL + proof JSON) catches defect
            classes that neither source catches alone:
            - proof catches direction reversals, Z drift, arc undersampling
            - trajectory catches behavioral consequences (zig-zag landing,
              duplicate points, arc chord-cutting)
            Run both at every animation start. Review both after every run.
            The distinction between external-arrival landing chains and
            internal hold_loop promotion paths must be explicit — the same
            chain cannot serve both cases.

domain:     SU
