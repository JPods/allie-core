# TFTS — 2026-06-15T12:31:17Z

problem:   Traffic circle gw_c_* ring arcs animate CW (clockwise) — vehicles travel
           wrong-way on ring; no enforcement of CCW-only rule

arc:
  - try:      Proximity flip in build_maneuvers_from_ids orients each segment relative
              to prev_end_pt. Expected this would produce CCW for ring arcs.
    result:   Proximity flip is insufficient — it orients relative to the prior segment,
              but the FIRST segment in a ring trip has no prev_end_pt, so it takes stored
              direction. If stored CW, the first segment is CW and all subsequent segments
              chain from that wrong direction.
    revealed: Orientation must be fixed AT THE LOOKUP LEVEL before maneuver construction.
              The proximity flip is a connectivity aid, not a direction enforcer.

  - try:      Add enforce_ccw_ring_arcs!(lookup) to RubyNatalie module, called after
              upgrade_arcs_from_beam_path! and synthesize_arc_pts_from_math!.
              Algorithm: group gw_c_* by station, fit ring center from dense anchor arcs
              (≥5 pts), evaluate cross-product at pts[0]→pts[1] for each ring arc.
                tx = pb.x - pa.x;  ty = pb.y - pa.y
                cross_z = tx*(center.y - pa.y) - ty*(center.x - pa.x)
              positive → CCW ✓; negative → CW → reverse pts in-place.
              Both dotted and underscored key variants updated.
    result:   Implemented. Every gw_c_* arc is now guaranteed CCW before any maneuver
              is built. gw_uturn_* not included — proximity flip handles these correctly
              once pts are dense (predecessor gw_cp_out_lead gives prev_end_pt context).
    revealed: The circle fit already performed by synthesize_arc_pts_from_math! provides
              the correct center. enforce_ccw_ring_arcs! reuses the same _fit_circle_xy
              on anchor arcs already in the lookup — no redundant geometry work.

principle: CCW enforcement belongs at the lookup layer, not the maneuver layer.
           Once lookup entries are CCW-correct, build_maneuvers_from_ids proximity
           flips operate on a consistent basis and cannot produce wrong-way travel.
           The cross-product check is the canonical test: cross_z > 0 = CCW.
           Source hierarchy now: beam_path → math synthesis → CCW enforce → mandate guard.
           The CCW pass runs last so it corrects orientation regardless of which
           earlier step produced the pts (Build time beam_path or animation-time math).

domain:    SU
files:     jpod_vehicle_anim.rb: enforce_ccw_ring_arcs! (new method in RubyNatalie);
           call site after upgrade_arcs_from_beam_path! and synthesize_arc_pts_from_math!
