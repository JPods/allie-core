# TFTS — 2026-06-07T04:33:09Z

problem:   show_followme_json_overlay completely missing — NoMethodError on load_map_json
fault_ref: (no fault file — observed as "uturn, gw_cp_*, and traffic_circle missing or square")

arc:
  - try:      Investigated path.json and map.json to find why arcs appeared square.
    result:   path.json had 12-segment curved data (generated at 03:51Z). map.json had
              2-pt entries for all curved intra-station tracks (gw_c_0_0, gw_uturn_0, etc.)
              because followme.json carries pts=0 for these tracks.
    revealed: Two separate bugs:
              (1) show_followme_json_overlay calls JPods::JPodVehicleAnim.load_map_json —
                  NoMethodError since the module reorganization moved it to
                  RubyNatalie.load_map. Entire overlay crashes, nothing drawn.
              (2) Even when working, no path.json merge — map.json 2-pt entries dominate,
                  all arcs appear as straight lines.

  - try:      Fix show_followme_json_overlay to use RubyNatalie.load_map + build_line_lookup,
              add path.json merge (same pattern as show_route_followus_overlay).
    result:   SUCCEEDED. Overlay no longer crashes. Curved arcs (gw_uturn, gw_c_*,
              gw_platform_in) get 12-segment polylines from path.json instead of
              2-pt fallback from map.json.

principle: When a module is reorganized (RubyNatalie carved out of JPodVehicleAnim),
           every caller must be updated. grep for the old module.method name before
           declaring the refactor complete. A caller that references a removed method
           raises NoMethodError at runtime — silent at refactor time.

secondary: map.json has 2-pt entries for intra-station curved tracks because followme.json
           carries no pts for them. Any overlay that reads ONLY map.json will show squares
           for gw_uturn, gw_c_*, gw_platform_in, etc. The fix: always merge path.json
           (which has full curved polylines from extracted.json + corrections) into the
           overlay lookup. path.json wins on conflict.

animation_note: JPodVehicleAnim.start auto-regenerates path.json via PathJSON.export,
                then merges path.json (wins) + map.json (gap-fill). Animation was never
                broken — it always had the correct curved paths.

domain:    SU
