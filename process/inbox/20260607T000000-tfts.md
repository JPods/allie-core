# TFTS — 2026-06-07T00:00:00Z

problem:   gw_platform_in1/in2 not animating — pod bypasses platform landing, continues on gw_near_main
fault_ref: (no fault file — observed by Bill on s003 and s004 parking stations)

arc:
  - try:      CCW/naming direction correction covered only gw_cp_in_* and gw_cp_out_*.
              gw_platform_in* and other ramp tracks received no direction correction.
    result:   Pod at s003/s004: gw_cp_in_lead_0 exits at (39945.4, -4539.3). Animator
              searches for successor starting there. gw_platform_in1 first pt = (29935.2,
              -3024.2) — no match. gw_near_main first pt = (-23618.2, -4539.3) — also
              no match. gw_platform — no match. Pod jumped directly to gw_platform via
              some fallback. Sally had no visibility into the bypass.
    revealed: gw_platform_in1 is stored backward in extracted.json: last pt = (39945.4,
              -4539.3) = the diverge point. CCW extraction order places this at the END,
              not the beginning. The existing direction correction only matched gw_cp_in_*
              and gw_cp_out_* by regex — gw_platform_in_*, gw_platform_out_*, gw_lift_in,
              gw_lift_parking were silently left uncorrected.

  - try:      Extend centroid-distance direction correction to cover all inbound/outbound
              ramp tracks: gw_platform_in_*, gw_lift_in, gw_lift_parking (inbound);
              gw_platform_out_* (outbound). Same rule: farther end from station centroid =
              outer (peripheral) end. Inbound: outer end must be FIRST. Outbound: outer end
              must be LAST.
    result:   SUCCEEDED. gw_platform_in1 reversed → first pt = (39945.4, -4539.3) ✓.
              Animator finds it immediately after gw_cp_in_lead_0. Pod now traverses
              platform landing chain correctly: cp_in_lead_0 → platform_in1 → platform_in2
              → platform.

principle: The CCW/naming direction correction must cover ALL station tracks that can be
           stored backward — not just CP tracks. Any track that forms a directional chain
           (inbound ramp, outbound ramp) is subject to the same storage order issue.
           The centroid-distance rule (outer end = peripheral end farther from station
           center) is the universal classifier — apply it by track function category, not
           by regex on CP track names specifically.

secondary: When the animator can't find a successor, it silently falls through to whatever
           happens to match on position. This makes direction errors invisible: no crash,
           no fault, just the wrong track traversed. Sally should flag when a pod traverses
           a track that is not in the declared landing_chain for its arrival CP.

domain:    SU
