# TFTS — 2026-06-24T10:00:00Z
problem:   500mm track gap hallucination causes pod jams at station junctions
fault_ref: session observation — pods jamming at gw_platform_in2 fork
arc:
  - try:      Observe pods stopping at gw_platform_in2 junction
    result:   failed — pods jam, no visible cause
    revealed: Natalie was reversing gw_platform_in2 during maneuver build
  - try:      Check Natalie's maneuver builder for reversal logic
    result:   found — dist_rev < dist_fwd triggers pts.reverse
    revealed: dist_rev was ~500mm — measuring edge-to-edge across dual track (250mm each side)
  - try:      Add 500mm hallucination guard (350-650mm range blocks reversal)
    result:   succeeded — Natalie stops reversing on edge measurements
    revealed: this bug has been fixed before and keeps coming back (compression destroys the lesson)
  - try:      Add Noelle visual defect flags at gap locations
    result:   succeeded — red/orange posts appear in 3D model at defect coordinates
    revealed: users should never have to discover defects by observation
  - try:      Add Nora ride quality kink detection (>15° deflection)
    result:   succeeded — kinks reported in Crew Health
    revealed: Nora speaks for the passenger — ride quality is her domain
  - try:      Add crew flag system — any agent can flag defects at coordinates
    result:   succeeded — flags merge within 2m, count increments, stored in network.json
    revealed: defects must be documented, visible, counted, and specifically approved — no silent tolerance
principle: USE MATH NOT EDGES (Axiom 10). Any ~500mm gap is edge-to-edge, not centerline. The deeper principle: defects must be visible to the user without discovery. Silent tolerance produces failures that only experience can diagnose — and experience gets compressed away.
domain:    [SU] [Cross-domain]
