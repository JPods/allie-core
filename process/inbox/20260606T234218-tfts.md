# TFTS — 2026-06-06T23:42:18Z

problem:   followus ribbon traces FollowMe beam solid cross-section perimeter (115 pts, 36029mm)
           for gw_platform_out instead of the 2-pt centerline (854mm); and short-arc tracks
           like gw_platform_out and gw_c_0_0 need interior Bezier pts, not just chord endpoints.
fault_ref: 20260606T230403

arc:
  - try:    add edge_lookup from vehicle animator to followus overlay for seg_* tracks;
            restrict to seg_* only so gw_* tracks are not touched
    result:  succeeded for seg_* — chord upgraded to curve pts for inter-station segs;
             gw_* cross-section zigzag eliminated from followus
    revealed: edge_lookup reads FollowMe beam solid edges; works for long inter-station
              guideways where spine = longest edge chain; fails for short station tracks
              where cross-section perimeter > spine length

  - try:    in followus ribbon collection, apply len/chord > 2.0 filter to detect
            cross-section perimeter walks; fall back to largest-single-jump pair
    result:  succeeded as band-aid — gw_platform_out ribbon reduced from 115 pts to
             the correct 2-pt chord (854mm); gw_uturn arc offset to centerline (1750mm)
    revealed: root cause is upstream in populate_from_open_template (extraction):
              extracted.json already stores the 115-pt perimeter walk as SOURCE=extracted.
              Band-aid at followus time hides the symptom; extracted.json is still wrong.

  - try:    add len/chord > 2.5 rejection to Priority 1.5 (followme_trace_polyline_from_edges)
            in populate_from_open_template; if ratio > 2.5 reject and fall through to
            Priority 2 (bounding-box endpoints)
    result:  succeeded in preventing cross-section perimeter from entering extracted.json;
             straight tracks now write correct 2-pt result
    revealed: even after this fix, curved station tracks (gw_platform_out exit curve,
              gw_c_0_0 traffic circle arc) produce only 2-pt chords — the curve shape
              is lost. Priority 2 (bounding-box) gives endpoints only; no interior pts.

  - try:    establish design axiom: ALL JPods curves are cubic Bezier. Replace circular
            arc approximation (_arc_pts_from_tangents_mm, Priority A) with exact Bezier
            sampler (_bezier_pts_from_tangents_mm). Use start tangent (sv = B1 handle
            offset from P0) and end tangent (ev = B2 handle offset from P3, arrival
            direction = -ev) to reconstruct B(t) = (1-t)³P0 + 3(1-t)²tB1 + 3(1-t)t²B2 + t³P3.
            Parallel tangents (straight track) → return nil → 2-pt line.
    result:  succeeded — Bezier sampler in place; Priority A now produces correct interior
             pts for any curved single-segment station track that has sv and ev available
             from _track_endpoints_mm (ene_railroad controls[2]/[3] or equivalent)
    revealed: the approach is correct; validation pending after next Extract Template run
              on s002 template; followus ribbon will show correct curves once path.json
              is regenerated from the fixed extracted.json

principle: FollowMe beam solid edges are STRUCTURAL geometry, not PATH geometry.
           Never walk FollowMe solid edges to extract vehicle path pts.
           The correct path sources, in priority order:
             1. ArcCurve edges in template (hand-drawn arcs) — exact, authoritative
             2. Bezier from tangent vectors at endpoints — exact for any curve, any angle
             3. _generate_uturn_arc_pts_mm — fallback for 180° U-turns with no tangents
             4. 2-pt chord — correct for straight tracks, acceptable for short arcs
           The len/chord > 2.5 guard prevents perimeter walks from entering extracted.json.
           The Bezier axiom makes circular arc approximation obsolete.
domain:    SU
