# TFTS — 2026-06-11T05:00:42Z

problem:   hold_loop return pod traversed full platform entry→exit (past target slot), then teleported to entry, then drove to slot — "ps4 to ps1 to ps4"
fault_ref: 20260611T042416

arc:
  - try:      seed_pos from pod.entity.bounds.center to clip park maneuver start
    result:   failed — bounds.center was [-5015,-8042,7094]mm, 7000mm off the platform in Y and Z; projection d_proj >> FOLLOWME_CONNECT_TOL_RELAXED (3m) → @t=0 → pod started from platform entry end, not from arrival position
    revealed: entity.bounds.center lags by one tick and adds pod body geometry offset; unreliable as a seed for maneuver clipping in 3D stations

  - try:      use last_man[:pts].last as seed_pos instead of bounds.center
    result:   improved seed — now correctly projects to pod's actual arrival position; BUT pod still traversed gw_platform entry→exit in the landing chain (past ps4), so the seed projected past the end of park_pts (t=1.0, d_proj=2530mm ≈ within 3m tol) → @t=1.0 → pod snapped to ps4 with zero travel
    revealed: root cause was not the seed — it was that gw_platform was included in the landing chain, causing the pod to overshoot its target slot before park_dispatch fired

  - try:      remove gw_platform from both landing chains (in_cp0, in_cp1) in station_thru_dip/lines.json + use last_man[:pts].last as seed_pos
    result:   succeeded — pod stops at gw_platform_parking exit (= gw_platform entry end [6765,-1039,5913]mm); park_dispatch fires with seed at entry end; pod drives forward entry→ps4 with no backward motion; test confirmed excellent

principle: landing chains must end at gw_platform_parking, not gw_platform. gw_platform is the parking track — it belongs to the park maneuver, not the landing chain. Including gw_platform in the landing chain causes the pod to overshoot its target slot (traverses entry→exit), after which no forward-only park clip is possible. This is the same design as station_line_end (landing ends at gw_platform_parking; park dispatch drives gw_platform to slot). Second principle: entity.bounds.center is not a reliable track position — use last_man[:pts].last (the exact endpoint the animation system placed the pod at).
domain:    SU
