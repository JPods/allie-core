# TFTS — 2026-05-30T23:33:35Z

problem:   Platform slot overlap — compact placed pods at same position as populate
fault_ref: (from vehicle placement failure log 2026-05-30T23:20:21)

arc:
  - try:      Inspect compact_platform_static — it measures slot dist from vpath[0]
    result:   vpath[0] is the EXIT end (t=0 in the animation, blue cone)
    revealed: compact computes slot 6 at 16.5m from exit; populate computes slot 1 at 17.3m from
              entrance (= same physical location); two systems measure from opposite ends → overlap

  - try:      Read spawn_t_for_platform_slot_on_guideway Tier 2 for the correct model
    result:   ti=0 → entrance at total, dir=-1; ti=1 → entrance at 0, dir=+1
              dist_from_vpath_start = ti==0 ? (total - slot_dist) : slot_dist
    revealed: The entrance anchor depends on track_index, not on vpath[0] = always exit

  - try:      Replace compact's flat dist=(new_slot-0.5)*spacing walk-from-start with
              ti-aware: slot_dist=(new_slot-0.5)*spacing, dist_from_start = ti==0 ? total-slot_dist : slot_dist
    result:   succeeded — slot positions now measured from red entrance, matching populate exactly

principle: Both the parking populate (spawn_t_for_platform_slot_on_guideway) and the compact
           function (compact_platform_static) must measure slot positions from the RED ENTRANCE
           using track_index to determine which end of vpath is the entrance.
           vpath[0] is always the exit end for track_index=0, entrance end for track_index=1.
           Any code that walks vpath from index 0 without checking track_index is measuring
           from an arbitrary end and will invert half the time.

domain:    SU
