# TFTS — 2026-06-13T07:00:00Z

problem:   compact park maneuver TickLog delta=-56 — minor_defects verdict on every compact move
fault_ref: (observed in Shuffle Test log: "TickLog NORA_0001 seg=...gw_platform_park_ps9 len=21250mm expected=61.4 actual=5 delta=-56")

arc:
  - try:      compact_platform_idle builds park_pts from entry end (p_pts[0], x=20096)
              to target slot (slot_dist = 21250mm for ps9). park_man[:len] = 836.6in.
              Pod starts at seed_pos (ps8, x=18750mm). receive_maneuver projects seed_pos
              to t_proj≈0.882 on park_pts — pod travels the remaining 18% = 2500mm.
    result:   TickLog uses man[:len] = 836.6in to compute expected ticks.
              expected = 836.6 / (speed_in * ANIM_INTERVAL) ≈ 77 ticks ≈ 61.4s.
              actual ≈ 9 ticks ≈ 5s. delta = -56 → minor_defects on every compact move.
    revealed: park_man[:len] must reflect actual travel distance (from pod to target),
              not the full entry-to-target path length. receive_maneuver already handles
              the seed_pos offset for animation — len is the only mismatch.

  - try:      After building park_pts and computing park_len, project pod_pos onto
              park_pts via _project_t_on_polyline to get t_seed (≈0.882 for ps8→ps9).
              Set remaining_len = (1.0 - t_seed) * park_len. Use remaining_len as
              park_man[:len] instead of park_len.
    result:   succeeded — TickLog expected ≈ 5s, actual ≈ 5s, delta ≈ 0.
              No minor_defects for compact moves.

principle: park_man[:len] must equal the distance the pod actually travels, not the
           full polyline length. When seed_pos clips the maneuver start (receive_maneuver
           sets @t = t_proj), TickLog still reads man[:len] to compute expected time —
           it does not account for the starting offset. Any maneuver built from a longer
           path but entered mid-path must set len = (1 - t_proj) * full_len, or TickLog
           will always report a large negative delta.
           Rule: whenever seed_pos is non-nil and meaningful, compute park_man[:len]
           from the projected remaining fraction.

domain:    SU
