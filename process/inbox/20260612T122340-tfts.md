# TFTS — 2026-06-12T12:23:40Z

problem:   Z_CONTINUITY faults on first build — seg_ endpoint Z did not match
           gw_cp_out/in outer Z; Δ=187.5mm on every seg_ junction

arc:
  - try:      Use map.json stub_tips (two-pass): first build generates map.json
              with correct gw_cp Z values; second build reads them
    result:   worked but required two builds — unacceptable for users
    revealed: the root cause is that `cp_center.z + BEAM_DEPTH` fails when
              entity.transformation has lateral slope (Y→Z coupling); the formula
              is only coincidentally correct on gentle terrain

  - try:      Single-pass: read gw_cp_out outer tip from geometry.json + entity.transformation;
              use as anchor Z for center path; both offset rails inherit same Z
    result:   XY correct; Z still wrong for reverse track — both rails pinned to same
              anchor Z, but the two rails are on opposite Y-positions so they need
              different world Z values on sloped terrain
    revealed: offset_path is XY-only; Z is unchanged from center. Forward rail needs
              gw_cp_out Z; reverse rail needs gw_cp_in Z. These differ by
              2 * half_off * Rzy = 2 * 1750mm * 0.03 = 105mm at 3% slope.
              The architecture had: "center Z → both rails" which is only correct
              on flat terrain.

  - try:      Compute all four rail Zs (from_out, from_in, to_out, to_in); use averages
              for center path anchor; pin each rail endpoint Z in BeamExtruder after
              offset+reverse: track_idx=1 departure=[from_out], arrival=[to_in];
              track_idx=0 departure=[to_out], arrival=[from_in]
    result:   build geometry correct — CP connection is 1st priority; terrain-following
              owns only the interior between the two pinned endpoints
    revealed: geometry.json tracks are in definition-local space (same coordinate
              frame that entity.transformation maps to world space);
              station_thru_dip CP0: gw_cp_out_0 at Y=1250.9mm, gw_cp_in_0 at
              Y=4750.9mm — 3500mm apart (DUAL_TRACK_SPACING). Both rails at the
              SAME Z (9871.4mm local) on the current model (flat terrain).
              Z_CONTINUITY faults persisted → root cause was elsewhere.

  - try:      Investigate source of Z_CONTINUITY faults. Z_CONTINUITY check compares
              seg_ pts[0]['z'] against gw_cp_out.pts.last['z'] in map.json.
              gw_cp_out outer Z = 9871.4mm (from geometry.json, correct).
              seg_ startPoint Z = 9683.9mm (from CP center world Z, set by fdcced6).
              Delta = 187.5mm = gw_cp outer Z - CP center Z. Not BEAM_DEPTH (500mm).
              Fix: generate_map_json now calls Network.stub_tip_world(from_entity,
              from_cp_idx, 'out', geo_cache) for seg_ startPoint Z, and
              stub_tip_world(to_entity, to_cp_idx, 'in', geo_cache) for endPoint Z.
              XY stays from CP center. geo_cache shared across loop (parse-once).
    result:   succeeded — seg_ endpoint Z now matches gw_cp outer Z exactly;
              Z_CONTINUITY Δ = 0mm.

principle: seg_ endpoint Z in map.json must match the Z of the gw_cp track outer tip,
           not the CP center Z. CP center and gw_cp outer tip are 187.5mm apart in Z
           (local geometry of the station template). generate_map_json is the correct
           fix location — the build pipeline (BeamExtruder) was the wrong target.
           Axiom 19: one source of truth — geometry.json + entity.transformation,
           no derived formula, no map.json. geo_cache (keyed by formation_id) is shared
           across a build so each template's geometry.json is parsed once.
           The two rails at a CP gate are 3500mm apart in Y (DUAL_TRACK_SPACING) but
           at the same Z on flat terrain. On sloped terrain they will differ; BeamExtruder
           pinning (the prior arc step) handles that case correctly.

domain: SU
