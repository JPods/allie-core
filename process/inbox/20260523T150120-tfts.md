# TFTS — 2026-05-23T15:01:20Z

problem:   Vehicles stacked at platform entrance instead of parking at assigned slots

fault_ref: (no single fault file — pattern emerged across two animation runs)

arc:
  - try:      Append parking maneuver to Trip 1 at dispatch time (append_parking_maneuver
              called in build_fleet and natalie_dispatch_idle before animation starts)
    result:   Failed — Sally not initialized when build_fleet runs, so parking maneuver
              never appended; legacy teleport fired instead
    revealed: Dispatch time and initialization time are not the same thing. A maneuver
              that depends on Sally cannot be built before Sally exists.

  - try:      Fix legacy teleport direction (orient gw_platform pts from arrival end
              using pod's current position as reference)
    result:   Partially worked — teleport landed closer to correct slot position, but
              pods still stacked at entrance because teleport snapped them backward
    revealed: Teleport is fundamentally wrong for this case. The visual artifact
              (snap-back) is the symptom of mixing travel and parking in the same
              position-update model.

  - try:      Two separate trips: Trip 1 ends at platform entrance (BFS stops before
              gw_platform); at arrival, Sally assigns slot and Natalie dispatches Trip 2
              (pod drives forward into platform to slot position)
    result:   Succeeded — pod drives continuously from network into platform, stops
              at assigned slot under its own motion, no teleport, no snap-back
    revealed: Sally is always initialized before any pod can complete Trip 1, so the
              timing dependency disappears entirely.

principle: Two trips is simpler than one trip with two parts. When a trip has a phase
           boundary — travel vs. parking, normal speed vs. slow, different authority
           (Natalie vs. Sally) — make it two trips. A trip boundary is a natural
           synchronization point: state is clean, the next authority takes over with
           full context, and each trip can be reasoned about independently.
           One trip with two parts requires the first part to pre-configure the second,
           creating a timing dependency that fails whenever initialization order changes.

domain:    SU
