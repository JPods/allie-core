# TFTS — 2026-06-06T22:00:00Z
problem:   Intra-station successor wiring in map.json always produces successors:[] — BFS falls back to endpoint proximity and finds wrong exit sequence (skips gw_cp_out_lead_1, gw_cp_in_lead_1, gw_far_main on station_thru_dip exit)
fault_ref: (observed in 18:17 trajectory — pod routes gw_platform_out→gw_uturn_1 instead of gw_platform_out→gw_cp_out_lead_1→gw_uturn_1→gw_cp_in_lead_1→gw_far_main)

arc:
  - try:      Read generate_map_json in noelle.rb to find where successors are wired for intra-station tracks
    result:   Succeeded — found proximity auto-wiring (line 2630), dep_segs/arr_segs external wiring (line 2663), and lj_chains override from lines.json (line 2692)
    revealed: lj_chains override reads lines.json and wires [role_a, role_b] pairs correctly — BUT none of the wiring fires

  - try:      Trace why station_ids is empty for station s002
    result:   Found case mismatch bug:
              station_ids = line_geo.keys.select { |lid| lid.start_with?(prefix) }
              prefix = "s002."  (lowercase — sid from stations_out.each_key)
              BUT lines_out keys for intra-station tracks = "S002.gw_platform_out" (uppercase)
              because dot_id = "#{station_id}.#{role}" where station_id = m[1] = "S002" (uppercase from regex)
              station_ids is always empty → proximity wiring, clear step, and lj_chains wiring ALL skipped
    revealed: Two bugs:
              1. dot_id uses uppercase station prefix (S002) but sid is lowercase (s002)
              2. dep_segs matching: m[1] == sid fails because m[1]="S002" and sid="s002"

  - try:      Fix both case mismatches in noelle.rb generate_map_json
    result:   Succeeded — two-line fix:
              1. Line 2011: dot_id = "#{station_id.downcase}.#{role}"
                 Normalizes all intra-station lines_out keys to lowercase ("s002.gw_platform_out")
                 matching stations_out.each_key which iterates lowercase SIDs
              2. Line 2659: m[1].downcase == sid  (and m[3].downcase == sid for arr_segs)
                 Makes dep_segs/arr_segs matching case-insensitive for seg_* connection IDs

principle: When lines_out intra-station keys use a different case than the station loop's sid variable,
           every station-level operation silently skips all intra-station tracks: proximity wiring finds
           zero candidates, lj_chains clear step clears nothing, dep_segs matching finds no segments.
           The result is successors:[] on every intra-station line — BFS falls back to endpoint
           proximity which finds a shorter but topologically wrong route.

           The case mismatch was: followme.json connection IDs use uppercase station prefix (S002_gw_platform_out),
           regex extraction preserves that case into dot_id = "S002.gw_platform_out", but
           stations_out is keyed by sid.downcase ("s002"). The `lid.start_with?("s002.")` check
           never matches "S002.gw_platform_out".

           Fix: normalize dot_id to lowercase at the source. The nested_lines output at line 2847
           already applies m[1].upcase so the map.json file structure is unaffected. The animator's
           build_lookup uses key.downcase throughout, so lowercase intra-station keys are handled.
           Segment registry uses canonical = key.downcase for ID assignment — backward compatible.

           dep_segs/arr_segs matching fix: seg_* IDs use uppercase station prefix from followme.json
           (seg_S002_cp0_S001_cp2) → regex m[1] = "S002". Station loop sid = "s002". Comparison
           m[1] == sid always false → dep_segs/arr_segs always empty → gw_cp_out_N never wired
           to inter-station seg_ even after the lj_chains override clears and re-applies.

domain:    SU
