# TF — 2026-06-21T04:10:00Z
summary: Trip Simulator clock starts at booking, not at dispatch. The ETA and progress bar run while Natalie is still building the route and Sally is advancing the pod to departure slot. Fix: Trip Simulator should start its clock when Natalie actually dispatches the pod (pod begins traveling), and show arrival when the pod parks at the destination. Dispatch and arrival events should trigger the Trip Simulator, not timers.
code: dispatch_server.rb:enter_trip_ui, ui/trip/index.html:startTripProgress
