# TFTS — 2026-06-06T23:12:44Z

problem:   followus ribbon traces beam cross-section (0.5m × 0.25m right angles) on gw_platform_out instead of the path centerline; no ribbon over gw_platform_out itself
fault_ref: 20260606T230403

arc:
  - try:    add edge_lookup upgrade to show_route_followus_overlay — same guard as
            vehicle animator (chord ≤2 pts, endpoint alignment ≤500mm)
    result: failed — gw_platform_out ribbon traces BEAM_WIDTH × BEAM_DEPTH cross-section
            edges (0.5m and 0.25m right angles); no ribbon drawn over the actual track
    revealed: edge_lookup reads ALL edges of the FollowMe beam solid, including the
              cross-section square at each path point. For short straight tracks like
              gw_platform_out, the cross-section perimeter (4 × 0.5m = 2m) is longer
              than or comparable to the track length, so the edge chain algorithm picks
              up cross-section edges. The endpoint alignment check (~500mm tolerance)
              does not reject them because the track is short and the cross-section
              endpoints happen to be within tolerance of the chord endpoints.

  - try:    restrict edge_lookup upgrade to seg_* keys only — station interior gw_*
            tracks must never be touched by edge_lookup; path.json is their authority
    result: succeeded — cross-section zigzag eliminated from gw_* tracks;
            inter-station seg_* tracks still upgraded from 2-pt chords to curve pts

principle: edge_lookup reads structural beam geometry, not vehicle path geometry.
           For inter-station seg_* tracks, the longest edge chain in the beam solid
           IS the path (the Bezier). For station interior gw_* tracks, the beam
           solid geometry is NOT reliable as a path source — cross-section edges
           dominate on short tracks, and the FollowMe solid interior has many
           spurious edges. gw_* tracks have exactly one authoritative source:
           path.json (extracted from ArcCurve or generated from lines.json).
           edge_lookup must never override path.json for gw_* tracks.
domain:    SU

## Related arc: gw_uturn inside rail (same session)

problem:   followus ribbon traces inside rail arc (1500mm = 3m diameter) not centerline (1750mm = 3.5m)
arc:
  - try:    read without correction — ArcCurve pts from extracted.json are used directly
    result: failed — ArcCurve in SketchUp IS the inside rail by design (MIN_STATION_ARC_RADIUS_MM = 1500mm enforces inside rail, not centerline)
    revealed: vehicle path requires centerline = inside + BEAM_WIDTH/2 = 1500 + 250 = 1750mm

  - try:    in path.json Code Path 1 (extracted.json source): after settling pts_mm,
            detect avg radius ≈ 1500mm and scale pts outward from arc center by factor
            (1750/1500); same correction in _generate_uturn_arc_pts_mm Code Path 2
    result: succeeded — correction logged at build time; gw_uturn ribbon moves
            outward 250mm to vehicle centerline

principle: ArcCurves in SketchUp station templates mark the INSIDE RAIL structural
           edge. The vehicle travels 250mm (BEAM_WIDTH/2) farther out on the centerline.
           All arc pt extraction must apply this offset before storing pts in path.json.
           The correction is: detect radius ≈ MIN_ARC_RADIUS_MM (±150mm), scale from
           arc center to MIN_ARC_RADIUS_MM + 250mm.
domain:    SU

## What followus now reveals (still outstanding)

1. gw_platform_out: no ribbon drawn — path.json may have no entry, or pts are
   at the wrong location. Investigate what path.json stores for this track.
2. Inter-station curved segs: need to confirm edge_lookup upgrade produces correct
   curve pts after the seg_*-only restriction.
