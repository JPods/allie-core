# TFTS — 2026-06-11T19:48:41Z

problem: BFS routes pod to s001.gw_cp_out_0 instead of s001.gw_platform_parking for s002→s001 trip

arc:
  - try:      Observed pod in s002→s001 trip stalling at s001.gw_cp_out_0 (unconnected exit stub).
              Trip plan showed 14 segments ending at s001.gw_cp_out_0 — pod was dispatched on
              a path that exits the station instead of landing at the platform.
    result:   Trip dispatched, pod stalled. Log: seg [14] = s001.gw_cp_out_0, no next guideway.
    revealed: bfs_route returns a path ending at the exit stub. The no_more_dest condition
              (`adj[last].none? { |nxt| station_of(nxt) == dest_sid }`) fired at gw_cp_out_0
              because adj["s001.gw_cp_out_0"] is EMPTY in this 2-station ring — there is no
              outgoing inter-station segment from s001.cp0 in the s002→s001 direction.

  - try:      Trace why adj["s001.gw_cp_out_0"] is empty.
    result:   The 2-station ring has TWO inter-station segments:
              - seg_s001_cp1_s002_cp0 (from s001.cp1 → s002.cp0, used by s001→s002 via U-turn)
              - seg_s002_cp0_s001_cp1 (from s002.cp0 → s001.cp1, used by s002→s001)
              The pod arrives at s001.cp1 (the parking station's internal U-turn CP, not cp0).
              JPods_station_parking has NO landing chain for in_cp1 — only in_cp0. The pod
              traverses the intra-station tracks backward via endpoint proximity until it hits
              gw_cp_out_0 (exit at cp0), which has no adj entry (no connection from s001.cp0
              in this ring). no_more_dest fires → BFS stops here.
    revealed: TWO root causes:
              1. BFS should never declare arrival at a gw_cp_out_* or gw_cp_in_* stub.
                 These are connection stubs — pods should stop at gw_platform_parking or
                 gw_platform, not at the stub.
              2. The network topology is mismatched: the guideway connects s002.cp0 to s001.cp1,
                 but JPods_station_parking only accepts arrivals at cp0.

  - try:      Fix BFS: add is_cp_stub guard to no_more_dest condition.
              `is_cp_stub = last.match?(/\.gw_cp_(?:in|out)/)`
              `no_more_dest = !is_cp_stub && adj[last].none? { |nxt| station_of(nxt) == dest_sid }`
    result:   BFS no longer terminates at gw_cp_out_0. With adj["s001.gw_cp_out_0"] empty,
              no successors are added to queue at that node; BFS exhausts; returns nil.
              plan_route falls back to _build_route_json. If that also fails, pod re-enters
              dwell with diagnostic message: "arrival at parking station cp1 which has no
              landing chain — re-orient station so cp0 faces the incoming guideway."
    revealed: This is the correct code fix. The underlying topology problem (arriving at cp1
              of a parking station) is a network design issue that Bill must fix in SketchUp:
              rotate or reposition s001 so that cp0 (not cp1) faces the guideway from s002.

principle: |
  BFS must never declare "arrival" at a gw_cp_in_* or gw_cp_out_* connection stub at the
  destination. These stubs are physical connection points — arrival is only valid at
  gw_platform_parking, gw_platform, or a platform approach segment. The no_more_dest guard
  correctly handles segments with no further dest_sid successors, but must be gated on
  is_cp_stub first. Without this gate, BFS stops at any dead-end in the station graph —
  including exit stubs — and dispatches pods on paths that strand them at unconnected CPs.

  Secondary: JPods_station_parking only receives at cp0. Its cp1 is an internal U-turn
  midpoint, not an external arrival CP. Network connections must enter this station at cp0.
  When the guideway connects to cp1 instead, no landing chain exists and BFS correctly
  returns nil (after the is_cp_stub fix). The diagnostic message guides the fix.

domain: SU
