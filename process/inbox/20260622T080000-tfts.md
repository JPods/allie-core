# TFTS — 2026-06-22T08:00:00Z

problem:   Full v2 rewrite needed — old code in codearchive, station tests broken, build pipeline fragmented, console wired to dead modules, no domain authority

arc:
  - try:      Patch codearchive one file at a time with shims
    result:   partial — kept hitting new missing constants in chain
    revealed: Codearchive files are tightly coupled. Can't patch selectively.

  - try:      Rewrite build.rb from scratch without studying old code
    result:   failed — wrong Z, no bezier, no solar, no columns
    revealed: Don't rewrite code you don't understand. Study first.

  - try:      Systematic migration: manifest, read each file, migrate single-purpose functions
    result:   succeeded — 13/13 files migrated or removed in one session
    revealed: The code separates cleanly when you read it first. Most was dead code.

  - try:      Station tests (shuffle/departure/arrival) with Sally authority
    result:   succeeded after 6 bug fixes — Sally owns ps[]/pods[], trips include gw_platform
    revealed: Sally clips gw_platform to ps.N. Departure serial from ps_max. No copies of state.

  - try:      3+circle network animation
    result:   succeeded — routing through traffic circle, bezier build, columns, solar
    revealed: Circle architecture (one ring, entries/exits) + routing graph needs full circle traversal entries

  - try:      Domain authority audit
    result:   found 2 violations — Animation deciding departure, compat bypassing Sally
    revealed: Sally authorizes FIRST, then Natalie routes, then Nora executes. No exceptions.

  - try:      Console Network Display
    result:   succeeded — connections list visible with expandable help
    revealed: iframe timing matters — don't selectTask after showing NE pane, it covers it

principle: Migrate by understanding. Domain authority is non-negotiable. Sally's arrays are the ONLY truth. The crew works in order: Sally authorizes → Natalie routes → Nora executes → Animation orchestrates timing only.

domain:    [SU]

## Session statistics
- Files migrated: 13 codearchive → 0 in boot.rb
- Lines removed: ~18,000 archived → ~2,500 clean v2
- New v2 files created: 18
- Bugs found and fixed: ~20
- Station tests: 4 types × 4 templates = all passing
- Network: 3+circle animating (5 stations, 4 connections)
- Duration: ~12 hours continuous
