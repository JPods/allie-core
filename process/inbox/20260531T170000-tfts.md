# TFTS — 2026-05-31T17:00:00Z

problem:   Pod entering S004.CP1 jumped from gw_cp_in_1 to gw_cp_in_lead_0 instead
           of gw_cp_in_lead_1. station_thru_dip pass-through not working.

arc:
  - try:    read animation log; check lines.json eps and successors
    result: lines.json ep 1 already declares gw_far_main → gw_cp_out_lead_0 correctly.
            ep was initially suspected missing (confusion with a higher id); it is
            present as ep id 1. successors gw_cp_in_1 → gw_cp_in_lead_1 also correct.
    revealed: lines.json was not the source of the bug; animation uses map.json

  - try:    check map.json for S003 (station_thru_dip instance)
    result: S003.gw_far_main: out=[] — dead end in map.json; no successor wired
    revealed: map.json is built by noelle.rb from noelle_features.json routing chains;
              noelle_features.json still used old gw_stub_pair_* segment names that
              don't exist in the template; noelle silently skipped them; gw_far_main
              got no successor in map.json; proximity fallback misfired

  - try:    update noelle_features.json station_thru_dip with correct segment names
            from SAVE_Stub_Pair_Station_Dip_Thru.json eps; also correct landing.in_cp1
            which was routing through platform tracks (wrong for a pass-through pod)
    result: pending — requires Build to regenerate map.json
    revealed: noelle_features.json and lines.json eps must use IDENTICAL segment names.
              When eps were renamed (stub_pair → cp_in/cp_out), noelle_features.json
              was not updated simultaneously. This is the same lesson as the TFTS
              from 20260531T120000: changing segment names requires simultaneous
              update of both files. Never change one without the other.

principle: noelle_features.json and lines.json eps[] are a MATCHED PAIR.
           Every segment name in noelle_features.json landing/originating chains
           must exist as a track in lines.json. When eps are renamed, BOTH files
           must be updated in the same commit.

           Noelle SHOULD validate this at Build time — for each segment name in
           a landing/originating chain, check that it exists in the template's
           lines.json. If not, issue a fault:
             "TEMPLATE: station_thru_dip landing.in_cp1 references 'gw_stub_pair_1_in'
              which is not declared in lines.json. Rebuild noelle_features.json entry."

           Specifically for through-stations: Noelle should verify that gw_far_main
           has a declared successor in lines.json (gw_cp_out_lead_0 or gw_cp_out_0).
           A gw_far_main with out=[] in a through-station is physically impossible —
           the far main IS the pass-through lane; it must connect to a CP exit.
           Fault: "TEMPLATE: gw_far_main has no successor — pass-through lane
                  is disconnected. Add ep: far_main → cp_out_lead_0."

domain:    SU
