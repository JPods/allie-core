# TFTS — 2026-06-19T14:47:49Z

problem:   172mm vector_in vertex included in gw_cp_in_N pts_mm, producing a 3-pt kinked animation path
fault_ref: 20260618T185919

arc:
  - try:     d4b27a6 — min_edge_mm: 300.0 added to _walk_edge_vertices fallback in _track_endpoints_mm
    result:  failed — endpoint CLUSTERING correct but pts_mm PATH still kinked
    revealed: _track_endpoints_mm fallback only fixes the centroid computation; the 3-pt
              kinked path was coming from Priority 0.5 (jpods_path attr), not from the
              bbox fallback at all.

  - try:     clear authored=false on gw_cp_in_N / gw_cp_in_lead_N in lines.computed.json
    result:  failed — Compute re-extracted the same 3-pt path on the next run
    revealed: clearing authored=false forces re-extraction, but re-extraction reads the
              jpods_path attr (Priority 0.5) which still had the contaminated 3-pt path
              written by a prior run. Priority 0.5 perpetuated the bad pts.

  - try:     exclude gw_cp_in_N from Priority 0.5 (jpods_path attr read)
    result:  succeeded — forces fall-through to Priority 2 (_track_endpoints_mm with ene_railroad
             controls), which returns the correct 2-pt arm [outer_tip, inner_arm_tip].
             Priority 0.1 post-pass then snaps outer endpoint to cp_marker arm tip.
    code:    jpod_path_json.rb line 2540 — added || tag =~ /\Agw_cp_in_\d+\z/ to exclusion

principle: gw_cp_in_N tracks must NEVER use the jpods_path attr (Priority 0.5) — that attr
           was written by a prior bbox extraction that included the 172mm vector_in vertex
           as an intermediate path point. The ene_railroad controls in _track_endpoints_mm
           always produce the correct 2-pt straight arm. Excluding gw_cp_in_N from P0.5
           ensures ene_railroad is always authoritative for these tracks.

           The perpetuation mechanism: bad 3-pt path written via _write_path_attr after
           first bad extraction → next Compute reads it at P0.5 (size >= 9 = 3pts × 3coords)
           → marks authored=true → writes it back → stable wrong path forever.

           The 172mm vector_in edge is direction-only — it must not appear in pts_mm.
           Its only role is to tell _find_direction_vector() which way the pod travels.

domain:    SU
