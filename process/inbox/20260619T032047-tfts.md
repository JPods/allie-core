# TFTS — 2026-06-19T03:20:47Z

problem:   traffic_circle ring center 1250mm off in X — circle centered on CP0/CP2 axis but not CP1/CP3 axis
fault_ref: 20260618T185919

arc:
  - try:      Priority B centroid-arc synthesis using _arc_pts_from_center_mm with radius
              fitted from endpoint-to-endpoint distance
    result:   failed — TC-1 (wrong radius 7766mm instead of 7516mm), TC-2/TC-3/TC-4 downstream
    revealed: radius must come from nora.arc_radii (designer authority), not geometry

  - try:      _synthesize_traffic_circle_tracks — new synthesis reads nora.arc_radii (7500mm),
              computes ring center as centroid of all 4 noelle.cps gate positions
    result:   synthesis itself correct — ring center = [-15882, 0] in noelle.cps frame
    revealed: BUT post-synthesis rigid body frame transform (noelle.rb ~3756) then applied

  - try:      trace rigid body transform math:
              arc_hub_N = midpoint(gw_cp_in_N.first, gw_cp_out_N.last)
                        = gate_N + stub_half * tangent_N  (stub_half = 1250mm)
              wld_hub_N = noelle.cps[N].center
              For CP0 (tangent=(1,0)): arc_hub_0=(1250,0), wld_hub_0=(0,0) → tx=-1250, rot=0°
    result:   transform applies -1250mm X shift, moves ring to [-17132, 0] (wrong)
    revealed: transform is spurious for traffic circles — synthesis already in correct frame;
              transform was designed for archive-to-live alignment, not applicable here;
              the 2-CP correspondence breaks for traffic circles because
              |arc_hub_0 → arc_hub_1| ≠ |wld_hub_0 → wld_hub_1| (no rigid body exists
              that can correctly align all 4 gates simultaneously via stub-end midpoints)

  - try:      guard the transform in noelle.rb write_lines_computed_from_geometry:
                if !tmpl_key.match?(/\Atraffic_circle/) && cps_for_xf.size >= 2
              (was: if cps_for_xf.size >= 2)
    result:   succeeded — transform skipped; ring center will land at [-15882, 0] after Compute

principle: When a synthesis function already generates pts_mm in the target coordinate frame
           (noelle.cps frame), the post-synthesis rigid body alignment transform must be
           bypassed. The transform assumes arc_hub = wld_hub + small_error; for traffic circles
           arc_hub = wld_hub + stub_half*tangent (a structural, not accidental, offset),
           which causes a systematic 1250mm shift that a 2-point rigid body cannot correct
           without also distorting the CP1/CP3 alignment. Guard by formation type.

domain:    SU
file:      su_jpods/noelle.rb:3756 (guard condition on rigid body frame transform)
           su_jpods/jpod_path_json.rb:2431 (_synthesize_traffic_circle_tracks bypass)
