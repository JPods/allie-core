# TFTS — 2026-06-28T04:32:54Z

problem:   Waypoint Z not applied at markers — guideways flat at CP interpolation, ignoring waypoint beam_z elevation
fault_ref: 20260623T140000-tfts.md (prior Z-defect arc)

arc:
  - try:      Edited jpod_network.rb in wrong directory (/Documents/skp_jpods/JPods/) —
              live plugin is at /Library/Application Support/SketchUp 2026/SketchUp/Plugins/su_jpods/
    result:   No effect on builds — changes invisible to SketchUp
    revealed: SketchUp 2026 loads from Plugins/su_jpods/, not the Documents working copies.
              The Documents/skp_jpods/JPods/ and su_mostly/ directories are stale copies.

  - try:      Restored hard_floor_z to min_z_bounds (from bc8dcdf fix)
    result:   Partial — Z changed but raw SU terrain mesh bumps forced beam up at noise points
    revealed: Smooth guideways are primary. hard_floor_z (raw mesh) is noise, not signal.
              The 1df1a60 commit correctly removed it.

  - try:      Changed MIN_Z_CHANGE_DIAMETER from 60m to 3m (the bc8dcdf value)
    result:   Defect at WP9 very pronounced — smoother radius too small, sharp transitions
    revealed: 60m was the working value in 1df1a60, not 3m. The bc8dcdf value predated
              the smooth guideways design.

  - try:      Added re-clamp after Gaussian smooth (from 1df1a60 original)
    result:   Partial — right direction but 0.42m low
    revealed: Waypoint beam_z is beam-bottom (terrain + CLEARANCE_HEIGHT). CP anchor_zs
              add BEAM_DEPTH. Waypoint Z needs + BEAM_DEPTH to match the beam-top frame.

  - try:      Hard-pin waypoint Z after smoothing (from bc8dcdf)
    result:   Sharp spike at waypoint — wrong direction, violates minimum Z-change radius
    revealed: Single-point pin creates discontinuity. The smoother exists to prevent exactly this.
              Pin was removed in 1df1a60 for good reason.

  - try:      Parabolic arc (w = 1 - d²/r²) blending neighbors toward waypoint Z
    result:   Arc applied upside-down — beam rotated around waypoint instead of sitting at it
    revealed: Parabolic constant-curvature arc bows away from waypoint, creating a
              peak/valley instead of a plateau.

  - try:      Cubic Hermite blend (smoothstep h01) — beam flat at waypoint, S-curves to natural
    result:   Still upside-down — blend shape wrong direction
    revealed: All post-smoother modifications fight the Gaussian. The working version
              (1df1a60/2331a6c) solved this WITHOUT post-smoother waypoint modification.

  - try:      Restore exact build_path.rb from 2331a6c (yesterday's working push)
    result:   Correct — matches working state
    revealed: The regression was introduced by today's session, not by any commit.
              The working code was: Gaussian smooth WITHOUT re-clamp, NO waypoint pin,
              NO post-smoother arc. Waypoint Z flows through desired_z (piecewise linear),
              Gaussian smooths transitions, and that IS the final answer.

principle: The Z pipeline that works is: piecewise-linear desired_z through waypoint anchors →
           grade clamp → Gaussian smooth (no re-clamp) → endpoint blend. Nothing touches beam_z
           after the smoother. Every attempt to "fix" the waypoint Z after smoothing (pin, arc,
           blend) creates worse defects than the smoother alone. The smoother IS the waypoint
           transition. Do not add post-smoother waypoint corrections.

secondary: When editing SketchUp plugin code, verify the load path first. SketchUp 2026 loads
           from ~/Library/Application Support/SketchUp 2026/SketchUp/Plugins/su_jpods/, NOT
           from ~/Documents/skp_jpods/JPods/ or su_mostly/. Edits to the wrong directory are
           invisible and waste entire debugging arcs.

tertiary:  Waypoint beam_z is beam-BOTTOM (terrain + CLEARANCE_HEIGHT). CP anchor_zs are
           beam-TOP (center.z + BEAM_DEPTH). Waypoint Z must add BEAM_DEPTH to match the
           anchor frame. The 0.42m gap was this frame mismatch.
domain:    [SU]
