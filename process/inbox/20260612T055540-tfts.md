# TFTS — 2026-06-12T05:55:40Z

problem:   8 Z_CONTINUITY faults in SC_Greenville_Bolden — all Δ=105mm,
           gw_cp_in outer Z was 105mm ABOVE the arrival seg endpoint Z

arc:
  - try:      Add constant 105mm offset to to_z in compute_anchor_zs
    result:   rejected — 105mm is terrain-specific (SC_Greenville_Bolden lateral
              slope = 3%); different networks would show different values
    revealed: cp_center Z is the midpoint between the two rails, not on either rail;
              gw_cp_in and gw_cp_out are at the same LOCAL Z in geometry.json but
              entity.transformation with lateral slope separates them in world space

  - try:      Trace the math: derive 105mm from entity.transformation coefficients
    result:   solved analytically: Rzy = 105 / 3500 = 0.03 (3% lateral slope);
              same for all 4 stations in SC_Greenville_Bolden confirming consistent
              terrain slope at all station locations in that model
    revealed: gw_cp_out outer = cp_center_world.z + BEAM_DEPTH (departure, ✓)
              gw_cp_in outer  = cp_center_world.z + BEAM_DEPTH + Rzy × 3500mm (arrival, ✗)
              The formula cp_center + BEAM_DEPTH only correctly anchors the DEPARTURE end

  - try:      Extend stub_tips in build_from_config (and build_from_json) to also
              capture gw_cp_in outer Z (pts.first, the outer/gate end of gw_cp_in)
              from map.json; pass to_sid + to_stub + stub_tips into Plan so
              compute_anchor_zs can use the actual gw_cp_in outer Z for to_z
    result:   succeeded — two-pass: first build generates map.json with correct
              gw_cp_in Z; second build reads it and produces zero-delta junctions
    revealed: stub_tips already existed for gw_cp_out; the pattern extends cleanly
              to gw_cp_in with pts.first (vs pts.last for gw_cp_out); no structural
              changes to Plan, PathResolver, or BeamExtruder required

principle: The departure and arrival rails of a dual-track station are on opposite
           sides (DUAL_TRACK_SPACING = 3500mm apart in Y). Any terrain lateral slope
           at the station location raises one rail above the other in world space.
           compute_anchor_zs cannot recover this from cp_center alone — cp_center is
           the midpoint between the rails and its Z is the average, not either rail.
           Use the actual gw_cp_in outer Z from map.json for the arrival anchor;
           fall back to cp_center + BEAM_DEPTH only on first build (before map.json
           exists). On second build, all 8 Z_CONTINUITY faults clear.

domain: SU
