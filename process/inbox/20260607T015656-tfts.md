# TFTS — 2026-06-07T01:56:56Z

problem:   gw_uturn and gw_cp_in/out missing or offset in Show Route ribbon — recurring defect
fault_ref: (no single fault file — recurring across multiple sessions)

arc:
  - try:      Store extracted.json pts by scanning model.entities with identity parent_xf
              (original approach, pre station_inst fix).
    result:   Worked for export() in network context, but Proof Lines in template context
              double-applied station_xf → SEVERE errors for all nested-formation templates
              (station_thru_dip, station_line_end). template-world coords baked into pts;
              Proof Lines applied station_xf again on top → 9200mm offset in Proof Lines.
    revealed: Two consumers read extracted.json pts in different coordinate contexts:
              Proof Lines runs in the TEMPLATE model (inst = formation component,
              inst.transformation = station_xf); export() runs in the NETWORK model
              (inst = JPods Structure, inst.transformation = template-world → network-world).
              A single stored coordinate cannot satisfy both without a declared frame.

  - try:      Station_inst fix — scan station_inst.definition.entities (formation-local)
              with identity parent_xf. Store pts in formation-component-local space.
              Proof Lines in template context: world_xf = station_xf → transforms
              formation-local → template-world ✓. SEVERE count dropped to 0.
    result:   Proof Lines fixed. But export() broke: world_xf = inst.transformation
              (template-world → network-world) applied to formation-local pts — missing
              the inner station_xf offset (≈9200mm Y for station_thru_dip) → ribbon
              appeared offset by exactly the formation component's position in the template.
              Additionally: gw_cp_in/out and gw_uturn live OUTSIDE the formation component
              directly in model.entities — scanning only formation-local missed them entirely
              → they vanished from extracted.json → missing from Show Route ribbon.
    revealed: Two bugs compounded: (1) coordinate frame mismatch in export(), (2) gw_cp/uturn
              topology outside formation component definition.

  - try:      Fix export() by searching inst.definition.entities at network export time
              to find the formation sub-component and reconstruct its transformation.
              _find_formation_sub_xf helper added; compose world_xf * inner_xf.
    result:   Returned nil — the entity-tree search couldn't match formation_id reliably
              across the template-loaded-as-definition boundary. Offset persisted.
    revealed: In-network entity-tree search for template internals is fragile. The
              definition name or attribute may not round-trip cleanly through
              model.definitions.load(skp). Better to capture at extract time.

  - try:      Store formation_xf (station_xf.to_a as 16-float array) in extracted.json
              at extract time. export() reads formation_xf and composes:
              eff_world_xf = world_xf * Geom::Transformation.new(formation_xf).
              Added to save_extracted as optional param; load_extracted_formation_xf reader added.
              Schema bumped to v2 with version, dt, built_by, su_version, revision_log
              (blockchain-stub: chained SHA256 entries).
    result:   Offset fixed. Station_thru_dip and station_line_end ribbons land correctly.
              BUT: gw_cp_in/out and gw_uturn still missing — the station_inst scan that
              produced the formation-local pts did not include entities outside the
              formation component. extracted.json had no entry for them.
    revealed: The station_inst scan (formation-local only) is the root of the missing-entity
              bug. Need to scan ALL of model.entities, not just the formation definition.

  - try:      Replace station_inst branch with a single scan:
                _index_gw_entities(model.entities, station_xf_inv, gw_index)
              Pass station_xf_inv as parent_xf. This normalizes all gw_* entities
              to formation-local equivalent regardless of where they sit in the hierarchy:
                Inside formation:  station_xf_inv * station_xf * local = local ✓
                Outside formation: station_xf_inv * world_pos (formation-relative) ✓
              export() then applies eff_world_xf = world_xf * station_xf for both cases:
                eff_world_xf * station_xf_inv * world_pos = world_xf * world_pos ✓
    result:   SUCCEEDED. All gw_* entities found (uturn and cp back). Offsets gone.
              Flat templates: station_xf = identity → station_xf_inv = identity →
              identical to original model.entities scan (no regression).

principle: extracted.json pts must be stored in formation-component-local space so that
           Proof Lines (template context) can apply inst.transformation = station_xf and
           land in template-world. export() (network context) must apply
           eff_world_xf = world_xf * stored_formation_xf to recover network-world.
           The formation_xf (station_xf at extract time) must be persisted in extracted.json —
           not re-derived at export time from the entity tree.
           The gw_index scan must always use model.entities with station_xf_inv as parent_xf
           so that ALL gw_* entities are captured regardless of nesting, and all land in
           the same formation-local equivalent space.

secondary: gw_cp_in/out and gw_uturn are placed OUTSIDE the formation component definition
           in nested-formation templates. Any scan that only looks inside the formation
           component definition (station_inst.definition.entities) will miss them.
           Always scan model.entities.

domain:    SU
