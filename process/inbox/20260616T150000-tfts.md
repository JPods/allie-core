# TFTS — 2026-06-16T15:00:00Z

problem:   All platform advances end at wrong position; pods collapse to same point;
           confirm_slots snaps pods to wrong slot; personal space violated.

fault_ref: (continuation of 20260616T064720-tfts.md arc — sequencer=0 still present)

arc:
  - try:      Assumed network.json template fallback in init_sequencers_from_model
              would load sequencer after entity scan found 0 with formation_id.
    result:   failed — count=0 persisted despite fallback being loaded
    revealed: The entity scan loop (model.entities.each) found entities with
              structure_id only IF those entities ALSO had formation_id set.
              template_from_json was populated but the entity fallback path required
              the entity to first have structure_id — and no entity in 2_thru_dip
              has structure_id set on the component instance.
              Result: template_from_json was loaded but never applied.

  - try:      Add direct template_from_json iteration AFTER entity scan.
              If count==0 after entity scan, iterate template_from_json directly
              and call init_sequencer_for_station for each sid.
    result:   succeeded — sequencer now loads for s006 and s007
    revealed: formation_id is NEVER written to SketchUp component instances for
              station_thru_dip. The entity attr is a historical artifact.
              network.json stations[sid]["template"] is always current after Build.
              The correct fallback is to use network.json directly, not as a
              secondary check within entity scan.

  - try:      advance_pod_slot was passing pod_pos: from_pt to _platform_pts_entry_first.
              When from_pt is at a middle slot (e.g. ps3), this caused incorrect
              orientation when slot_track = gw_platform_parking.
    result:   orientation inverted — all advances ended at gw_platform_parking exit end
    revealed: _platform_pts_entry_first uses gw_platform_parking as proximity anchor
              for orienting gw_platform. But when slot_track IS gw_platform_parking,
              pp_ent[:pts] == gw_entry[:pts] (self-referential) — d_first==d_last==0
              → no reversal → falls through to ps1 anchor. But ps1 position was also
              on gw_platform_parking (wrong track due to sequencer=0) and was computed
              with an inverted pts orientation (formula placed ps1 near the exit end).
              The chain: sequencer=0 → ptrack=gw_platform_parking → ps1 near exit →
              orientation check reversed → advance clips from pod toward exit end.

  - try:      Fix 1: direct template_from_json fallback (above).
              Fix 2: _platform_pts_entry_first detects self-referential pp_ent and
                     skips the proximity check (falls to ps1 anchor).
              Fix 3: advance_pod_slot passes entry_anchor = Sally.slot_position(sid,1)
                     to _platform_pts_entry_first instead of from_pt (middle slot).
    result:   succeeded — once sequencer loads (ptrack=gw_platform), slot positions
              are on gw_platform with correct orientation; advance clips correctly.

principle: |
  network.json stations[sid]["template"] is ALWAYS current after Build and is the
  authoritative source for sequencer initialization. entity attributes (formation_id)
  are not reliably written to SketchUp component instances for all templates.
  init_sequencers_from_model must use network.json directly as primary source,
  not as a fallback within the entity scan.

  Rule: after entity scan, if count==0 AND template_from_json is non-empty, iterate
  template_from_json directly and init sequencer for each known sid.

  _platform_pts_entry_first orientation rule: when orienting track T, only use T's
  neighbor (gw_platform_parking) as anchor when T ≠ gw_platform_parking. If the
  anchor IS the same track as T (self-referential), skip to the next anchor (ps1,
  then pod_pos). Self-referential proximity always produces 0==0 (no signal).

  advance_pod_slot orientation rule: always pass ps1 slot_position as the
  entry_anchor for _platform_pts_entry_first. The pod's current position (from_pt)
  is at a middle slot — it is NOT a reliable proxy for the entry end of the track.
  ps1 is always at the entry end by definition, regardless of which slot the pod
  currently occupies.

  Sally loop timing:
  - 4s idle: platform quiet for PLATFORM_QUIET_THRESHOLD ticks
  - 1s active: any slot event (arrival/departure/advance) resets quiet_ticks
  - Surge mode (≈0.25s): ps1 blocked + internal gaps exist
  - When platform is FULL and a pod loops: reset @@platform_quiet_ticks=0 so
    poll_sally_dispatch fires within 1s to dispatch the ps_cap pod.

domain: SU
files:
  - jpod_sally.rb: init_sequencers_from_model — direct template_from_json fallback
                   after entity scan; on_maneuver_complete — platform_full branch
                   resets quiet_ticks + logs dispatch candidate
  - jpod_vehicle_anim.rb: _platform_pts_entry_first — skip self-referential pp_ent;
                           advance_pod_slot — pass ps1 as entry_anchor not from_pt;
                           enhanced diagnostic logging for orientation and clipping.
