# TF — 2026-08-05T23:00:00Z
summary: Tax and commission are structurally identical patterns — build one, replicate the other. Header default → per-line override → audit trail → report by grouping entity. Hide domain-specific columns behind toggle buttons (C for commission). WC2 commission pattern (rate × level_factor × split) survived intact into WC3.
code: webClerk3/apps/transactions/services/totals.py:161 (tax priority chain), commission.py:146 (calculate_line_commission), React2025/src/hooks/useLineCard.ts:270 (hidden commission columns)
