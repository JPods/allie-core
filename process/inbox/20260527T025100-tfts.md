# TFTS — 2026-05-27T02:51:00Z

problem: cpu/lines.json could not be generated correctly by scanning the SketchUp model — gw_uturn topology and arc length were wrong in every attempt.

fault_ref: (no single fault file — progressive scan failures across session)

arc:
  - try: scan cpu/model.skp with _scan_gw_entities_recursive; gw_uturn was entire donut solid tagged gw_uturn
    result: failed — 30669mm winding path; both uturn endpoints clustered near the diverge junction; MODEL_ERROR ep
    revealed: all solid edges of the half-donut carried the gw_uturn tag; chain algorithm wandered through the solid

  - try: place uturn/model.skp component inside cpu, re-scan
    result: failed — 25172mm winding path; endpoints still ~585mm apart near the diverge junction; MODEL_ERROR ep
    revealed: the uturn component brought its own solid geometry; scan went inside the component and found the same tagged solid edges

  - try: create named group gw_uturn inside uturn model containing only the arc; re-scan
    result: failed — 4000mm straight line (chord of outer arc); 2 junctions, 0 EPs
    revealed: group contained a single straight edge (or arc chain returned only 2 points); followme_trace fell through to bounding-box fallback which gives chord distance; 4000mm = outer diameter chord

  - try: store arc path as attribute dictionary on the group (ene_railroad pattern — path as data, not geometry)
    result: not attempted — Bill's insight arrived first: "a line confuses the issue of a gw_ being a guideway"; ene_railroad stores path as control points, never derives path from edge topology
    revealed: the scan tool is appropriate for complex/unknown geometry; for fixed mathematical structures the scan is the wrong tool

  - try: write lines.json from math: arc length = π × 1750 = 5497.8mm; endpoints from known cpu coordinate geometry; diverge and merge EPs from Bill's corrected topology spec
    result: succeeded — cpu/lines.json and cps/lines.json written as mathematical ground truth; no scanning needed; uturn arc length corrected in all four track_formation templates (was 4872.9mm = scanned inner arc at ~1551mm radius)

principle: For fixed-geometry station structures, lines.json is a mathematical declaration, not a scan output. Scan the model to discover unknown geometry; use math to define known geometry. Once written from math, lines.json is the authority — the scan is for verification only. The ene_railroad principle applies: path is data, not geometry.

domain: SU
