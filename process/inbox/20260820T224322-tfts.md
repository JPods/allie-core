# TFTS — 2026-08-20T20:00:00Z
problem:   78-model field layout task was overwhelming as a manual text-editor task
fault_ref: none (design process problem, not system fault)
arc:
  - try:      Bill manually arranged field layouts in a text editor for each model
    result:   failed — tedious, slow, no learning captured, no pattern propagation
    revealed: The task is not hard per model — it's hard because 78 models at once is paralyzing. The individual decisions are simple.
  - try:      Claude generates all 78 from database, Bill reviews in batches of 5, Claude learns from corrections and applies to next batch. Alice logs each correction as a pattern.
    result:   succeeded — 28 models done in one session with clear pattern evolution (3-tier → 4-tier, JSON split into important vs standard, stats placement)
principle: When users face an overwhelming configuration task, Alice should break it into chewable pieces — generate a first pass, present small batches for human review, learn from corrections, propagate patterns. The human's job is editorial judgment, not data entry. This is radically better than expecting humans to build from scratch.
domain:    WC3
