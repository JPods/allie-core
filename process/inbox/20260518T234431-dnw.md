# DNW — boot.rb load list vs main.rb load list divergence (recurring)

tried:    assume boot.rb and main.rb stay in sync
result:   third file found missing from boot.rb (jpod_vehicle_anim, jpod_layer_manager before it,
          presumably others in future). boot.rb sets $jpods_main_loaded = true before calling
          main.rb — so main.rb's entire load block is skipped on every plugin load.
revealed: the two lists WILL drift. any file added to main.rb but not boot.rb silently fails.
          the symptom is always "uninitialized constant JPods::SomeClass" at runtime.

fix:      added jpod_vehicle_anim to boot.rb after jpod_animator.
need:     a permanent guard — boot.rb should be the ONLY load list. main.rb's list should
          be deleted or replaced with a comment pointing to boot.rb.
