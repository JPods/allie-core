# TFTS — 2026-06-15T04:00:00Z

problem:   gw_c_* arcs revert to 2-pt straight-line chords on every animation start
fault_ref: 20260614T230000-tfts.md (original bezier mandate violation arc)

arc:
  - try:      Edge lookup (.upcase→.downcase fix) to upgrade 2-pt chords to 14-pt arcs.
    result:   Worked transiently. Regressed on next animation run.
    revealed: PathJSON.export(model) is called at EVERY animation start (jpod_vehicle_anim.rb
              line ~1952). It rewrites path.json with only inter-station seg_* entries.
              Any previously stored intra-station gw_c_* arc data is wiped. PathJSON.load
              then returns no gw_c_* entries; map.json fallback gives 2-pt chords. The
              edge lookup (build_edge_lookup_from_model) also yielded 0 intra-station entries
              — _collect_intra_station_edge_paths never found gw_c_ tracks reliably.
              Coord log diagnostic: source="" (not "edge_attr") for all gw_c_ entries.

  - try:      Math-based arc synthesis: fit circle to gw_c_* chord endpoints per station,
              synthesize dense pts at ≤1m spacing. No edge walk, no path.json dependency.
    result:   synthesize_circle_arcs! fits circle from all chord endpoints, generates pts
              from _synthesize_arc_pts. Source field = :arc_math. Stable across animation
              restarts — chord endpoints from map.json are stable. ✓
    revealed: The traffic circle geometry is fully determined by the chord endpoints in
              map.json. No need to read it from ene_railroad attributes or path.json.

principle: PathJSON.export runs at every animation start and only writes inter-station
           seg_* entries. Any intra-station arc geometry must come from a source that
           survives this rewrite: map.json chord endpoints (stable) + math.
           Rule: gw_c_* arcs are circular — fit the circle from chord endpoints,
           synthesize pts. Never depend on path.json or edge attributes for arc shape.
           The coord log (anim-coords.jsonl) source field confirms: "arc_math" = math
           synthesis won; "" = map.json chord (violation).

domain:    SU
files:     jpod_vehicle_anim.rb: synthesize_circle_arcs!, _fit_circle_xy, _synthesize_arc_pts
           (replaces edge_lookup merge block); edge lookup code removed
commit:    467bd10
