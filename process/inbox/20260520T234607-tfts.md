# TFTS — 2026-05-20T23:46:07Z

problem:   Waypoints (via_markers) between S048 and S050 were being dropped. Build produced guideways without the 3 waypoints; columns appeared at waypoint locations separately.
fault_ref: (no FAULT file)

arc:
  - try:      Assumed the issue was via_markers not preserved in feature.json — investigated
              generate_feature_json's existing_via lookup. Found feature.json had [1,2,3]
              for cp_S048_cp1_S050_cp0 and [] for cp_S050_cp0_S048_cp1.
    result:   Fixing [] by mirroring [3,2,1] from reverse direction into feature.json.
              Topology still showed [1,2] — guideways still wrong geometry.
    revealed: The feature.json values were correct going INTO the build. Something during
              the build was overwriting them.

  - try:      Traced the build pipeline. Found generate_feature_json was creating SEPARATE
              cp_ entries for BOTH directions of a bidirectional connection:
              cp_S048_cp1_S050_cp0 (via_markers=[1,2,3]) AND cp_S050_cp0_S048_cp1 (via_markers=[]).
              build_from_config iterates both, calling build_segment TWICE for the same
              physical guideway pair. The second call (cp_S050_cp0_S048_cp1 with []) erases
              and rebuilds the guideways just laid by the first call, stamping via_markers=[].
    result:   Root cause confirmed. The second build_segment call overwrites the first.
    revealed: build_segment for a cp_ entry builds BOTH directional guideways (track_idx=0
              and track_idx=1). A second cp_ entry for the reverse direction covers the same
              pair and always wins last.

  - try:      Fixed generate_feature_json: when processing a seg_ connection, check if the
              reverse cp_ key already exists in conns_out. If yes, nest this seg_ entry into
              that existing entry (don't create a new cp_). Result: ONE cp_ entry per physical
              pair containing BOTH seg_ sub-entries. build_from_config calls build_segment ONCE.
    result:   Build correct. Trip S048→S050 grew from 465m to 556m (waypoints being routed).
              feature.json now has 3 cp_ entries (not 6), with via_markers=[1,2,3] on the
              S048-S050 pair. Next build will stamp [1,2,3] on both guideways.

principle: generate_feature_json must produce ONE cp_ entry per physical guideway pair. Both
           directional seg_ entries nest inside that single cp_. Duplicate cp_ entries cause
           build_from_config to call build_segment twice: the second call always erases and
           overwrites the first, dropping any via_markers the first call set.

warning:   Future design may allow single-guideway (one-way) connections — e.g., a one-way
           loop or a maintenance spur. If single-guideway build is added, the "one cp_ per
           physical pair" rule must be revisited. A one-way connection has only one seg_ entry
           under its cp_ and build_segment produces only one track. Document this case before
           implementing — the merge logic in generate_feature_json assumes bidirectional pairs.
domain:    SU
