# TFTS — 2026-06-07T07:00:00Z

problem:   traffic_circle ring arcs broken after re-extract (gw_c_1_1, gw_c_2_2 traversed backward)
fault_ref: (no fault file — observed as ring breaking mid-loop; pod misses gw_c_1_1 successor)

arc:
  - try:      Investigated why traffic_circle, uturns, gw_cp_in/out broken after commit 989736c.
              Only code change since: 7d7bfdb (gw_platform_in direction correction).
              That change does not touch ring or cp logic.
    result:   Code diff ruled out as cause. Data change identified: re-extract of traffic_circle7
              changed gw_c_0_0 and gw_c_1_1 from 2 pts to 13 pts.
    revealed: With 2-pt arcs, forward=reverse for straight chord — direction irrelevant.
              With 13-pt curved arcs, direction matters. Step 3 (topology snap) finds
              the minimum-distance endpoint pair across all 4 combinations (A.first/A.last
              × B.first/B.last), snaps B's connecting endpoint to A's — but does NOT
              reverse B's pts array. Animator looks up successor by first-pt match; if
              B's first pt ≠ prior track's last pt, animator silently skips B → ring breaks.
              Specific: gw_c_1_1 stored first=(-20843,5646), last=(-10920,5646).
              Ring traversal needs entry at (-10920,5646) → B stored backward.
              Same for gw_c_1_2, gw_c_2_2, gw_c_2_3.

  - try:      Add Step 4 direction correction after Step 3 (topology snap) in export().
              For each consecutive pair A→B in ring_circle: compare A's last pt distance to
              B's first and B's last. If d_last < d_first, B is stored backward — reverse pts_b.
              Sequential loop so each corrected predecessor feeds the next check.
    result:   SUCCEEDED. gw_c_1_1, gw_c_1_2, gw_c_2_2, gw_c_2_3 reversed to correct
              traversal order. Ring chain: each track's FIRST pt now matches prior track's
              LAST pt. Commit 86a910f.

principle: Step 3 (topology snap) identifies which endpoint connects — it does not fix direction.
           Step 4 (direction correction) must run after Step 3 and use A's LAST pt as the
           reference: if B's LAST is closer than B's FIRST, B is backward — reverse it.
           The two-step pattern (snap → direction) is required whenever ring arcs can be
           extracted in either direction. Two-pt arcs mask this; multi-pt arcs expose it.

secondary: Ring direction bug was invisible at commit 989736c because extracted.json still had
           2-pt arcs for gw_c_0_0 and gw_c_1_1. Re-running Extract Template populated 13-pt
           curved arcs. Any time extracted.json geometry is regenerated, the direction correction
           must be re-validated. Step 4 makes this automatic.

domain:    SU
