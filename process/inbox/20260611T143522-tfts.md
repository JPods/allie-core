# TFTS — 2026-06-11T14:35:22Z

problem: JPods_station_parking platform_shuffle test: loops=0 pods dispatch originating chain immediately instead of shuffling forward; test freezes

arc:
  - try:      Applied station_id = fid.downcase fix (prior session) to normalize template_lookup keys
    result:   Fixed gw_platform not in lookup + NORA_0003 missing from @@pods
    revealed: Mixed-case station_id caused all Sally lookups to miss
  - try:      Observed log: originating chain fired BEFORE advance_complete log line for NORA_0002
    result:   Confirmed: NORA_0002 was already in @@dwelling with @dwell_until=nil (from prior dwell cycle), dwelling handler saw phase='' → else branch → originating chain dispatched BEFORE arrival handler printed "advance complete"
    revealed: The dwelling handler has no guard against pods that just arrived from an advance
  - try:      Set station_test_phase='shuffle_parked' in _advance_ps arrival handler
    result:   Correct for pods that already advanced; but phase='' on FIRST landing (before any advance) still triggers originating chain after first dwell
    revealed: _park_ps arrival handler also needs the shuffle_parked guard for initial landing
  - try:      Set station_test_phase='shuffle_parked' in _park_ps arrival handler for station_test pods with empty phase
    result:   Prevents originating chain on first landing. But shuffle_parked dwelling branch removed pod from @@pods (via @@pods.reject!) — Sally could no longer advance it
    revealed: shuffle_parked was a legacy path from removed _shuffle_forward; it was treating the pod as permanently parked
  - try:      Redesign shuffle_parked dwelling branch: slot < cap → remove from @@dwelling only (pod stays in @@pods); slot >= cap → phase='', @dwell_until=nil → next tick fires else→originating chain
    result:   Pods advance correctly and eventually exit. But hold_loop_parked was removing V1 from @@pods after looping, so V1 never exited and remaining count never reached 0
    revealed: hold_loop_parked was a terminal state; test requires ALL pods to exit
  - try:      Transition hold_loop_parked → shuffle_parked lifecycle (instead of removing from @@pods)
    result:   V1 re-enters the shuffle lifecycle, advances to exit end, dispatches originating chain, erases

principle: |
  station_test_phase is the dispatch gate — not just a phase label.
  Rule: any arrival path that should NOT dispatch an originating chain must explicitly set
  station_test_phase to a non-empty value before start_dwell fires. Leaving phase='' means
  "dispatch originating chain on next dwell expiry." This is now enforced at every arrival:
    _park_ps: initial landing → shuffle_parked
    _advance_ps: advance complete → shuffle_parked
    hold_loop_parked → shuffle_parked (transition, not terminal)
  shuffle_parked semantics: "parked, Sally may advance further OR exit if at highest slot."
  The dwelling handler evaluates slot vs capacity every dwell cycle — no external signal needed.

domain: SU
