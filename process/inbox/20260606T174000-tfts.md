# TFTS — 2026-06-06T17:40:00Z
problem:   Proof Lines SEVERE on multi-pt FollowMe solid tracks (115pt→14pt non-deterministic scan)
fault_ref: (no FAULT file — discovered during Proof Lines run)

arc:
  - try:      Store path as Array of Geom::Point3d in 'path' attribute on gw_* entity during Extract Template
    result:   Failed silently — SketchUp cannot serialize Geom::Point3d via set_attribute (rescue nil caught)
    revealed: set_attribute only accepts primitive Ruby types (Integer, Float, String, Bool, Array of same)

  - try:      Store as nested Ruby arrays [[x,y,z],...] in 'path' attribute
    result:   Failed — SketchUp serialized nested array to a String; Priority 1 guard raw.is_a?(Array) failed
    revealed: SketchUp attribute storage does not support nested Ruby arrays reliably

  - try:      Store as flat Float array [x0,y0,z0, x1,y1,z1,...] in 'path' attribute, read back with each_slice(3)
    result:   Partial success — gw_lift_in and gw_platform_out resolved (115/115). gw_platform_in still 115/2
    revealed: Scanner's depth-first traversal finds a different Group object than the indexer for gw_platform_in
              (SketchUp Group entity identity diverges between model.entities traversal and inst.definition.entities traversal)

  - try:      Switch attribute key from 'path' to 'jpods_path' and use get_attribute directly (not dict iteration)
    result:   gw_platform_in still 115/2 — still a different Group entity without the attribute
    revealed: Root cause is SketchUp entity identity: two different Ruby proxy objects for the same logical Group
              in different traversal contexts. Writing to one does not write to the other.

  - try:      Accept scanner limitation; classify FollowMe solid tracks (>20 declared pts, <1/3 found) as SCAN
              (parallel to ARC classification for arc guideways). extracted.json is authoritative; animation unaffected.
    result:   Succeeded — 0 SEVERE across all 4 templates. SCAN classification documents limitation honestly.
    revealed: Scanner cannot reliably verify FollowMe solid tracks (Group-based). extracted.json is the authority.

principle: For FollowMe solid tracks stored as SketchUp Groups, Proof Lines cannot guarantee entity identity
           between the indexer (populate_from_open_template) and the scanner (proof_lines). Do not fight
           SketchUp's entity object model. Accept the limitation, classify these tracks as SCAN, and trust
           extracted.json. The animator reads path.json ← extracted.json — animation is correct regardless.
           Priority 1 (jpods_path attribute) still helps where it works (gw_lift_in, gw_platform_out).

domain:    SU
