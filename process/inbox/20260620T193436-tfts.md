# TFTS — 2026-06-20T19:34:36Z
problem:   Pods not resuming after Stop → Start — trapped as parked ps0, never dispatched
fault_ref: (none — observed during testing)
arc:
  - try:      Resume intercept in build_fleet fired AFTER Sally hold_loop intercept
    result:   failed — hold_loop consumed all pods. Sally.reset (called by start→stop) cleared slot records, so every pod hit "hl_slot unknown" fallthrough which created parked pod at ps0 and did `next`, skipping resume entirely
    revealed: The "falling through to normal dispatch" message was misleading — it fell into the catch-all code at line 3098, not through the hold_loop block. Local variable `hl_sid` was already captured, so clearing the entity attribute didn't change the if-block result
  - try:      Moved resume intercept BEFORE hold_loop in build_fleet
    result:   succeeded — pods with resume_state are now processed first regardless of hold_loop attributes
principle: Resume intercept must be the FIRST check in build_fleet. A pod with resume_state was traveling when stopped — that fact overrides all other entity attributes. Order of intercepts in build_fleet is load-bearing.
domain:    [SU]
