# TFTS — 2026-06-17T14:50:00Z

problem:   Placing vehicles and animation do not comply with Show Track — two coordinate systems
fault_ref: User observation: "2 coordinate systems, not one united under networks.json"

arc:
  - try:      Assumed pts mismatch or wrong transform in animation lookup
    result:   Partial — found root cause is missing track coverage, not wrong transform
    revealed: network.json only had 4 CP-level stub tracks per station (gw_0_in, gw_0_out etc.)
              from map.json beam_path attributes. Full intra-station tracks were only in path.json.

  - try:      Traced Show Track vs animation coordinate sources:
              - Show Track: lines.computed.json geometry['tracks'].pts_mm + inst_xf * inner_xf → world
              - Animation: network.json tracks (4 stubs) + proximity → incomplete/wrong routing
              - Vehicle placement: followme.json platform positions (world, correct)
    result:   confirmed: animation had no access to full intra-station track geometry
    revealed: path.json was the bridge (PathJSON applied same inst_xf * inner_xf transform),
              but it was archived. Without path.json, animation fell back to 4 CP stubs only.

  - try:      Add geometry track augmentation to generate_network_json:
              - For each station, scan inst.definition.entities for inner_xf (formation component)
              - Compute eff_xf = inst.transformation * inner_xf (same as Show Track)
              - Load lines.computed.json geometry['tracks'].pts_mm
              - Transform to world mm, write ALL tracks to network.json
              - Geometry tracks (many pts) WIN over 2-pt CP stubs from map.json
    result:   succeeded (committed 5535da2)
    revealed: The fix requires a full Rebuild of 2_thru_dip to regenerate network.json

principle: |
  network.json must contain world-space pts for ALL intra-station tracks, not just
  the CP-level stubs from beam_path attributes. The source is lines.computed.json
  geometry['tracks'].pts_mm (formation-local) + inst_xf * inner_xf transform.
  This is IDENTICAL to what Show Track applies — guaranteeing one coordinate system.
  
  The 4 CP-stub tracks (gw_0_in, gw_0_out) from map.json have wrong names (not lines.json
  names) and only 2 pts each — they cannot power BFS routing or animation.
  
  inst.definition.entities is the correct accessor for ComponentInstances in SketchUp
  (not inst.entities which works for Groups). beam_paths section in network.json was
  always empty because the entity scan used inst.entities (Group API) on ComponentInstances.
  
  After fix: rebuild writes full track coverage to network.json. Animation uses network.json
  directly; path.json is no longer required but still regenerated by PathJSON.export at
  animation start as a consistency check.

domain: SU
