# TFTS — 2026-06-17T17:05:02Z

problem:   Compute crashed with "undefined local variable or method `geo_tracks_xf'" after live edge scan block was inserted at wrong position

fault_ref: (no separate fault file — error reported in SketchUp console)

arc:
  - try:      Add live edge scan block inside write_lines_computed_from_geometry to rescue
              chord-cut curve tracks by reading arc tessellation directly from SketchUp model
              entities (commit 0a04ba7)
    result:   Failed — block was placed at line ~3565, before geo_tracks_xf is assigned at
              line ~3707 (now ~3635). Ruby raises "undefined local variable or method
              `geo_tracks_xf'". Compute fails for any template.
    revealed: Variable ordering matters in this 3700-line method. Any code that references
              geo_tracks_xf must be after line 3635 where it is assigned.

  - try:      Remove live edge scan block entirely (Bill rejected the approach — directed
              "no edge scan; everything from cp_marker math")
    result:   Succeeded — Compute runs again; geo_tracks_xf reference removed.
    revealed: Bill's direction: the cp_marker_* entities have 1750mm lines that end at the
              exact bottom center of each gw_→seg_ connection point. hub ± perp(tangent) *
              half_offset gives authoritative rail positions. This is more principled than
              edge scanning (which reads stale geometry, not the authoritative cp_marker).

  - try:      Implement cp_marker math synthesis for gw_cp outer endpoints (commit b17ef96):
              - Extend scanned_cps_mm to store tangent + half_offset (mm) from live cp_markers
              - After transform block, pin gw_cp_in_N.first and gw_cp_out_N.last to
                hub ± perp(tangent) * half_offset
              - Fix false-positive chord cut detection: _lead tracks that are axis-aligned
                (constant Y and Z, as in station_thru_dip) are NOT chord cuts — correct 2-pt
                representation for straight segments
    result:   Succeeded — Compute runs cleanly; cp_marker positions are authoritative
              for the seg_/gw_ connection boundary.
    revealed: The gw_cp_*_lead tracks in station_thru_dip are STRAIGHT horizontal segments
              (constant Y, constant Z in formation-local space). The Noelle logging was
              incorrectly flagging them as "chord cuts" because the pattern /_(lead|uturn|...)/ 
              assumed all _lead tracks are curves. In station_thru_dip they are not.
              Only flag _lead as chord-cut when 2-pt chord is non-axis-aligned.

principle: cp_marker math is authoritative for gw_→seg_ connection points. The cp_marker
           1750mm line ends AT the connection point; hub ± perp(tangent) * half_offset
           derives the exact rail position. Never use edge scanning for this — edges are
           stale geometry. The live cp_marker entity IS the spec.
           Variable placement rule: in write_lines_computed_from_geometry, any code
           referencing geo_tracks_xf must appear after line ~3635 where it is assigned.

domain:    SU
