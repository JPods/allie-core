# TFTS — 2026-06-17T21:05:00Z

problem:   2500mm jerks at gw_cp_out_lead/gw_cp_out/gw_cp_in junctions in network
           animation but not in standalone model

fault_ref: (no separate fault file — reported in animation log as CP gap warnings)

arc:
  - try:      Assumed the 2500mm gap was the cp_marker inset transform issue
              (prior session TFTS). Checked path.json — showed 0mm gaps.
    result:   Not the bug. Path.json has correct 2500mm coords for all cp tracks.
    revealed: Path.json is correct. Nora/Natalie use a DIFFERENT coordinate source
              for network trip plans — not path.json.

  - try:      Traced trip plan coordinates to network.json designer.stations[N].tracks.
              Found gw_cp_out_0/1: 317mm pts, gw_cp_in_0/1: 291mm pts — bbox stubs.
    result:   Root cause confirmed. Network.json has Build-time bounding-box stubs
              (317mm) for gw_cp_out and gw_cp_in. The geometry augmentation was
              supposed to replace them with lines.computed.json pts (2500mm) but failed.
    revealed: Augmentation condition: 'world_pts.size > existing.size'. Geometry from
              lines.computed.json for straight tracks is 2 pts. Bbox stub is also 2 pts.
              2 > 2 = false → bbox stub always won. Only curved tracks (uturn = 7 pts)
              were correctly replaced.

  - try:      Change condition to 'if true' — lines.computed.json geometry always wins.
              Commit 2504f5a.
    result:   Committed. Awaiting rebuild + animate test.
    revealed: The principle: lines.computed.json IS the authority. Never let a Build-time
              bounding box stub survive when geometry is available. The bbox is a fallback
              for tracks that couldn't be measured at Build time — not an alternative to
              the centerline geometry.

principle: lines.computed.json always wins over bbox stubs in network.json augmentation.
           The condition 'more pts = better' is wrong for straight tracks — it means
           a correct 2-pt centerline segment loses to a wrong 2-pt bbox stub.
           Correct rule: if lines.computed.json has geometry for a track, use it. Always.

           Answer to Bill's question: Natalie's function is identical in models and
           networks. The difference was DATA — standalone model reads correct
           lines.computed.json geometry; network reads network.json bbox stubs.
           Same function, different inputs → different behavior. Fix the input.

domain:    SU
