# TFTS — 2026-06-12T21:18:00Z

problem:   Z_CONTINUITY Δ=105mm fault on all seg_ ↔ gw_cp_in junctions after Build+Validate
fault_ref: 20260612T200646

arc:
  - try:      Baseline restore (origin/su_jpods_claude 6f69a7e) — TFTS 20260612T172000 claimed delta=0
    result:   Still showing Δ=105mm after reload+rebuild
    revealed: Baseline fix solved synthesis path, but gw_cp_in entries in map.json come from
              a geometry SCAN (not synthesis). The scan reads actual built SketchUp guideway
              edges and populates lines_out["s004.gw_cp_in_0"] BEFORE the synthesis runs.
              At line 2413: `next if lines_out.key?(dot_id)` — synthesis is skipped entirely.
              The scanned geometry was built by an older pipeline at a different Z than
              the entity attribute gives now. Two Z sources → delta.

  - try:      Override synthesis source (cp_gate_world from StructurePlacer entity attribute)
    result:   Build log confirmed cp_gate=12336mm, but validate still showed 12420.7mm
    revealed: Synthesis path is bypassed. The scanned gw_cp_in outer tip (pts.last) reflects
              the BUILT SketchUp geometry Z, not the entity attribute Z.
              The bridge reads pts.last = outer tip (inbound paths authored inner→outer).
              seg_ endPoint Z = entity attribute cp_center.z * 25.4 = 12315.7mm.
              Scanned gw_cp_in outer tip Z = 12420.7mm (built geometry, older pipeline).

  - try:      Post-scan Z correction in noelle.rb, after the arm synthesis loop (line ~2423):
              After scan populates lines_out for gw_cp_in_N and gw_cp_out_N,
              override pts.last['z'], endPoint['z'], and segments.last['ze'] to
              arm_anchors[n]['cp_center_z_mm'] = (cp_gate_world.z * 25.4).round(1).
              cp_gate_world comes from StructurePlacer.connection_point (entity attribute) —
              the same datum used by seg_ cp_math.
    result:   Succeeded:
              "Noelle map: s004.gw_cp_in_0 outer-tip Z corrected 12420.7 → 12335.7mm (entity attr)"
              "Noelle map: s004.gw_cp_out_0 outer-tip Z corrected 12315.7 → 12335.7mm (entity attr)"
              Validate: "Noelle: Network validation OK — streak reset" — no Z_CONTINUITY fault.

principle: The scan reads actual built SketchUp guideway geometry and takes priority over
           synthesis (`next if lines_out.key?`). When the model was built by an older pipeline
           the scanned outer-tip Z may differ from the entity-attribute CP center Z used by
           seg_ (cp_math). The fix is a post-scan correction pass after the arm synthesis loop:
           iterate arm_anchors, find each gw_cp_in_N and gw_cp_out_N in lines_out,
           and force pts.last['z'] to (cp_gate_world.z * 25.4) from the entity attribute.
           This makes both sides of every seg_ ↔ gw_cp junction derive from the same datum
           (StructurePlacer entity attribute) → Z_CONTINUITY delta = 0 trivially.
           The correction only fires when the discrepancy exceeds 1mm; it is logged.

           Bridge convention: for both gw_cp_in and gw_cp_out,
           pts.last = outer tip (faces the seg_). Inbound paths authored inner→outer.

domain: SU
