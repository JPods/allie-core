# TFTS — 2026-05-31T18:00:00Z

problem:   station_thru_dip routing through 2 CPs — pod entering S004.CP1,
           traversing far_main, exiting at S004.CP0, then taking another
           inter-station seg back to S003.CP1. "seg_ must connect to only
           2 open CP units" rule violated.

arc:
  - try:    fix noelle_features.json landing.in_cp1 with correct segment names
            (gw_cp_in_1, gw_cp_in_lead_1, gw_far_main, gw_cp_out_lead_0, gw_cp_out_0)
    result: gw_far_main dead end resolved — pass-through now wires in map.json.
            But trip planner's arrive_segments(S004, 1) now includes gw_cp_out_0,
            which is S004.CP0's exit boundary. Pod's trip plan ends at gw_cp_out_0,
            dep_segs then wires gw_cp_out_0 → seg_S004_cp0_S003_cp1. Pod continues
            past the station boundary, arriving back at S003.CP1.
    revealed: landing.in_cp1 and the map.json routing chain are separate systems.
              noelle_features.json landing chains feed the trip planner (arrive_segments).
              lines.json successors[] feed map.json routing chain override.
              Including CP0 exit territory in landing.in_cp1 conflates the two.

  - try:    truncate landing.in_cp1 to ["gw_cp_in_1", "gw_cp_in_lead_1", "gw_far_main"]
    result: pending — requires Build to regenerate feature.json then trip.json
    revealed: gw_far_main → gw_cp_out_lead_0 is declared in lines.json ep 1 and
              wired in map.json by the routing chain override. It does NOT need to
              be in the landing chain. The landing chain defines the TRIP PLAN path
              (what Nora navigates exactly); map.json topology defines the physical
              connections. These must stay separate.

principle: landing.in_cpN defines the intra-station route for pods arriving at CPN
           as a DESTINATION. It must stay within CPN's own territory. For
           station_thru_dip, CP1 territory ends at gw_far_main — the far lane is
           CP1's dedicated inbound corridor. gw_cp_out_lead_0 and gw_cp_out_0 belong
           to CP0's territory. Including them in landing.in_cp1 causes the trip plan
           to end inside CP0's territory, enabling dep_segs wiring to fire a new
           inter-station seg FROM CP0 — routing through 2 CPs.

           The seg_ → station boundary rule: each inter-station seg connects exactly
           one CP of station A to one CP of station B. A landing chain that extends
           past the arrival CP's territory into another CP's exit path violates this
           boundary. The correct architecture:
             - landing.in_cp1 ends at gw_far_main (CP1 territory)
             - gw_far_main → gw_cp_out_lead_0 is wired by lines.json ep 1 in map.json
             - These are separate files with separate responsibilities

           Noelle should validate this: any track in landing.in_cpN that appears in
           originating.out_cpM (where M != N) is crossing a CP boundary — fault:
           "TEMPLATE: landing.in_cp1 includes gw_cp_out_lead_0 which is in
            originating.out_cp0 territory. Landing chain must not cross CP boundaries."

domain:    SU
