# TFTS — 2026-06-17T18:38:38Z

problem:   Compute gap checker reported 11 severe gaps (333.3mm, 166.7mm, 500.0mm) in
           station_thru_dip that did not exist in the SketchUp model

fault_ref: (no separate fault file — error appeared as Compute blocked message)

arc:
  - try:      Walk formation_inst.definition.entities, collect edges tagged gw_*,
              chain into polylines, compare endpoints for junction gaps
    result:   Failed — reported 11 false severe gaps including phantom tracks
              gw_lift and gw_platform_parking at 333.3/166.7/500.0mm offsets
    revealed: Two compounding causes:
              (1) gw_lift is reference geometry (vertical transition zone), not a drive
                  path. PathJSON.station_track? explicitly excludes it, but the custom
                  edge walker had no such filter.
              (2) gw_* groups contain structural geometry (u-channel, rail cross-section)
                  alongside the centerline path edges. Edge chaining has no way to
                  distinguish centerline edges from structural edges — it picks up
                  everything in the group. The 500mm regular spacing is the u-channel
                  cross-section width, not a real positional gap between track endpoints.
              Log notation .first and .last was also unknown to the designer.

  - try:      Filter gw_lift via station_track? equivalent; propagate gw_ sub-group tags
              so parent containers don't absorb child track edges
    result:   Failed — same 11 false gaps, same values. The u-channel edges were the
              source, not the container hierarchy. Filtering by tag did not remove them.
    revealed: The problem is structural geometry INSIDE gw_-tagged groups, not the
              group hierarchy. No amount of tag filtering fixes raw edge traversal when
              the group contains multiple edge sets.

  - try:      Delegate to JPods::PathJSON.populate_from_open_template +
              generate_geometry_json — the same extraction pipeline Show Track uses.
              PathJSON reads the jpods_path centerline attribute on each entity,
              not raw edges. Excludes gw_lift via station_track?.
    result:   Succeeded — Compute ran clean: 19 tracks, no severe gaps, no chord cuts.
              geometry.json archived. lines.computed.json written.
    revealed: The jpods_path attribute is the ONLY reliable source of centerline geometry
              from gw_* groups. Raw edge traversal is always wrong because gw_* groups
              contain structural cross-section geometry alongside the path edges.
              Show Track works because it reads jpods_path too — it never traverses
              structural edges.

principle: No edges. Only math.
           SketchUp edges are structural geometry — u-channel, rails, cross-sections.
           They are never the authoritative source for vehicle path position.
           The jpods_path attribute IS the path. cp_marker math IS the CP position.
           Any code that needs gw_* track pts must go through PathJSON (jpods_path).
           Any code that needs CP positions must use cp_marker hub ± perp * half_offset.
           Edge traversal is always wrong for JPods geometry extraction.

           Gap log notation: .first and .last are opaque to designers.
           Use [start] and [end] — terms that match how designers think about tracks.

domain:    SU
