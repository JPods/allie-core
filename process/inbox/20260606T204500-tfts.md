# TFTS — 2026-06-06T20:45:00Z
problem:   Animation "odd motions" on s002.gw_platform_out — pod zig-zags in 165mm area instead of traveling 18m ramp
fault_ref: (no FAULT file — observed at animation start on fresh trial5 setup)

arc:
  - try:      Read trajectory log (20260606T175949-trajectory.jsonl) to identify which tracks produced odd motion
    result:   Succeeded — s002.gw_platform_out t_pct 15→94 with Y/Z oscillating ±500mm in a ~165mm X range
    revealed: pod is traveling the FollowMe solid's cross-section edges, not the ramp centerline

  - try:      Check anim-coords log for s002.gw_platform_out path data
    result:   Found two variants:
              S002.gw_platform_out — source=edge_attr, 14 pts, X goes -102294 to -120681 (correct 18m ramp)
              s002.gw_platform_out — source=extracted, 115 pts, X stays -103000 to -103165 (zig-zag)
    revealed: Animator uses lowercase-dotted key (s002.gw_platform_out) → gets 115-pt zig-zag from path.json

  - try:      Trace how s002.gw_platform_out got 115-pt zig-zag into path.json
    result:   path.json source=extracted 114 segments. Segments form repeating pattern:
              move Y 167mm → Z +250mm → Y 167mm → Z +250mm → Y 167mm → Z -500mm (back to start cross-section)
              This is the FollowMe solid beam's face edges, not the centerline.
    revealed: populate_from_open_template traced FollowMe solid edges → 115 zig-zag pts stored in extracted.json
              → path.json inherits them. Source field = "extracted" preserved throughout.

  - try:      Trace why edge_attr 14-pt smooth path doesn't win in animator merge
    result:   Edge merge logic (jpod_vehicle_anim.rb line 1825):
              edge_entry[:pts].size > json_entry[:pts].size → 14 > 115 = false → edge LOSES
              Designed for arc upgrade (2-pt chord → 14-pt arc). Fails when extracted has MORE zig-zag pts.
    revealed: The "more pts wins" heuristic breaks when extracted source contains FollowMe solid face edges
              (115 non-monotonic pts > 14 correct smooth ramp pts).

  - try:      Add condition: edge_attr always beats extracted source when edge has ≥ 2 pts
    result:   Succeeded — modified jpod_vehicle_anim.rb edge merge:
              elsif json_entry[:source].to_s == 'extracted' && edge_entry[:pts].size >= 2
                lookup[k] = edge_entry   # edge_attr beats extracted
              All 4 cid variants (S002., s002., S002_, s002_) replaced with 14-pt edge_attr path.
    revealed: Lowercase cid (s002.gw_platform_out) is what the maneuver builder actually uses.
              After fix, it gets the correct 14-pt smooth ramp from ene_railroad attributes.

principle: When extracted.json contains FollowMe solid edge-trace geometry (zig-zag pts), the
           animator's "more pts wins" heuristic incorrectly favors the zig-zag over the correct
           ene_railroad smooth path. Source metadata is more reliable than pts count for deciding
           which path is authoritative. edge_attr (ene_railroad) always beats extracted (populate
           edge-trace) for intra-station tracks — extracted may contain FollowMe solid topology
           rather than a centerline.

           The path.json/extracted.json for gw_platform_out contains zig-zag because:
           populate_from_open_template → jpods_path attribute write → Priority 1 in scanner →
           Proof Lines says OK (115/115 match) → path.json inherits 115 zig-zag pts.
           Proof Lines verified self-consistency (attr matches extracted) not smoothness.
           The fix is in the animator, not in the verification pipeline.

           gw_lift_in also has 115-pt zig-zag in extracted/path.json, but lift tracks are
           excluded from routing (LIFT_TRACK_RE) and edge_lookup — no visible animation effect.

domain:    SU
