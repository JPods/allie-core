# TF — 2026-06-21T16:00:00Z
summary: Decision to rewrite the Compute pipeline as a new program. Current code is 3700+ lines of accumulated patches — edge walkers, naming normalizations, authored flags, monolithic functions. The three-phase architecture (validate → build chains → extract geometry) is proven in concept. The design axioms, schemas, agent responsibilities, and TFTS lessons carry forward. The 3+circle network with its known defects (missing chains, Z gaps, seg_ direction) is the proof-of-concept test case. Rewrite first, then prove on 3+circle.
code: jpod_path_json.rb → new class-based Compute pipeline
