# TFTS — 2026-05-23T23:15:08Z

problem:   CP stutter — vehicles appear to turn at the stub_pair→inter-station junction
fault_ref: (observed, no separate FAULT file)

arc:
  - try:      noelle.rb generate_map_json — snap stub_pair end_pt XY to cap_pt
    result:   succeeded for map.json paths; stutter gone when path.json was empty
    revealed: two separate geometry files serve the animator: map.json AND path.json

  - try:      path.json populated after new BUILD (d73b95f Z normalization commit ran
              a successful build for the first time in this model); animator uses
              path.json over map.json; path.json stub_pair endpoints come from deep
              entity scan (raw stub outer face position, no cap_pt snap) vs
              inter-station beam endpoints at cap_pt; 83mm gap reintroduced via path.json
    result:   stutter back
    revealed: cap_pt mechanism must exist in EVERY file the animator reads, or not at all

  - try:      remove end caps from station templates; remove cap_pt XY logic from
              build_segment and generate_map_json; stub outer face IS the connection seam
    result:   (pending template edits — code changes made in this session)
    revealed: the cap is an architectural source of complexity with no physical benefit;
              unconnected stubs can be indicated by color/tag instead

principle: A workaround that patches one file must be applied to every file that reads
           the same geometry.  If a fix must live in multiple places, it is not a fix —
           it is a recurring liability.  Remove the source geometry instead.

domain:    SU

## What still needs to happen

1. User: in SketchUp, open each station template component, delete the dead_cap_end
   group from every gw_stub_pair_N end (both _in and _out stubs, all CPs).
   Templates: station_solar, station_line_end, JPods_station_parking, station_thru_dip
   (traffic_circle7 has no stub_pair lines — verify cap status separately)

2. User: regenerate line.json for each template after cap removal.
   Run: Extensions > JPods > Utilities > Populate Template Geometry (or equivalent)

3. Code (done): build_segment now uses from_cp[:center] XY directly, no cap_pt fallback.

4. Code (done): noelle.rb generate_map_json cap_pt snap removed.

5. Code pending: add unconnected stub visual — paint gw_stub_pair groups that have no
   matching inter-station connection in followme.json a warning color (orange) during
   Validate, so students can see which stubs need connections.

6. Code pending: remove cap_pt_lookup block from generate_map_json (now dead code).
   Remove open_all_structure_stubs_from_cache's cap_extension tracking.
   Remove from_cp[:cap_pt] and cap_extension from normalized_cp.

## Z normalization (keep)

stub_tips Z normalization in build_segment (d73b95f) is still needed:
from_cp[:center].z = gate_ctr = average of departure + arrival stub Z.
If both stubs are at the same height, exact.  If not, per-stub Z from
map.json stub_pair_0_out corrects the anchor.
