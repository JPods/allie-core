# TFTS — 2026-05-24T18:00:00Z

problem:   Visual raggedness at gate connections — inter-station beam seam at stub junction
fault_ref: (ongoing from 20260523T231508-tfts.md)

arc:
  - try:      Fix CP position: world_xform.origin → world_xform * bounds.center
    result:   CP detection confirmed working (4/4 found). Seam still visible.
    revealed: CP position was not the primary cause. bounds.center still off
              in along-track direction (~81-296mm short of outer face).

  - try:      Add outer-face projection: project 8 bounding box corners onto outward
              tangent; shift cp center to max projection (outer face of cp component).
    result:   Outward_ext = 111-296mm — inconsistent across station types.
              Seam still visible ("still the same problem").
    revealed: Bounding box corners are not reliable. cp component bounding box includes
              decorative geometry that extends past the actual gate face.

  - try:      Erase beam cap faces instead of hiding them (ene_railroad open-tube).
    result:   Pending build test.
    revealed: Hidden faces z-fight with station stub cap at same position.
              Erasing removes the beam cap entirely; no z-fighting possible.

principle: Hidden faces still exist in SketchUp geometry and z-fight with coincident
           faces in other groups/components. At gate junctions, the station stub has a
           cap face and the inter-station beam had a cap face at the same XY position.
           Two faces at the same position = z-fighting = visual artifact ("raggedy").
           Erase caps; do not hide them. This is the ene_railroad open-tube principle.

domain:    SU

## Unresolved: bounds.center vs. origin for cp gate reference

The 1.5m-off measurement that caused the switch to bounds.center was made on OLD geometry
(before commit 37b7bd9 placed cp instances in all 4 templates on 2026-05-24 morning).
The new cp instances may have origin at the gate outer face. Added origin= to CP detection
log — read on next build to determine correct reference.
