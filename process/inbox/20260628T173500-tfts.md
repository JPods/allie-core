# TFTS — 2026-06-28T17:35:00Z

problem:   cp_marker in/out tip assignment unstable — 180° rotated tracks, 4.3m offsets, breaks on Show Formation Tracks
fault_ref: Session 2026-06-28 — Compute rewrite introduced model.bounds.center for in/out determination
arc:
  - try:      model.bounds.center distance comparison to determine in_tip vs out_tip
    result:   failed — bounds.center shifts when Show Formation Tracks adds geometry, swapping in/out randomly
    revealed: bounds.center is visual, not a stable datum (SketchUp API gotcha, Axiom 10)
  - try:      Platform tag scanning (_find_tagged_reference) to determine in_tip
    result:   failed — gw_cp_in/out groups are at the same CP, equidistant from both tips
    revealed: Proximity-based inference can't distinguish tips at the same marker
  - try:      Transformation matrix Y-axis for tip positions
    result:   failed — world_xf.origin is component insertion point, not hub vertex
    revealed: Component origin ≠ hub position
  - try:      Hub-to-hub cross product for left/right convention
    result:   failed — convention assumes consistent handedness, breaks on station_parking where in/out swaps between CPs
    revealed: in/out is topological (CCW flow), not geometric (left/right)
  - try:      Read edge tags/layers on cp_marker rail edges
    result:   failed — edges have no in/out tags, and edge scanning is fragile
    revealed: Bill's rule: "No edges. Pure math."
  - try:      Read from component definition without transforms (avoid world coords)
    result:   partial — both markers share same definition, returned identical coords
    revealed: Instance transform needed to differentiate markers, but only within model scope
  - try:      Instance transform within station definition (no top-level model transform)
    result:   partial — hubs correct but tangent identical for both (222mm in definition space same for both instances)
    revealed: 222mm direction must be transformed per-instance
  - try:      Pure math from points — find hub by centroid proximity, classify by distance, dot product for outward/inward
    result:   succeeded
    revealed: The cp_marker has 4 points: hub (intersection), 222mm end (direction), two 1750mm ends (guideway centerlines). Dot product of (tip - hub) with (222mm_end - hub) determines outward vs inward. No edges, no bounds, no transforms in Compute.

principle: |
  cp_marker geometry is four points from a hub: two 1750mm guideway centerlines + one 222mm network direction + one 177mm vertical.
  Read POINTS, not edges. Find hub by centroid, classify by distance, assign outward/inward by dot product with 222mm vector.
  Compute is MODEL ONLY — reads definition-local coordinates, never applies world transforms. Build translates to world.
  The in/out assignment is topological (from lines.json CCW chains), not geometric. The 222mm vector determines which tip
  faces the network; lines.json determines which track name (gw_cp_in vs gw_cp_out) maps to which physical tip.

domain:    [SU]
