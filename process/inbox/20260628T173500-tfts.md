# TFTS — 2026-06-28T17:35:00Z

problem:   cp_marker in/out tip assignment unstable — 180° rotated tracks, 4.3m offsets, 2500mm shifts
fault_ref: Session 2026-06-28 — Compute rewrite introduced model.bounds.center for in/out determination
arc:
  - try:      model.bounds.center distance comparison to determine in_tip vs out_tip
    result:   failed — bounds.center shifts when Show Formation Tracks adds geometry, swapping in/out randomly
    revealed: bounds.center is visual, not a stable datum (SketchUp API gotcha, Axiom 10)
  - try:      Platform tag scanning (_find_tagged_reference) to determine in_tip
    result:   failed — gw_cp_in/out groups are at the same CP, equidistant from both tips
    revealed: Proximity-based inference can't distinguish tips at the same marker
  - try:      Transformation matrix Y-axis for tip positions
    result:   failed — world_xf.origin is component insertion point, not hub vertex
    revealed: Component origin ≠ hub position
  - try:      Hub-to-hub cross product for left/right convention
    result:   failed — convention assumes consistent handedness, breaks on station_parking where in/out swaps between CPs
    revealed: in/out is topological (CCW flow), not geometric (left/right)
  - try:      Read edge tags/layers on cp_marker rail edges
    result:   failed — Bill: "No edges. Pure math."
    revealed: Edge scanning is fragile and violates the pure math principle
  - try:      Read definition vertices without transforms (no parent transforms)
    result:   failed — both markers share same definition, returned identical coords
    revealed: Instance transform needed to differentiate markers within model scope
  - try:      Dot product of 222mm direction with tip offset
    result:   failed — 222mm is perpendicular to tips, dot product = 0 for both
    revealed: 222mm points toward network SIDE, not along station axis
  - try:      Tangent from hub-to-hub axis, initial sign wrong
    result:   failed — tangent inverted, uturn offset went wrong direction (5000mm)
    revealed: Above mid_y should get OPPOSITE axis direction (outward), not same
  - try:      Cross product of 222mm direction × tip offset for outbound/inbound assignment
    result:   succeeded — positive Z = outbound, negative Z = inbound
    revealed: 222mm vector distinguishes outbound from inbound via cross product, stable per marker
  - try:      All tracks extend INWARD from cp_marker tip, uturn at junction 2500mm in
    result:   succeeded — Show Tracks aligned with station geometry, colors correct
    revealed: cp_marker tip IS the outermost dangling end; everything extends inward

principle: |
  cp_marker geometry: four points from CP point (hub). Two 1750mm guideway centerlines,
  one 222mm network direction (perpendicular to axis), one 177mm vertical.
  
  Reading: pure math from points. Hub = vertex with most edges. Classify by distance.
  Cross product of 222mm_dir × tip_offset: +Z = outbound, -Z = inbound.
  
  Tangent: from hub-to-hub axis, NOT from 222mm. Each marker's tangent points OUTWARD
  from model center. Above mid_y → opposite axis direction. Below → same as axis.
  
  Track layout: cp_marker tip (outermost, dangling for Network) → gw_cp_in/out (2500mm
  inward) → JUNCTION (uturn) → gw_cp_in/out_lead (2500mm more inward) → model interior.
  ALL tracks extend inward from the tip. The uturn is at the junction, 2500mm in.
  
  Compute is MODEL ONLY. Reads definition-local coordinates. Build translates to world.
  lines.json defines flow (successors + predecessors on every track).
  All guideways one-way. All circulation CCW (Rule 12).

domain:    [SU]
