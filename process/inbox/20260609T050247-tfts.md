# TFTS — 2026-06-09T05:02:47Z

problem:   Bezier curves for gw_lift_in/platform_in/platform_out looped instead
           of taking the shortest path between endpoints

arc:
  - try:      Used ev = vehicle arrival direction (-1,0,0) in B2 = p3 + h*ev.
              This placed B2 on the same side as the destination in the -X
              direction. The curve was forced to loop back to arrive at p3 from
              the correct side.
    result:   Curve made a large loop (~180° error at outbound end) instead of
              a smooth arc.
    revealed: B2 = p3 + h*ev places the handle in the ev direction FROM p3.
              The cubic Bezier arrives at p3 FROM B2, so the arrival direction
              is p3 - B2 = -ev. If ev = arrival direction, arrival becomes -(-1) = +X,
              which is 180° wrong.

  - try:      Set ev = outward from p3 back toward the approach side = (+1,0,0)
              for tracks where vehicle arrives in -X. B2 = p3 + h*(+1,0,0)
              places B2 to the +X side. Curve arrives at p3 from +X, traveling
              in -X. Station_line_end (stored reversed): ev=(-1,0,0).
    result:   Correct smooth S-curves. Spacing ~970-996mm. No loops.
    revealed: ene_railroad convention confirmed: sv/ev are OUTWARD from each
              endpoint — sv = departure direction from p0, ev = direction BACK
              TOWARD p0 from p3 (not the arrival direction but its opposite).

principle: In B1 = p0 + h*sv, B2 = p3 + h*ev:
           sv = outward at FROM end (vehicle departure direction) ✓
           ev = outward at TO end = OPPOSITE of vehicle arrival direction
           The curve arrives at p3 from B2, so arrival direction = p3 - B2 = -ev.
           Equivalently: ev = -1 × (vehicle travel direction at p3).
           If sv and ev are both the vehicle travel direction, the Bezier loops.
           Axiom 11 states this for the TO endpoint: "use to_cp[:tangent].reverse".

domain:    SU
