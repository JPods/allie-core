# TFTS — 2026-06-15T05:50:00Z

problem:   gw_uturn_* animate as 2-pt straight lines; gw_c_* connector arcs animate wrong
           (62m phantom arcs for cpu stations); recurring bezier mandate violation

fault_ref: 20260615T040000-tfts.md, 20260527T025100-tfts.md

arc:
  - try:      synthesize_circle_arcs! using map.json chord endpoints for circle fitting.
    result:   Traffic circle (s001) gw_c_* long arcs upgraded correctly.
              cpu stations (s002/s003/s004) gw_c_* produced 62m phantom arcs.
              gw_uturn_* not handled at all (ARC_TRACK_RE matched only gw_c_*).
    revealed: map.json for cpu stations contains 25172mm scan artifacts (TFTS 2026-05-27:
              "gw_uturn was entire donut solid — 25172mm winding path"). Map.json was
              NEVER regenerated after lines.json was fixed with correct math.
              Uturn chord in map.json = 303mm XY / 585mm 3D (physical beam stub position),
              not 3500mm arc diameter. Circle fit from these wrong endpoints produces
              enormous wrong circles. map.json is NEVER a valid source for arc geometry.

  - try:      Confirmed: path.json for 3+circle model has 86 entries, ALL seg_* (inter-station).
              Zero intra-station entries. PathJSON.export() wrote nothing for station interiors.
              Everything intra-station was coming from map.json gap-fill.
    result:   Root cause confirmed. The geometry.json fix to traffic_circle7 was correct
              but had no effect because path.json was empty of intra-station data.
    revealed: PathJSON.export() requires station component instances with formation_id
              attributes. Without them, no intra-station data is written to path.json.
              The entire intra-station geometry pipeline (geometry.json → path.json) was
              silently bypassed.

  - try:      Two-pass arc upgrade:
              1. upgrade_arcs_from_beam_path! — scan JPods Guideway groups for arc tracks,
                 use beam_path (set at Build time from lines.json math) when denser than
                 current lookup entry.
              2. synthesize_arc_pts_from_math! — for remaining 2-pt arcs:
                 gw_uturn_*: validate chord ≈ 3500mm (uturn diameter), synthesize 180° arc
                 at 1750mm; skip if chord << 3500mm (scan artifact).
                 gw_c_*: fit circle from dense ANCHOR arcs (≥5 pts, from beam_path) only;
                 synthesize 2-pt chord connectors in same station using fitted circle.
                 Skip station if no dense anchor exists.
    result:   Implemented. beam_path is authoritative; math synthesis handles any
              remaining gaps; mandate guard blocks if both fail.
    revealed: The entire failure chain is driven by one violated principle: map.json
              scan data for arc tracks is never correct geometry. The correct source
              is always beam_path (Build time, from math) or explicit math synthesis.

principle: NEVER use map.json pts as arc geometry source. For gw_c_* and gw_uturn_*:
           beam_path attribute on built JPods Guideway groups is the authoritative source.
           It is set at Build time by PathBuilder from lines.json mathematical declarations.
           It does not change unless the user rebuilds. It does not get wiped by PathJSON.export.
           Math synthesis (synthesize_arc_pts_from_math!) is a fallback for unbuilt stations only.
           Validation rule: uturn chord must ≈ 3500mm (diameter); if not, it is a scan artifact.
           The mandate guard must block animation — not degrade gracefully — when arcs remain
           at 2 pts, directing the user to run Build.
           Source hierarchy: beam_path > math synthesis > BLOCK.
           Rule: never synthesize from wrong endpoints. Validate before synthesizing.

why_it_recurred: Every session fixed the symptom (2-pt chords) at the WRONG LAYER:
  Session 1: .upcase→.downcase key fix in edge lookup → one-time fix, no persistence
  Session 2: edge merge condition fix → one-time fix, wiped by PathJSON.export
  Session 3: synthesize_circle_arcs! from map.json endpoints → wrong endpoints for cpu
  Session 4 (today): geometry.json connector fix → path.json still empty, no effect
  Each fix touched the animator or the file pipeline, not the BUILD TIME source.
  beam_path is set at BUILD time and persists. That is the correct layer.

domain:    SU
files:     jpod_vehicle_anim.rb: upgrade_arcs_from_beam_path!, synthesize_arc_pts_from_math!
           (replaces synthesize_circle_arcs!); expanded ARC_TRACK_RE; updated mandate guard
commit:    83036e6
