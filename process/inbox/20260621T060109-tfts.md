# TFTS — 2026-06-21T05:00:00Z
problem:   _cp stripping in seg_ normalization destroyed track names (gw_cp_in_lead_0 → gw__in_lead_0), breaking routes to s004/s005
fault_ref: (observed during testing — pods not dispatched to s004/s005)
arc:
  - try:      gsub('_cp', '_') to strip _cp from seg_ connection IDs
    result:   failed — also stripped _cp from track names like gw_cp_in_lead_0, gw_cp_out_0. These tracks became invisible to the routing graph and were pruned as unconnected. All routes through s004/s005 broken.
    revealed: _cp appears in both seg_ connection IDs (where it's redundant) and track names (where it's essential). A blanket gsub cannot distinguish.
  - try:      gsub(/_cp(\d)/, '_\1') — only strip _cp when followed by a digit (CP index)
    result:   succeeded — seg_s001_cp1_s003_cp1 → seg_s001_1_s003_1 (correct), gw_cp_in_lead_0 preserved (correct)
principle: Never gsub a substring that appears in both target and bystander strings. Use a regex that matches the specific context — _cp followed by a digit is a seg_ ID artifact; _cp_ followed by a letter is a track name component.
domain:    [SU]
