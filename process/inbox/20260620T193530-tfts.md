# TFTS — 2026-06-20T19:35:30Z
problem:   Show Track bezier follows curve on one direction but draws straight line on the other
fault_ref: (none — observed after rebuild)
arc:
  - try:      Assumed both directions had beam_path from built guideway groups
    result:   failed — Build creates ONE physical guideway group per connection (e.g. seg_s006_cp1_s007_cp0). The reverse direction (seg_s007_cp1_s006_cp0) is a separate map entry but has no built group, no beam_path entity attribute. Show Track and network.json fell back to 2-pt chord (straight line between CPs).
    revealed: One guideway group, two map directions. beam_path is stored per physical group, not per logical direction.
  - try:      When a seg has no beam_path, parse the connection ID to construct the reverse ID (swap from/to stations and CPs). If the reverse has beam_path, use its pts reversed.
    result:   succeeded — both directions now follow the bezier. Fixed in noelle.rb (network.json write) and jpod_animator.rb (Show Track overlay read).
principle: Build creates one physical beam per connection. Any code that reads beam_path must check the reverse connection ID. The physical curve is the same — just reversed. Parse seg_A_cpN_B_cpM ↔ seg_B_cpM_A_cpN.
domain:    [SU]
