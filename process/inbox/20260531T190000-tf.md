# TF — 2026-05-31T19:00:00Z

summary: lines.json is the complete authority on station path geometry.
         landing_chains and exit_chains defined by model designer.
         Noelle derives chain_mm and chain_switches and writes to feature.json.
         Sally reads feature.json to tell Natalie exact path requirements
         before every pod movement. noelle_features.json retains only
         behavioral policy (pass_far, pass_near, cp_count, structure_type).

code:    su_jpods/noelle.rb — generate_feature_json landing_chains + exit_chains + chain_mm + chain_switches
         su_jpods/noelle_features.json — originating + landing removed for station_thru_dip
         su_jpods/templates/track_formations/station_thru_dip/lines.json — landing_chains + exit_chains
