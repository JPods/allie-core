# TFTS — 2026-06-23T14:00:00Z
problem:   Build Z-defects at waypoints on terrain
fault_ref: session observation
arc:
  - try:      Read waypoint Z from bounds.center (bounding box midpoint of all marker geometry)
    result:   failed — bounds.center.z is visual center of stem+post+circles, not routing Z
    revealed: marker stores beam_z attribute — the authoritative routing elevation
  - try:      Add CLEARANCE_HEIGHT to marker Z for waypoint target
    result:   failed — beam_z is ALREADY at beam level (terrain + clearance at placement)
    revealed: double clearance pushed target 4.6m too high, grade limits fought it
  - try:      Grade envelopes from CP endpoints with waypoint Z as desired profile
    result:   failed — grade_fwd from s005 Z=6.77m overrode waypoint Z at 195m distance
    revealed: grade cones must run between WAYPOINT anchors, not between CPs
  - try:      Hard pin waypoint Z after smoothing
    result:   failed — created sharp spike at single point, smooth everywhere else
    revealed: the pin fights the smoother; let piecewise desired_z flow naturally
  - try:      hard_floor_z (raw terrain + clearance) in min_z_bounds
    result:   failed — every SU terrain mesh bump forced beam up
    revealed: smooth guideways are primary; columns absorb terrain; raw mesh is noise
  - try:      Terrain raycast into Entities directly
    result:   failed — Sketchup::Entities has no raytest method
    revealed: only Model has raytest; use model.raytest with filtering instead
  - try:      :no_terrain Symbol as sentinel in Point3d.z
    result:   failed — Point3d requires Float, Symbol crashes math
    revealed: sentinels in SketchUp must be numeric (use impossible Z value)
  - try:      Revert to bounding-box skip + z=0 interpolation
    result:   succeeded — Build works, profiles smooth
principle: When the working simple approach builds correctly and the elegant replacement breaks, revert and ship. The proper fix is documented for later — don't hold working code hostage to a better architecture.
domain:    [SU]
