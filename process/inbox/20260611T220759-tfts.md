# TFTS — 2026-06-11T22:07:59Z

problem:   CP gap jumps during s002→s001 trip — 8 of 14 maneuvers skipped, pod deviated
fault_ref: (no separate fault file — diagnosed from console log lines 410537-410689)

arc:
  - try:      Sally.sequencing? hypothesis — checked if Sally hold_loop intercept was hijacking NORA_0001 mid-trip
    result:   failed — Sally.reset() is called at animation stop, clearing @@pod_states; stale state impossible across runs
    revealed: Sally.sequencing? could still fire if animation was never stopped (mid-session switch from hold_loop to trip)

  - try:      Large dt hypothesis — timer latency causes 16× speed, short segments complete in <1 tick, skip
    result:   failed — each maneuver still takes at least 1 tick to arrive; large dt alone cannot skip a maneuver from the queue

  - try:      receive_maneuver returns false hypothesis — pod state not :arrived when dispatch fires
    result:   failed — arrived_ids is built from pods in :arrived state; enforce_spacing! skips :arrived proposals

  - try:      Duplicate NoraPod objects in @@pods — diagnosed from console log
    result:   succeeded — triple TickLog for seg=52 (actual=47, 46) and quadruple trip_complete (actual=5,4,3,2)
              proved 4 NoraPod objects for NORA_0001 existed by trip end, all arriving at seg=15 in the same tick
    revealed: Each pod shares @@queues[NORA_0001]; when N pods consume the shared queue interleaved,
              every other queue item gets consumed by a "shadow" pod that doesn't emit visible log output
              → appears as maneuvers being skipped, but they were consumed by duplicate pods

principle: >
  _dispatch_next_sequential and compact_platform_idle both do @@pods << pod without checking if
  the nora_id is already active in the fleet. natalie_sweep fires on a periodic timer and calls
  _dispatch_next_sequential for any station with pending_dispatch entries. If a trip pod (NORA_0001)
  is also in Sally's pending_dispatch queue (placed there during build_fleet for the s001 hold_loop
  vehicles that share the same vehicle_id), each sweep creates another NoraPod for NORA_0001.
  N sweep cycles → N duplicate pods → N pods sharing one queue → systematic maneuver skipping.
  Fix: guard at both creation sites — skip if @@pods.any? { |p| p.nora_id == nid }.

domain: SU
