# TFTS — 2026-06-05T06:33:23Z

problem:   CP Calculate assigned wrong indices to traffic_circle7 CPs (CW scrambled order)
fault_ref: (this session — nil model + wrong CP ordering + wrong cp_marker assignment)
arc:
  - try:      Fix nil model in cmd_execute (model closure going stale)
    result:   succeeded — added `model = Sketchup.active_model if model.nil?` at top of cmd_execute block

  - try:      Read cp.json in recompute_all_cps as step 0 (formation map authority)
    result:   failed — cp.json positions are in LOCAL definition space of the template model;
              applying inst.transformation in _show_cp_circles double-transforms them into
              wrong world positions; CPs no longer appeared at cp_marker locations

  - try:      Remove order_connection_points from cp_marker code path; sort by :index directly
    result:   partially succeeded — CCW order restored, but still off by 1
              (cp_marker_0 labeled 1, cp_marker_1 labeled 2, etc.)

  - try:      Fix double-transform: `e.transformation * e.bounds.center` → `e.transformation.origin`
    result:   still off by 1 — nearest-neighbour matching was unreliable because
              the edge-detected hub vertex position ≠ component origin position

  - try:      Eliminate nearest-neighbour entirely: save marker_index in collect_cp_instances
              from the _N suffix; assign index[i] = collected[i][:marker_index] directly
    result:   succeeded — cp_marker_0 → index 0, cp_marker_1 → index 1, etc.; exact match

principle: Nearest-neighbour matching between two independently-computed positions
           (edge hub vertex vs. component origin) is fragile — any geometry offset causes
           wrong assignments. When the N is encoded directly in the entity name/tag,
           read it there and assign it directly. No distance math needed.
           `collect_cp_instances` is the collection point; save marker_index there.
domain:    SU
