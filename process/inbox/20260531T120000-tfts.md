# TFTS — 2026-05-31T12:00:00Z

problem:   CP Calculate not detecting connection point coordinates from station templates

arc:
  - try:      stub_pair component containers — two parallel stub tracks tagged gw_stub_pair
              grouped as a component; midpoint of outer tips = CP center
    result:   worked but fragile — required stub_pair groups to exist in template model;
              traffic_circle7 had none; any template missing the container silently failed
    revealed: two different detection paths (stub_pair for stations, atan2 fallback for TC)
              meant CP index assignment diverged between template types

  - try:      cp component instances — a uniform "cp" component placed at each gate,
              containing 222mm tang edge + 1750mm rail edges; collect_cp_instances matched
              tag/name 'cp' or /cp(#\d+)?/
    result:   worked for models with cp component instances; station templates had
              cp_marker_# tagged entities which did not match the pattern
    revealed: the naming convention had evolved (cp → cp_marker_#) but the detection
              pattern was never updated; "none found" message for every station template

  - try:      expand collect_cp_instances pattern to also match tag/name /cp_marker(_\d+)?/
    result:   succeeded — all station templates detected 2 CPs; traffic_circle7 retained
              its placement_data path; CP Calculate output "N CPs from cp instances" for
              stations and confirmed correct hub/tangent/rail geometry
    revealed: cp_marker_# and the plain cp component have identical geometry
              (222mm tang edge, 1750mm rail edges, hub vertex = CP center).
              The tag name had incremented but the geometry contract had not changed.

  - try:      (this session) broader architectural change — replace stub_pair with
              gw_cp_out_# / gw_cp_in_# as universal CP interface in noelle.rb and
              noelle_features.json
    result:   broke CP Calculate for stations — noelle_features.json routing chains
              changed names that station model geometry did not match; reverted
    revealed: CP detection and BUILD/map.json routing are separate concerns.
              Do not change routing chain names in noelle_features.json unless
              the station template geometry is simultaneously renamed.

principle: cp_marker_# is the single universal CP detection mechanism. It is placed
           by the model author at each gate in every template (stations and traffic
           circles). It contains:
             - hub vertex      = CP centerline position
             - 222mm tang edge = outward tangent direction
             - 1750mm rail edges = ±1750mm to each guideway rail

           collect_cp_instances must match: tag or name == 'cp',
           /cp(#\d+)?/, or /cp_marker(_\d+)?/.

           Detection priority in recompute_all_cps:
             1. cp instances (including cp_marker_#) via detect_cps_from_top_level_cp
             2. gw_stub_pair tags via detect_cps_from_stub_pair_tags
             3. placement_data pair_stubs (traffic_circle7 only — no cp_marker yet)

           CP detection and map.json routing are SEPARATE. Renaming segment tags
           in noelle_features.json requires simultaneous rename in the template SKP.
           Never change one without the other.

           This principle has been relearned incrementally across multiple sessions.
           The cp_marker_# geometry contract (222mm + 1750mm) is now the authoritative
           definition. Do not introduce a third CP mechanism.

domain:    SU
