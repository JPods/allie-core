# TFTS — 2026-06-21T21:30:00Z

problem:   v2 station tests (shuffle, departure, arrival) broken — old Sally/JPodGuideway removed, console deeply wired to archived code, pods overrunning each other, Sally's arrays not synchronized

arc:
  - try:      Load old Sally from codearchive
    result:   failed — 3120-line module with deep dependencies on other archived modules
    revealed: Old code can't be patched; needs clean rewrite

  - try:      Write Sally compat layer (230 lines) delegating to SallyV2
    result:   succeeded — slot_positions, place_vehicles, sequencer queries all work
    revealed: The API surface is smaller than the old code suggests; most methods are internal

  - try:      Write station_tests.rb — shuffle, departure, arrival using v2 modules
    result:   partially succeeded — pods placed and animated but multiple bugs
    revealed: Six bugs found and fixed through iterative testing:

  - try:      Fix transit_test CW ring arcs
    result:   CCW correction in Compute + circle architecture (entries/exits/circle array)
    revealed: Ring is ONE thing — define once, traverse at runtime. 16 pass_chains replaced by circle + entries + exits

  - try:      Fix shuffle return parking at wrong slot
    result:   pod_starts_loop (not pod_departs) + gw_platform appended for Sally clip
    revealed: Trip always includes gw_platform — Sally clips to assigned slot. Looping pod stays in pods[] so Sally knows it's coming back.

  - try:      Fix departure pods overrunning each other
    result:   Sally enforces ps_max-only departure order, serial not parallel
    revealed: Sally must not allow pods to depart out of order. Only highest_occupied_slot may depart. Conveyor advances after each departure.

  - try:      Fix arrival pods stacking at end of gw_platform
    result:   Sally clips gw_platform maneuver to assigned slot position at maneuver transition
    revealed: Pod travels gw_platform but Sally intercepts the maneuver start, assigns slot, clips pts to slot world position. Pod stops at its slot, not end of track.

  - try:      Fix advance probes not confirmed as parked
    result:   station_test_phase='advance_park' triggers Sally confirmation on completion
    revealed: Every phase transition needs explicit Sally confirmation with verbose state logging

principle: Sally's ps[] and pods[] are the ONLY authority. No copies. All pods must be visible to spacing enforcement. Trip plans include gw_platform at both ends — Sally converts gw_platform into ps.N by clipping the maneuver. Departure is serial (ps_max only), arrival clips to assigned slot, advance confirms with Sally.

domain:    [SU]
