# TFTS — 2026-06-11T15:06:35Z

problem: JPods_station_parking platform_shuffle test: CCW mandate violated; pods advance backward through each other; test stalls with NORA_0001 not in active pods

arc:
  - try:      Observed pods advancing but log showed CCW violations; pod at ps6 advancing toward entry end
    result:   CCW violation confirmed
    revealed: _platform_pts_entry_first falls back to pod_pos heuristic when gw_platform_parking is absent.
              JPods_station_parking has no gw_platform_parking (uses gw_platform_in2 as final landing track).
              When pod is past the platform midpoint, d_rev < d_fwd → pts reversed → advance goes toward entry
              end → CCW violation.

  - try:      Replace pod_pos fallback with Sally ps1 slot_position anchor in _platform_pts_entry_first
    result:   ps1 is always at the entry end (dist_mm=1250 from entry). Sally.init_from_model uses the same
              raw_pts direction (no gw_platform_parking → no reversal). Anchoring on ps1 guarantees
              advance_pod_slot uses the identical orientation frame that Sally used to compute @@slot_positions.
    revealed: This is the correct general fix for any template without gw_platform_parking.
              Pod position as heuristic is inherently unreliable for pods past the midpoint.

  - try:      Change _advance_platform_queues from spot-by-spot to direct-to-final-slot
    result:   Sort occupied pods descending by current slot. Assign targets: rank 0 (highest) → cap,
              rank 1 → cap-1, etc. Break after first advance per tick (1s stagger).
              With cap=9 and 2 pods: tick1 → highest pod to ps9, tick2 → lowest pod to ps8.
              Highest slot advances first, clearing the track for pods behind it.
    revealed: Spot-by-spot was correct but slow (O(n²) ticks for n pods). Direct-to-final is O(n) ticks.
              The 1s stagger (break after first) prevents personal space violations because the leading
              pod is 1s × speed ahead when the trailing pod starts moving.

  - try:      Release stale Sally slot when advance_pod_slot finds pod not in @@pods
    result:   Stops repeated 'NORA_0001 not in active pods' spam and prevents Sally from blocking
              ps9 with a stale entry after the pod has been erased.
    revealed: When originating chain dispatches, Sally.release_slot is called (line 3129). But if the
              pod was removed from @@pods by some other path (compact_parked fallback, test cleanup edge case),
              Sally's registry can still hold the old slot. advance_pod_slot is the right place to clean
              this up defensively — it knows both the pod and the sid.

  - try:      compact_platform_idle model.entities nil guard
    result:   Matches the nil guard added to confirm_slots in prior session. Prevents crash when
              model is in transition state during animation reload.

principle: |
  The platform pts orientation must match Sally.init_from_model's frame — always.
  Rule: when gw_platform_parking is absent, both _platform_pts_entry_first AND init_from_model
  use raw pts direction (no reversal). The ps1 slot_position (computed by Sally on that same
  raw direction) is the correct anchor for advance_pod_slot orientation. Pod position is
  unreliable as an orientation anchor once the pod has advanced past the midpoint.

  For direct-to-final compaction: highest-slot-first ensures the pod closest to the exit
  departs first, clearing the track. One advance per tick (1s) provides natural stagger
  without explicit inter-pod coordination.

domain: SU
