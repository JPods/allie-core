# TFTS — 2026-06-19T15:00:00Z

problem:   station_parking animation tracks splattered — 15 successor junctions disconnected
fault_ref: (observed directly on Show Track after Compute)
domain:    SU

arc:
  - try:     run Compute on station_parking.skp, review Show Track
    result:  tracks splattered across model
    revealed: Compute did not cause the splatter — geometry was already broken in git (3863b7d)
              before today's session. 15 gaps in successor chain.

  - try:     gap analysis — compare LAST of each track to FIRST of each successor
    result:  identified 4 categories: wrong-location uturns (A), reversed leads (B),
             wrong endpoints on cp_in tracks (C), 500mm centerline gaps (D)
    revealed: Group D gaps are NOT geometry errors — they are systematic edge-extraction
              artifacts. Edge-based extraction anchors endpoints to the guideway rail,
              250mm from centerline. Opposite track anchors to the other rail: +250 + 250
              = 500mm apparent gap. True centerline = midpoint of the two endpoints.

  - try:     Python script — reverse 2 cp_lead tracks, regenerate uturns at ring ends,
             fix cp_in endpoints, snap all 4 Bezier junctions to centerline midpoint
    result:  21 OK, 0 GAPS. Committed b2a842b.
    revealed: all gaps resolved in one pass

principle: Edge-extraction produces rail-edge coordinates, not centerline coordinates.
           Any track authored from SketchUp edges will have its endpoints 250mm off
           centerline. When two such tracks meet, the gap = 500mm. Fix by taking the
           midpoint of the two mismatched endpoints — that IS the centerline.
           This is the same root as Axiom 10/19: math beats edges as authoritative reference.
           Never use raw edge coordinates as junction anchors.

domain:    SU
