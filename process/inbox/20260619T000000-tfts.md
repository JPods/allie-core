# TFTS — 2026-06-19T00:00:00Z

problem:   station_thru_dip cp arm endpoints drop ~0.2m in Z after Compute
fault_ref: (observed after station_parking PASS; reported by Bill: "After Compute the ends of gw_cp_in/out_... drop .2m I do not know what is this. It is some stale artifact.")
domain:    SU

arc:
  - try:     identify why gw_cp_in_0.LAST was at Z=9777.65 instead of Z=9871.4 (uturn Z)
    result:  succeeded (diagnosis only)
    revealed: Two stacked bugs in the post-passes:
              1. Priority 0.1 (arm rebuild) used a single flat `z` for both arm endpoints,
                 derived from stale track FIRST point. For flat stations (station_parking)
                 this was invisible — all Z values are the same. For 3D stations (station_thru_dip)
                 the outer cp tip is at Z=9683.9 and the uturn tip is at Z=9871.4 — flattening
                 destroyed the slope and gave wrong Z at both ends.
              2. Priority 0.15 (lead rebuild) took Z from lead_pts[0].at(2) — the stale
                 jpods_path attr Z from ene_railroad era. Applied same flat Z to all 4
                 interpolation points. Stale Z=9777.65 came from a prior bbox extraction
                 that averaged the arm tip Z values (9683.9+9871.4)/2 = 9777.65.

  - try:     fix Priority 0.1 — use fp[2] and lp[2] directly from cp_marker_index tips
             and uturn_pts; fix Priority 0.15 — use target[2] for uturn_end and interpolate
             Z linearly across the 4 lead points; exclude gw_cp_out_N from Priority 0.5
    result:  succeeded — committed 4430304
    revealed: cp_marker_index tips carry correct 3D Z from SketchUp entity transformation.
              The tips were always correct; they were being ignored in favor of a stale Z.

principle: Any post-pass that rebuilds arm or lead track endpoints must use the 3D
           coordinates directly from the authoritative source (cp_marker_index tips,
           uturn_pts). Never derive a shared `z` from existing track data and apply it
           flat to both endpoints — flat-Z logic is invisible on flat-ring stations
           (station_parking) and silently wrong on 3D stations (station_thru_dip).
           When Z varies across an arm or lead track, interpolate linearly from fp to lp.
           The stale jpods_path attr Z is always suspect — treat it as contaminated
           and let hub math (cp_marker_index, uturn_pts) be authoritative.

           The 9777.65 value is the mathematical midpoint of 9683.9 and 9871.4 —
           exactly what a bbox centroid averaging the two arm-tip Z values produces.
           This is a bbox artifact, not a model defect.

domain:    SU
