# TFTS — 2026-06-17T13:42:55Z

problem:   Vehicle animation in 2_thru_dip network model stuck in dwelling — pods never depart
fault_ref: (console log from prior session showing dwelling + formation=unknown RED FLAG)

arc:
  - try:      Assumed BFS failure due to empty successors on seg_ connections in model_json_to_map_compat
    result:   Partial — but proximity (Pass 2) was always the fallback; the deeper issue was formation=unknown
    revealed: Two independent failures: (1) BFS topology gap and (2) formation ID missing so _load_formation_lines returned {}

  - try:      Fixed model_json_to_map_compat to use routing_graph for seg_ successors (rg[seg_id] || [])
              Fixed load_model_json to try .network.json first, fall back to .model.json
              Fixed _build_route_json to derive formation ID from definition name when model_id attribute absent
              Fixed jpod_path_json.rb both scanning sites — same definition-name fallback stripping 'JPods Formation: ' prefix
    result:   succeeded (committed 5be8353)
    revealed: Stations placed before model_id attribute was added to placement code — all older models have this gap

principle: |
  Station instances in network models placed before model_id attribute was introduced
  lack the JPods.model_id attribute. ALL callers that read model_id must have a fallback:
  instance.definition.name.sub(/\AJPods Formation:\s*/i, '').strip
  This applies to: _build_route_json (animator), jpod_path_json.rb (two sites).
  
  seg_ connections in model_json_to_map_compat must get successors from routing_graph
  (rg[seg_id]) not hardcoded []. routing_graph in network.json already has correct
  seg_→station_track entries. BFS Pass 1 wires these; proximity Pass 2 is only backup.
  
  load_model_json must try .network.json first — Build now writes this name; .model.json
  is the legacy name and should remain as fallback for older models only.

domain: SU
