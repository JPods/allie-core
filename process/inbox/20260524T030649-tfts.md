# TFTS — 2026-05-24T03:06:49Z

problem:   Trip UI needs HTML↔Ruby communication but WEBrick is not available in SketchUp
fault_ref: (no FAULT file — issue discovered during implementation, not a system-reported fault)
domain:    SU

arc:
  - try:      Write WEBrick HTTP endpoints (/trip/stations, /trip/book, /trip/status, /trip/enter);
              bind to 0.0.0.0; mount static file handler for index.html
    result:   JPODS_WEBRICK_AVAILABLE = false; @server = nil; http://localhost:5051 unreachable
    revealed: WEBrick is not part of SketchUp's Ruby runtime. Standard library assumptions
              do not hold in the plugin context.

  - try:      Keep WEBrick code, add UI::HtmlDialog with add_action_callback as active path;
              dual-path JS (IN_SKETCHUP branch uses sketchup.xxx(), else fetch())
    result:   Succeeded — HtmlDialog opens, callbacks fire, all four endpoints respond correctly

principle: SketchUp's Ruby runtime is not standard Ruby. Never assume standard library
           availability. UI::HtmlDialog + add_action_callback is the correct transport
           for HTML↔Ruby communication in the plugin context. WEBrick, open-uri,
           net/http — all require explicit availability checks before use.

bonus_principle: The dual-path JS pattern (IN_SKETCHUP = typeof sketchup !== 'undefined')
                 is reusable. It lets the same HTML file run in SketchUp dialog AND in
                 a browser for development/testing. Write all future trip/console HTML
                 with this pattern.
