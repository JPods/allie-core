# TFTS — 2026-06-05T20:00:00Z

problem:   Intra-station arc segments (gw_uturn_*, gw_c_N_N) animate as straight chords
fault_ref: 20260605T114711 (trajectory log: gw_c_3_3 X goes -20497→-11556 at constant Y — line, not arc)

arc:
  - try:      Store full pts_mm in followme.json track records and chain in noelle.rb generate_map_json
    result:   Partial — inter-station seg_* now carry full Bezier pts. Intra-station arc pts
              in map.json/path.json still only 2 points (extracted.json from lines.json has
              only start/end per arc segment).
    revealed: The JSON pipeline loses arc fidelity at the Extract Template step.
              lines.json stores arc endpoints, not intermediate arc vertices.

  - try:      Read actual SketchUp edge geometry via followme_trace_polyline_from_edges
              called on station component sub-entities at animation start.
              followme_collect_edges_recursive already walks edge adjacency graphs.
              ArcCurve arcs in SketchUp = ~24 connected short edges → 25 pts per arc.
    result:   Succeeded — build_edge_lookup_from_model merges arc-faithful pts into
              the animation lookup. Edge entry wins when pts.size > JSON entry pts.size.
              gw_c_3_3 and gw_uturn_* will now trace actual arc curves.

principle: SketchUp edge geometry IS the authoritative arc path. The FollowMe tool itself
           follows these edges. For animation, read the same edges — do not trust JSON
           files that may record only start/end. followme_trace_polyline_from_edges
           is already in the codebase for the FollowMe overlay; the missing step was
           calling it at animation start and merging edge-sourced pts into the lookup.
           This is the ene_railroad approach: geometry → animation, not JSON → animation.

domain:    SU
