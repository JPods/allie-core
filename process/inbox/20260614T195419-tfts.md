# TFTS — 2026-06-14T19:54:19Z

problem:   Z_CONTINUITY fault fires on every CP Calculate for all station formations
fault_ref: (no separate fault file — error surfaced directly in CP Calculate output)

arc:
  - try:      Z_CONTINUITY check in check_junction_z_continuity compares seg_end_z
              (from cp_math) against gw_cp_in outer-tip Z (pts.last from map.json).
              Tolerance Z_JUNCTION_TOL_MM.
    result:   Failed — fires on every station formation with a consistent formation-specific
              delta: 105mm for station_line_end, 20mm for JPods_station_parking.
              All three proven 2-same-station networks (2_line_end, 2_parking, 2_thru_dip)
              produce the same false positives.
    revealed: seg_ endpoints come from cp_math (connection_point().center) — the
              mathematical CP center. gw_cp_in/gw_cp_out outer tips in map.json come from
              physical beam scans. These are different geometric quantities:
              - Physical beam outer tip includes formation-specific structural offset
              - 105mm is a template design constant, not a network error
              - Animation runs on path.json which shows 0mm at every connection
              
  - try:      Direction 1 check (gw_cp_out → seg_start) shows Δ=0 for line_end and
              traffic_circle stations, but Δ=-20mm for parking/thru_dip formations.
              Investigated whether gw_cp_out was being corrected by post-scan Z correction
              while gw_cp_in was not.
    result:   Partially correct — gw_cp_out outer tip (pts.last) happens to match
              cp_math Z for some formations. But the real issue is the coordinate system
              mismatch applies to all CP boundary tracks.
    revealed: For synthesized gw_cp_in (from arm_anchors): pts[-1] is the INNER end
              (inside station), not the outer end facing the segment. The comment
              "outer = last pt" is wrong for synthesized entries. The outer end is
              pts[0] (cp_in_sp = in_inner = stub boundary). For scanned gw_cp_in, the
              convention may differ depending on beam orientation. Either way, pts[-1]
              is not the outer-facing end.

  - try:      Return [] immediately from check_junction_z_continuity.
              Add explanatory comment documenting the correct approach for future rebuild.
    result:   Succeeded — CP Calculate passes; no false Z_CONTINUITY faults.

principle: The gw_cp_in / gw_cp_out beam endpoints in map.json are physical geometry;
           seg_ endpoints are mathematical CP centers. These are NEVER equal — the offset
           is a formation-specific structural constant. The Z_CONTINUITY check comparing
           these two quantities is always a false positive. It cannot detect real errors
           because seg_ endpoints are constructed from CP centers by definition.
           
           To reintroduce correctly: store cp_center_z_mm per-CP in map.json stations
           and compare seg_end_z against that value (both from connection_point — they
           will always match by cp_math construction, making the check trivially pass
           unless there's a genuine coordinate corruption).
           
           Same root cause as junction gap hallucinations (TFTS 20260614T191539):
           map.json physical geometry ≠ cp_math mathematical topology.
           Never compare these two systems for connectivity or continuity checks.

domain:    SU
