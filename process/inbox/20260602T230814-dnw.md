# DNW — 2026-06-02T00:00:00Z

tried:    Diagnosed parking shuffle (ps3→ps1) without logs
result:   Could not isolate root cause — too many candidates:
          (a) pts_mm for gw_platform go EXIT→ENTRY (reversed), so slot 3 measured
              from exit end = physically near ENTRY end, looks like slot 1
          (b) compact_platform_idle uses Sally.capacity_for → if capacity=3, target=last(1)=[3]
              which is no-move; but if gw_platform pts reversed, pos is "wrong end"
          (c) second landing chain dispatch from Sally (already fixed — Sally.delete pod_states at :land)
revealed: Need actual coordinates from next run. PARK DISPATCH log now fires before
          parking Trip 2 with pod_pos, gw_pts[0], gw_pts[-1], reversed flag.
          Also need compact log to see what target slot it computes.
          Run again and post Ruby console log lines beginning with [Nora NORA_0001] PARK DISPATCH.
