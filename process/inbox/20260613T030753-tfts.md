# TFTS — 2026-06-13T03:07:53Z

problem:   Authority chain not enforced — Natalie released Nora before Sally confirmed parked
fault_ref: (previous session log — pods stacking at 0mm, PARK DISPATCH not firing)

arc:
  - try:      Add Sally two-array data model (vehicles FIFO + parking_slots) and vehicle_arrived/vehicle_parked hooks
    result:   Data model correct; visual behavior unchanged (pods still stacking)
    revealed: The start_dwell fallthrough ran for ALL cases including parking failures

  - try:      Add debug logging to Trip 2 else block to find why PARK DISPATCH never fired
    result:   Not yet run; superseded by architectural understanding
    revealed: Real issue was premature start_dwell and premature parked_station_id set

  - try:      Implement explicit authority chain with next in _park_psN and _advance_ps blocks
    result:   succeeded
    revealed: The original code called pod.start_dwell unconditionally after the if/elsif/else
              block — even for pods still traveling to their slot. The `next` in Trip 2 success
              path skipped it, but all failure paths (full platform, bad geometry, no track)
              fell through to start_dwell before Sally could confirm parking.

principle: Natalie must not release Nora (call start_dwell) until Sally.vehicle_parked fires.
           The authority chain is: Nora arrives → Sally assigns → Natalie dispatches park_man
           → Nora reports (_park_psN complete) → Sally confirms (vehicle_parked) → Natalie
           releases (start_dwell). All other paths (park failure, full, no track) must hold
           in @@dwelling with awaiting_slot — never call start_dwell without Sally confirmation.

domain:    SU
