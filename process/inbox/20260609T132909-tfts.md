# TFTS — 2026-06-09T13:29:09Z

problem:   seg_ tracks arrive at station CPs at ~6° angular error instead of perpendicular

arc:
  - try:      Traced the full CP pipeline (prepare_build_cp_cache → detect_cps_from_cp_markers
              → detect_cps_from_top_level_cp → store_connection_points → cached_connection_point
              → tangent_curve_pts / bezier_spline_pts). Verified transformation chain is correct.
    result:   succeeded in isolating root cause to cp.json INPUT DATA, not transformation code
    revealed: The bug is upstream of Build — it lives in the cp.json files

  - try:      Read cp.json for JPods_station_parking. Observed CP tangents [0.107, ±0.994, 0]
              instead of expected [0, ±1, 0]. Computed entity transformation from CP world
              positions (R(-90°) + translation). Verified: R(-90°)*[0.107,-0.994] = [-0.994,-0.107]
              → world arrival angle 6.1°. Confirmed against Trial6.path.json (all seg_ to
              station arrivals show same ~6° error; FROM traffic_circle are exact).
    result:   root cause confirmed: bounding box asymmetry in detect_cps_from_top_level_cp
    revealed: xy_tangent(defn.bounds.center, cp_hub_pos) is wrong when the formation bounding
              box center is not on the CP stub axis. For JPods_station_parking, parking bays
              extend far in +X, shifting bounds.center.x from ~272 inches while CPs are at
              x=428.49 — introducing 0.107 X contamination in the tangent.

  - try:      Fix cp.json tangents to exact cardinal directions; fix detect_cps_from_top_level_cp
              to use perpendicular-to-rail-span instead of xy_tangent(bounding_box_center).
    result:   succeeded — commit a938d2c
    revealed: The rail span IS the cross-track direction; its perpendicular IS the exact
              stub axis. formation_center is only needed for ±sign disambiguation, not for
              the angle — so bounding box asymmetry no longer matters.

principle: CP tangents must be derived from the CP gate geometry (perpendicular to rail span),
           NOT from the direction between the bounding box center and the hub. Bounding box
           center is unreliable for stations with asymmetric geometry. The rail span direction
           is always exactly cross-track; its perpendicular is always exactly the stub axis.

domain:    [SU]
