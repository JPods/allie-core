# TFTS — 2026-05-22T06:30:00Z

problem: Vehicle animation started from pts[0] of first maneuver instead of the
         vehicle's actual placed world position. On S001 departure, vehicle jumped
         from parking space 7 (x=-3737mm, placed position) to parking space 1
         (x=20128mm, gw_platform.pts[0]), then traversed the full platform before
         departing. Same problem on every loop re-dispatch.

fault_ref:

arc:
  - try: Implement _project_t_on_polyline and seed receive_maneuver from placed
         world position. Project entity origin onto first maneuver polyline using
         per-segment dot products.
    result: succeeded
    revealed: For S001 departure: vehicle at x=-3737 projects to t=1.0 of
              gw_platform [20128→-3737]. receive_maneuver sets @t=1.0, propose
              returns :arrived in first tick (zero remain), vehicle stays at -3737,
              immediately transitions to gw_platform_out1. No visual jump.
              Same for loop re-dispatch: entity.transformation.origin at trip_complete
              position maps to t=1.0, gw_platform traversal skipped.

principle: Animation must start from the vehicle's actual placed position, not
           from the mathematical start of the first polyline segment. Project the
           entity origin onto the first maneuver polyline at dispatch time and seed
           @t from that projection. This applies at initial dispatch (build_fleet)
           and at every loop re-dispatch (tick dwelling handler). Mid-trip maneuver
           dispatch needs no seed — the vehicle is already at the correct position
           from the previous maneuver end.

domain: SU

definitions_clarified:
  seed_pos: The entity transformation origin at dispatch time. Passed as keyword
            argument to receive_maneuver. Used only for the FIRST maneuver of a
            trip — not for mid-trip dispatches.

  _project_t_on_polyline: Iterates polyline segments, projects world_pt using
            dot product per segment, returns global t ∈ [0,1]. Avoids
            Vector3d * Float (illegal in SketchUp Ruby — no coerce method).
            Uses Math.sqrt and component arithmetic throughout.

  t=1.0 at dispatch: When projection gives t=1.0, propose immediately returns
            :arrived (remain=0). Vehicle stays at pts.last. Natalie dispatches
            the next maneuver in the same arrived-ids processing loop.
            The first maneuver appears in the log but is never visually traversed.
