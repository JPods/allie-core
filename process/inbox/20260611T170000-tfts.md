# TFTS — 2026-06-11T17:00:00Z

problem: JPods_station_parking platform_shuffle: hold_loop pod jumps from gw_cp_in_lead_1 to gw_platform when returning to park after intersection landing

arc:
  - try:      Ran platform_shuffle test. Observed pods advancing correctly to final slots.
              At the end, NORA_0003 (hold_loop pod) jumped visually from gw_cp_in_lead_1
              to gw_platform when returning to park.
    result:   Jump confirmed. Log showed:
              [Nora NORA_0003] maneuver: hold_loop[9/9]  start=(-23618,-4539)mm  end=(20096,-1039)mm
              [Sally] NORA_0003 → ps7
              [Nora NORA_0003] PARK DISPATCH ... pod_at=(-23618,-4539)mm
    revealed: park_man starts at (20096,-1039) = gw_platform entry. pod_at = (-23618,-4539) =
              start of hold_loop[9/9] (or end of the loop circuit). When receive_maneuver(park_man)
              fires with seed_pos=(-23618,-4539), the projection misses (distance >> 3m tolerance)
              → @t=0 → pod snaps to park_pts[0] = (20096,-1039). 44m visual jump.

  - try:      Traced root cause. The park dispatch (Trip-2 else branch) fires while
              @current_maneuver = hl_from+hl_loop (the initial loop circuit), not hold_loop[9/9].
              last_man[:pts].last = end of loop circuit ≈ (-23618,-4539). This is pod_at.
              The Sally intercept dispatches hold_loop[9/9] via receive_maneuver at line 2711.
              For reasons that weren't fully traced (receive_maneuver may have returned false,
              or the queue-empty block ran on the wrong tick), the Trip-2 block fires with
              stale last_man.
    result:   Root cause partially understood. Regardless of exact mechanism, the core issue
              is that park dispatch fires BEFORE the pod has physically arrived at gw_platform
              entry, so seed_pos is wrong.
    revealed: The fix must ensure that park_man fires ONLY after hold_loop[9/9] has completed
              and the pod IS at gw_platform entry. The queue mechanism (@@queues[nora_id]) is
              the correct tool — it plays maneuvers in sequence after the prior one completes.

  - try:      Pre-queue the park_man at hold_loop[9/9] dispatch time (Sally intercept).
              New method _enqueue_hold_loop_return_park:
                - Called immediately after pod.receive_maneuver(hold_loop_9_9_man)
                - Calls reserve_slot, builds park_pts, queues park_man in @@queues[nora_id]
                - Also sets station_test_phase = 'hold_loop_return' for template test cleanup
              When hold_loop[9/9] completes:
                - @@queues[nora_id] = [park_man] → NOT empty → queue path fires
                - prev_end = hold_loop_9_9[:pts].last = (20096,-1039) = gw_platform entry
                - pod.receive_maneuver(park_man, seed_pos: (20096,-1039)) → @t=0, no jump
                - CP gap = 0mm (park_pts.first == gw_platform entry)
    result:   Correct approach. Pod arrives at gw_platform entry via hold_loop[9/9], park_man
              immediately follows from that exact position. No teleport.
    revealed: Also needed: _advance_platform_queues must NOT update registry when advance_pod_slot
              returns false (pod traveling). Previously registry was updated before dispatch,
              so a pre-reserved slot could be moved in the registry (ps7→ps9) while pod was
              still airborne on the landing chain.

  - try:      Fix _advance_platform_queues to move registry update AFTER dispatch success.
              advance_pod_slot returns true on success, false on any skip/error.
              _advance_platform_queues: `next unless dispatched` before updating registry.
    result:   Registry stays correct while pod is traveling landing chain. No spurious
              ps7→ps9 advancement.
    revealed: Combined with pre-queue fix, the full return flow is clean:
              1. Sally intercept dispatches hold_loop[9/9] + queues park_man at ps7
              2. Pod travels hold_loop[9/9] (12s, 9 tracks)
              3. _advance_platform_queues sees ps7 occupied but advance returns false → skip
              4. hold_loop[9/9] completes → queue [park_man] → park fires from (20096,-1039)
              5. Pod travels gw_platform entry → ps7 → arrives → hold_loop_parked cleanup

principle: |
  When a pod transitions from hold_loop to platform via intersection landing, the park
  maneuver must be queued BEFORE the landing chain starts, not dispatched after it
  completes. The queue mechanism guarantees correct sequencing: park_man fires with
  prev_end = landing_chain.pts.last (gw_platform entry), producing continuous animation.
  Dispatching park_man via Trip-2 (else branch) is fragile because Trip-2 fires when
  the queue is empty AND a maneuver completes — if the wrong maneuver triggers it, the
  seed_pos is wrong. The queue-based approach sidesteps this entirely.

  Secondary: Sally's registry update in _advance_platform_queues must be atomic with
  dispatch success. Never update the registry speculatively before confirming the pod
  received the maneuver.

domain: SU
