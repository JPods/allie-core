# TFTS — 2026-06-21T18:00:00Z
problem:   v2 modules built in sequence had cross-module inconsistencies — Maneuver format mismatch, Sally not notified of pod state changes, no capacity checking, no parking rebalance on graceful stop
fault_ref: (discovered during forward/backward review)
arc:
  - try:      Build modules in dependency order (natalie → nora → noelle → animation) — each module correct in isolation
    result:   failed — modules didn't integrate cleanly. Nora expected Hash, Natalie returned Struct. Animation didn't call Sally. Camera used old module refs. EZone expected nora_id, Nora used pod_id.
    revealed: Building in sequence creates interfaces that don't match. Each module is correct internally but the seams are wrong. No single module is at fault — the integration is.
  - try:      Forward scrub (build order) then backward scrub (consumer → provider). Forward: fix interfaces, add aliases, update refs. Backward: check what each consumer needs from each provider, add missing calls and checks.
    result:   succeeded — all cross-module issues resolved. Sally notified on state changes. Natalie checks capacity. EZone compat via alias. Camera and Animate wired to v2.
principle: Build code as seems best, then run forward AND backward reviews until no defects found. Forward catches interface mismatches. Backward catches missing integration. Neither alone is sufficient. This is now a design principle.
domain:    [SU] [Cross-domain]
