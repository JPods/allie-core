# TFTS — 2026-06-10T05:06:03Z

problem:   Three Noras on gw_platform travel backward; personal space violations as all pods converge toward entry end instead of exit
fault_ref: (observed during Shuffle Test first run, 2026-06-09 session 3)

arc:
  - try:      Placed V3 at 2.5×slot_spacing (farthest from pts[0]), V2 at 1.5×, V1 at 0.5×.
              pts[0] = [-3768.7, -1039.3] (exit end); pts[-1] = [20095.9, -1039.3] (entry end).
    result:   failed — pods traveled from highest slots back toward slot 1, moving toward entry (wrong direction)
    revealed: clip_start in build_maneuver_from_tracks clips from pod position toward pts[-1].
              If pts[-1] is the entry end, clip_start drives pods toward entry — opposite of departure direction.

  - try:      Swapped gw_platform pts in geometry.json so pts[0] = entry end [20095.9] and
              pts[-1] = exit end [-3768.7]. Also reversed slot placement: V3 (3 loops) at
              0.5×spacing from entry (slot 1, deepest), V1 (instant-promote) at 2.5×spacing
              from entry (slot 3, front/exit end).
    result:   succeeded
    revealed: compact_platform_idle targets available_slots.last(n) = highest slot numbers.
              Highest slots must be near the exit (front of queue), not near the entry.
              With entry→exit pts, 0.5×spacing = deepest slot 1, 2.5×spacing = front slot 3 = near exit.

principle: Platform departure tracks (parking_chain tracks, from_platform first track) must be stored
           entry→exit. pts[0] = entry end (where arrivals land), pts[-1] = exit end (where departures
           leave toward hold loop). clip_start NEVER reverses — it clips from current pod position
           toward pts[-1]. Wrong direction means pods travel toward entry during departure, violating
           personal space of parked pods behind them. Slot numbers run entry→exit: slot 1 is deepest
           (near entry), highest slot number is front-of-queue (near exit).

domain: SU
