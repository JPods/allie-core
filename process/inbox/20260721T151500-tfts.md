# TFTS — 2026-07-21T15:15:00Z
problem:   Deploy WC3 + MeshMobility on GEEKOM IT15 with Ubuntu
fault_ref: none (greenfield install)
arc:
  - try:      dd command to flash Ubuntu ISO to USB
    result:   failed — sudo requires interactive TTY, no progress feedback
    revealed: automated environments can't run sudo interactively; need GUI tool
  - try:      balenaEtcher to flash ISO to 8GB SD card
    result:   succeeded — visual progress, reliable
    revealed: balenaEtcher is the proven path for Linux USB installers
  - try:      Tap DELETE/F7/F12 during boot for BIOS
    result:   unreliable — Fast Boot window too short
    revealed: Windows Shift+Restart → Use a device is deterministic
  - try:      Install Ubuntu, log in
    result:   failed — forgot credentials
    revealed: Write credentials down immediately during install
  - try:      Reinstall Ubuntu with memorable credentials
    result:   succeeded — fast (~15 min with existing USB)
    revealed: Reinstall is cheap; don't waste time guessing passwords
  - try:      rsync WC3 to server
    result:   partially failed — first rsync missed subdirectory files
    revealed: rsync without .git can miss nested Python files; run twice or verify
  - try:      pip install requirements.txt on Python 3.14
    result:   failed — pydantic_core 2.23.4 won't build (PyO3 max 3.13)
    revealed: Install latest pydantic first, then requirements minus pinned versions
  - try:      Start MeshMobility
    result:   failed — ModuleNotFoundError for CrashHarvester
    revealed: Sibling packages need symlinks; directory structure matters
  - try:      Fix symlink, restart
    result:   succeeded — MeshMobility serving on :5050
    revealed: Complete deployment requires: code + dependencies + sibling packages + correct working directory
principle: A production deploy is a chain of 20+ steps where any one failure blocks all downstream. Log each failure immediately (DNW) — the pattern of failures IS the specification for the one-touch installer.
domain:    [SYS]
