# TF — 2026-06-09T_TS_
summary: CP world Z is already at guideway height — adding BEAM_DEPTH raised seg_ tracks above gw_ junction
code:    su_jpods/jpod_network.rb — Plan#compute_anchor_zs
apply_to: any seg_ track that connects to a gw_ track at a station CP;
         anchor_zs = cp[:center].z (world) is the correct value;
         cp.json center Z is stored in definition-local with Z at guideway level,
         so after entity.transformation the world Z is exactly the guideway connection height.
         Adding BEAM_DEPTH shifted seg_ endpoint above gw_ endpoint.
