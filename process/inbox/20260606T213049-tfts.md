# TFTS — 2026-06-06T21:30:49Z
problem:   gw_uturn_1 animation was chaotic — Y oscillated past ±1500mm to ±2000mm,
           Z jumped between two values, pod appeared to loop the full outer rail circle
           rather than traverse the centerline semicircle. gw_cp_out_lead_1 and
           gw_cp_in_lead_1 traversed instantaneously with wrong endpoints (~300mm vertical
           chord at CP stub instead of 2500mm horizontal track).

arc:
  - try:      Inspect trajectory.jsonl for gw_uturn_1 tick positions
    result:   Confirmed Y going to +1932 (past +1500 endpoint), Z oscillating
              between 7706.2 and 8206.2 — clearly not the centerline semicircle
    revealed: Two Z levels present: 7706.2 (outer rail Z) and 8206.2 (beam top Z).
              Something was reading the FollowMe beam solid, not the centerline arc.

  - try:      Suspect anim-coords.jsonl as diagnostic source — it logs the full
              pts array for every lookup entry at animation start
    result:   anim-coords showed:
              s002.gw_uturn_1 — source=edge_attr, pts_count=56, len=25172.5mm
              s002.gw_cp_out_lead_1 — source="", pts_count=2, len=300.5mm,
                first=[-120681.6, 1500.0, 7186.2] last=[-120681.6, 1666.7, 7436.2]
    revealed: (1) gw_uturn_1 lookup entry was a 56-pt beam solid trace, not the
              13-pt centerline arc from path.json.
              (2) cp_lead_1 had the map.json 2-pt stub chord (source=""), not
              path.json's correct 2500mm horizontal track (source="extracted").

  - try:      Find where edge_lookup overrides the path.json entry in the
              three-source lookup assembly (jpod_vehicle_anim.rb ~1822)
    result:   Found two merge conditions that each fired incorrectly:
              (1) `edge_entry[:pts].size > json_entry[:pts].size` — 56 > 13 → TRUE
                  Replaced the path.json 13-pt centerline arc with the 56-pt beam solid
              (2) `json_entry[:source].to_s == 'extracted' && edge_entry[:source] == :edge_attr` → TRUE
                  Replaced path.json's correct 2-pt 2500mm cp_lead entry with edge_attr's
                  wrong 2-pt 300mm CP stub chord
    revealed: Both conditions were designed for a case where edge_lookup gives BETTER
              geometry than path.json. The logic is inverted:
              — build_edge_lookup_from_model reads FollowMe beam solid edges (all faces,
                outer rail full circle, inner rail, Z-jumping top/bottom face edges)
              — path.json stores centerline arcs extracted from ArcCurve entities during
                Build — this IS the correct geometry
              — More pts in edge_lookup means MORE face edges read, not better shape

  - try:      Fix: change the arc-upgrade condition to only replace when json_entry
              has ≤2 pts (chord). Remove the extracted→edge_attr rule entirely.
              New condition: `edge_entry[:pts].size > json_entry[:pts].size && json_entry[:pts].size <= 2`
    result:   Succeeded.
              — gw_uturn_1: 13 > 2 is FALSE → path.json 13-pt arc wins
              — cp_lead: extracted→edge_attr rule removed → path.json 2-pt 2500mm wins
              — n_extracted_replaced variable removed (no longer incremented)

principle: The edge_lookup override exists to upgrade chords (2-pt start/end) to real
           arcs when path.json only recorded endpoints. The condition MUST guard that
           the json_entry is actually a chord (≤2 pts). Without this guard, any
           edge_lookup entry with more raw edges than path.json beats the correct
           centerline — and build_edge_lookup_from_model always wins that race because
           FollowMe beam solids produce 50+ face edges vs. the 12-24 centerline arc pts
           path.json records.

           The second rule (extracted→edge_attr) had inverted semantics. "extracted"
           in path.json means centerline extracted from ArcCurve geometry during Build —
           this is the authoritative shape. ":edge_attr" in edge_lookup means read from
           the live FollowMe beam solid — this is more edges but wrong geometry.
           The rule believed "stored attribute beats extracted" when the opposite is true.

           Diagnostic shortcut: anim-coords.jsonl is written at every animation start.
           Check `source` field first. If a track shows source=edge_attr or source=""
           but you expect it to show source=extracted (path.json), the merge is wrong —
           not the upstream geometry.

           When a path is "chaotic" (Y past limits, Z oscillating, reversal):
           1. Read anim-coords.jsonl immediately — source + pts_count + full pts array
           2. If source=edge_attr and pts_count > 20: beam solid trace, not centerline
           3. Fix is in the edge_lookup merge conditions in jpod_vehicle_anim.rb

domain:    SU
