# TFTS — 2026-06-20T00:00:00Z
problem:   Show Track draws straight chord between seg_ CP endpoints instead of following built bezier
fault_ref: (no fault file — identified from observation during 2_parking rebuild)
arc:
  - try:    Read Show Track source — found it reads designer.connections[].pts from network.json
    result:  failed to fix — those arrays have only 2 pts (CP-math start/end)
    revealed: network.json connections_out was built from map.json which stores only startPoint/endPoint
              for seg_ connections, never the bezier. show_track_overlay drew the 2-pt chord.
  - try:    Fix noelle.rb generate_network_json to scan model entities for beam_path attribute
            and write full bezier pts into network.json during Build
    result:  correct direction but requires rebuild — doesn't fix existing built models
    revealed: beam_path is already stored on every guideway group by JPodGuideway.build (line 716).
              The data exists in the model; it just wasn't being read by Show Track.
  - try:    Fix show_track_overlay in jpod_animator.rb to read beam_path from model entities
            directly (before falling back to network.json pts). No rebuild required.
    result:  succeeded — Show Track now follows built bezier curves immediately on plugin reload
principle: beam_path attribute on guideway groups is the authoritative bezier geometry.
           Any display or planning function that needs seg_ pts should read beam_path
           from the model entity first, not from derived JSON files (map.json, network.json).
           JSON files are computed artifacts — the model entity is the source.
domain:    SU
