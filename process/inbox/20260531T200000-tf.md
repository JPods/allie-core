# TF — 2026-05-31T20:00:00Z

summary: lines.json is the same schema for SketchUp design and physical networks.
         gw_* track names, landing_chains, exit_chains, discovered_chains,
         length_mm, and switch settings are not SketchUp-only concepts.
         They are the exact data Nora follows on the scale model,
         Natalie uses to plan routes, and Sally (station chip) reports
         before every pod movement. A student authoring chains in SketchUp
         is authoring the physical control system.

         discovered_chains gives the model designer a topology inventory
         (CCW backward from gw_cp_out_0, alphabetical). From that inventory
         the designer authors landing_chains and exit_chains — one file,
         everything visible. Noelle expands each chain inline (length_mm,
         switches, track_lengths) on every Build. chains_header.approved_by
         is the gate: Noelle refuses unsigned templates.

         Sequence panel (Review / Manual / Detail / Discovered / Discover / Finder)
         is the designer's UI into this system directly from the console.

code:    su_jpods/jpod_sally.rb — draft_chains() topology mapping
         su_jpods/noelle.rb — generate_feature_json chains_approved guard + inline expansion
         su_jpods/templates/track_formations/station_thru_dip/lines.json — authoritative example
         su_jpods/dialogs/console.html — Sequence panel
         su_jpods/jpod_console.rb — console tasks + callbacks
