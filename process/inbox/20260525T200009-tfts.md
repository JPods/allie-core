# TFTS — 2026-05-25T20:00:09Z

problem:   topology override pass in noelle.rb used wrong key to find line.json — "model#2" instead of "JPods_station_parking"
fault_ref: (identified from all-empty successors/predecessors in CA_Gilroy S048-S051 map.json)

arc:
  - try:      stations_out[sid]['template'] for line.json folder lookup
    result:   failed — 'template' field is set from nf_templates key match against def_name.
              For duplicated SketchUp components (def_name = "model#2"), no nf_templates key
              matches (none is a substring of "model#2"), so template falls back to "model#2".
              File.exist?("templates/track_formations/model#2/line.json") = false → no override.
    revealed: SketchUp auto-names duplicates with "#N" suffix. The component definition name
              is NOT stable — it changes based on how many duplicates exist. The 'formation_id'
              attribute (written at placement via StructurePlacer) IS stable.

  - try:      Prefer formation_id attribute over template field; also store formation_id in
              stations_out hash; check File.exist? for both keys to find line.json
    result:   succeeded — formation_id "JPods_station_parking" resolves correctly regardless
              of whether the definition is named "model", "model#2", "model#3", etc.

principle: Component definition names in SketchUp are NOT stable identifiers — they carry
           #N suffixes when duplicated. Always prefer explicit attributes (formation_id set
           at placement) over derived names. Apply this rule to ANY lookup that uses a
           component definition name to find an external file or resource.

domain: SU
