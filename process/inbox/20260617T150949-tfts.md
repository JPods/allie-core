# TFTS — 2026-06-17T15:09:49Z

problem:   Vehicles animate correctly on seg_ (inter-station) but not gw_ (intra-station)
fault_ref: User: "vehicles track animation correctly on seg_# but not gw_#"

arc:
  - try:      Assumed lookup key mismatch (uppercase vs lowercase station ID in routing_graph vs map_lookup)
    result:   Not the bug — model_json_to_map_compat lowercases sid; route_object_to_flat_segments
              also lowercases. Keys match on both sides.
    revealed: Path.json wins over network.json for gw_ tracks (animation init line 2522:
              map_lookup.merge(lookup) — path.json entries override). The question becomes:
              what does path.json have for gw_ tracks, and is it correct?

  - try:      Traced PathJSON.export coordinate transform for gw_ tracks:
              export() calls load_extracted_formation_xf(model_id) which reads from extracted.json.
              station_thru_dip has NO extracted.json — so inner_xf_pre = nil.
              eff_world_xf = world_xf only (no inner_xf).
              Pts from lines.computed.json transformed by world_xf only → WRONG world position.
              network.json (my geometry augmentation) uses inst.transformation * inner_xf → CORRECT.
    result:   Root cause confirmed. path.json gw_ pts are at wrong position;
              path.json wins in animation init; vehicles follow wrong gw_ path.
    revealed: PathJSON.export was being called AT ANIMATION START, regenerating path.json
              with the wrong transform every time animation runs.

  - try:      (1) Remove PathJSON.export call at animation start.
              (2) Reverse priority: network.json wins for gw_ tracks, path.json wins for seg_.
    result:   succeeded (committed 2ee3133)
    revealed: generate_network_json was also re-reading map.json from disk after generate_map_json
              wrote it — unnecessary disk round-trip. Fixed: map_result[:data] now passes
              in-memory. map.json file still written for external consumers.

principle: |
  Two coordinate systems in path.json:
  - seg_ inter-station: beam_path attributes already in world space — transform is correct
  - gw_ intra-station: extracted from lines.computed.json (formation-component-local) via
    eff_world_xf = world_xf * inner_xf_pre where inner_xf_pre comes from extracted.json.
    If extracted.json doesn't exist, inner_xf_pre = nil, eff_world_xf = world_xf ONLY.
    Templates without extracted.json (station_thru_dip, station_line_end, etc.) get wrong
    world pts for gw_ tracks in path.json.

  network.json geometry augmentation (noelle.rb, commit 5535da2) uses inst_xf * inner_xf
  from LIVE MODEL SCAN — always correct, no extracted.json dependency.

  Authority split: network.json wins for gw_ tracks; path.json wins for seg_ tracks.
  path.json is only needed for: (1) chain-defect gate, (2) seg_ bezier pts (dense arc).

  Never regenerate path.json at animation start — it applies wrong transform for gw_.
  path.json is generated at Build time (generate_feature_json); that's sufficient.

domain: SU
