# TFTS — 2026-05-24T22:14:23Z

problem:   CP detection produced wrong center (1.75m off or 111-296mm off) causing raggedy gate connections
fault_ref: (ongoing from 2026-05-23 session)
arc:
  - try:      scan_stub_pair_tips nearest-tip snap — snap to closest rail vertex
    result:   failed — snapped to one rail tip (±1.75m off center) instead of track centerline
    revealed: scan_stub_pair returns individual rail tips, not the centerline between them

  - try:      scan_stub_pair cluster centroid — average both rail tips at same along-track position
    result:   partially correct for traffic_circle but ~157mm off for other stations
    revealed: gw_stub_pair outer face centroid ≠ cp component hub vertex position

  - try:      outward bounding-box corner projection — project 8 bbox corners onto outward tangent, take max
    result:   failed — gave inconsistent outward_ext (111mm vs 296mm across station types)
    revealed: cp component bounding box includes decorative indicator geometry beyond the gate face

  - try:      search cp component for a single 3500mm span edge
    result:   failed — actual geometry has two 1750mm edges (one per rail), not one 3500mm span
    revealed: cp component has edges [177mm, 222mm, 1750mm, 1750mm]; the hub vertex is where
              the 222mm tangent edge and both 1750mm rail edges all meet

  - try:      hub vertex via rv.position == tv.position comparison across rail and tang edges
    result:   failed to run (SketchUp loaded old bytecode, old format WARN still appeared)
    revealed: source_location confirmed correct file; vertex position equality unreliable in
              SketchUp Ruby when vertices are from different edge objects even if at same XY

  - try:      hub vertex via vertex-degree counting (vertex_count[v] += 1 for all key edges)
    result:   succeeded — no WARN fallback; all 4 station types detected correctly via new code
    revealed: Ruby Vertex object identity (hash key = object reference) is reliable;
              Point3d == comparison is not

principle: In SketchUp Ruby, find the shared vertex of multiple edges by counting occurrences
           of Vertex objects as hash keys (object identity), not by comparing Point3d positions.
           The hub vertex of a radial geometry (where N edges meet) is the vertex that appears
           in all N edges — max count wins. Point3d == uses tolerance comparison and can
           fail when vertices are separate objects even at the same position.
domain:    [SU]
