# TFTS — 2026-06-14T19:15:39Z

problem:   Junction gap FAULT files flood Allie's inbox on every animation startup
fault_ref: 20260614T182138 (first post-reload fault showing the pattern)

arc:
  - try:      Flag any consecutive maneuver pair in build_maneuvers_from_ids where
              map.json pts distance > JUNCTION_GAP_WARN_MM (5mm). Write FAULT file
              listing all pairs.
    result:   Failed — 12 FAULT entries per route per pod, every animation run.
              Every intra-station gw_* → gw_* transition showed gaps of 83–5173mm.
    revealed: map.json pts record physical 3D beam endpoints, not mathematical
              connection points. Dual-rail span (3.5m) and uturn arc diameter (3m)
              cause beam endpoints of adjacent topologically-connected tracks to be
              separated by metres in 3D space. The gaps are not real.

  - try:      Suppress when BOTH tracks are gw_* (is_seg_a || is_seg_b filter).
              Only flag when at least one end is a seg_* inter-station track.
    result:   Partially fixed — reduced from 12 to 2 FAULT entries per route.
              Remaining: gw_cp_out_1 → seg_*: 2550.9mm and seg_* → gw_cp_in_1: 506.9mm
    revealed: The CP diverge/merge tracks (gw_cp_out, gw_cp_in) are also gw_* tracks.
              Their physical beam endpoints in map.json don't coincide with the
              mathematical start/end of the inter-station segment. The 2550mm gap at
              gw_cp_out_1 → seg_* is the same class of artifact — the CP track diverges
              from centerline, so its 3D tip in map.json is offset from where path.json
              says the segment begins.

  - try:      Suppress when EITHER track is gw_* (!is_gw_a && !is_gw_b filter).
              A real gap requires both tracks to be non-gw_ (e.g. two seg_ tracks
              failing to meet). No such pair exists in the current network topology.
    result:   Succeeded — zero FAULT files on clean animation runs.

principle: map.json stores physical 3D beam geometry; animation runs on path.json
           mathematical topology. These two coordinate systems diverge by up to
           several metres at every gw_* track boundary due to dual-rail offset,
           uturn arc radius, and CP diverge angle. Any gap measurement that touches
           a gw_* track on either side is a geometry artifact, not a network fault.
           A real disconnection would appear as two seg_* tracks failing to meet —
           that pair does not exist in normal topology and would indicate a Noelle
           build error, which Noelle already catches at Build time.
           RULE: never measure connectivity from map.json pts. Use path.json or
           Noelle's build validation. If this check is ever rebuilt, it must be
           anchored to path.json mathematical connection points, not beam endpoints.

domain:    SU
