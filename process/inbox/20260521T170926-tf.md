# TF — 2026-05-21T17:09:26Z

summary: Station template geometry cannot be extracted from model.definitions by formation name — station templates load as anonymous Group definitions (Group#N), and the populate tool must search the live entity tree for JPods/formation_id attributes OR load the model.skp via definitions.load() and index gw_* entities by layer/tag name. A method defined in one module (e.g. StructureTool) is not automatically available in another (PathJSON) — each module needs its own _entity_tag helper.

code: jpod_path_json.rb — populate_all_from_template_files, _index_gw_entities, _entity_tag
