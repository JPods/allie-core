# TFTS — 2026-06-17T14:48:01Z

problem:   Vehicle placement and animation use two coordinate systems — not one unified under network.json
fault_ref: User: "2 coordinate systems, not one united under networks.json" + "BUILD should not be creating any of these files"

arc:
  - try:      Augment generate_network_json with full geometry tracks from lines.computed.json
              (inst_xf * inner_xf transform — same as Show Track). Committed 5535da2.
    result:   Succeeded for track geometry. But platform data (platforms[], parking_slots,
              spawn_t) still only in followme.json. Vehicle placement still read from followme.json.
    revealed: Two separate consumers: track routing reads network.json; vehicle placement
              reads followme.json. Followme.json still a second source of truth.

  - try:      Embed platform data in network.json designer.stations[*].platforms — call
              StructureTool.detect_platform_guideways_on_structure(inst) per station in
              generate_network_json. Compute parking_slots from entity attribute, spawn_t=0.12,
              connection_id from tag name. Committed 9559266.
    result:   succeeded
    revealed: load_followme_platforms also needed updating to read network.json first.
              Updated: tries network.json designer.stations[*].platforms → falls back to followme.json.

principle: |
  network.json is the single source of truth for both track geometry AND platform data.
  Two separate read paths (track routing from network.json, vehicle placement from followme.json)
  are two coordinate systems. Fix: embed both in network.json at Build time.

  Platform detection: StructureTool.detect_platform_guideways_on_structure(inst) works on
  ComponentInstances correctly (uses definition.entities internally). Returns world-space
  Geom::Point3d for start/end/midpoint — direct mm conversion via .to_m. Tag name (source.tag_name)
  is the connection_id (e.g. "gw_platform").

  load_followme_platforms must try network.json first. If designer.stations[*].platforms is
  non-empty after a Rebuild, followme.json is not read. Fallback kept for models built before
  this change.

  After this change, a Rebuild of any model populates network.json with platform data.
  No followme.json required for vehicle placement.

  The 5 separate Build output files → network.json consolidation status:
  - tracks: DONE (5535da2 — geometry from lines.computed.json)
  - platforms: DONE (9559266 — from detect_platform_guideways_on_structure)
  - beam_paths: already in network.json designer.stations[*].beam_paths
  - segment_registry: still in separate file (lower priority — read/write cycle in generate_map_json)
  - followme.json: still generated for routing geometry (separate concern from platform data)
  - build.log.json: diagnostic only, low priority

domain: SU
