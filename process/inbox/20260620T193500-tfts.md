# TFTS — 2026-06-20T19:35:00Z
problem:   Pods travel backwards on resumed intra-station tracks
fault_ref: (none — observed during testing)
arc:
  - try:      Resume v1 — save maneuver_id and t, rebuild from raw lookup pts using seed_pos projection
    result:   failed — Natalie reverses many intra-station tracks during trip build (e.g. gw_platform_out1 reversed for s006→s007). Raw lookup pts are unreversed. Pod at end of reversed track (t=0.998) projects to start of unreversed track (t=0.0). Pod travels entire track backwards.
    revealed: The lookup is not the trip. Natalie's build_maneuvers applies direction corrections that are lost when resume reconstructs from raw IDs. NORA_0014 saved at t=0.998 on s006.gw_platform_out1, resumed at t=0.0 — backwards. Log showed CP gaps of 10078mm confirming wrong direction.
  - try:      Resume v2 — save first point of each maneuver as traveled (man_start + remaining[].sp). On resume, compare saved start with lookup first/last pt. If saved start is closer to lookup LAST pt, reverse the lookup pts.
    result:   succeeded — all pods resume in correct direction. CP gaps eliminated.
principle: Any saved route bookmark must include direction info. The raw geometry lookup stores tracks in one canonical direction; the trip may traverse them in either direction. Save the start point as traveled — one comparison recovers the direction.
domain:    [SU]
