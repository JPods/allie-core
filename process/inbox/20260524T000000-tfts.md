# TFTS — 2026-05-24T00:00:00Z

problem:   Parking shuffle sometimes skips; vehicles lose personal space after CP Rule fix

arc:
  - try:      STANDARD_TEST_PERSONAL_SPACE_M = 2.5m (changed to get more slots)
    result:   pods 2.271m long; 2.5m slots leave 0.23m gap; no visible personal space
    revealed: slot spacing must be > vehicle length by a meaningful margin; 3m was correct

  - try:      compact_platform_idle target_slots = (1..slot_count).to_a.last(n)
    result:   ignores slots already reserved by in-transit vehicles (Sally queue advance)
    revealed: Sally sets parking_slot on entity before maneuver completes; compact sees
              that slot as free → assigns it to a second vehicle → two pods overlap

  - try:      collect all parking_slot > 0 values at station (including in-transit);
              subtract those not in all_parked → reserved_by_others;
              target from available_slots not reserved_by_others.
    result:   compact skips reserved slots; no overlap
    revealed: compact must treat Sally-reserved slots as occupied even before pod arrives

  - try:      snap-to-slot code used hardcoded 3000.0mm not the constant
    result:   if constant changes, snap and compact diverge
    revealed: snap must reference STANDARD_TEST_PERSONAL_SPACE_M like everything else

principle: Any slot with parking_slot > 0 at a station is occupied regardless of whether
           the vehicle holding it is dwelling, active, or in-transit. Compact must never
           assign a target slot that is already claimed.

domain:    SU
