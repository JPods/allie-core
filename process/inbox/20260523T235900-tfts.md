# TFTS — 2026-05-23T23:59:00Z

problem:   Vehicles jump 1750mm in Y at the CP→seg_ boundary during animation
fault_ref: (observed in animation log — no separate FAULT file)

arc:
  - try:      synthesize gw_stub_pair_N_out/in from CP center + tangent (noelle.rb)
    result:   correct direction vectors; zero-vector bug fixed; animation runs
    revealed: stubs ended at CP center (midpoint between rails), but seg_ starts at
              the actual rail centerline (1750mm = half_offset from CP center)

  - try:      compute perp = tangent × Z_UP; shift stub path laterally by ±half_offset
              outbound rail = CP_center + perp * half_offset
              inbound  rail = CP_center - perp * half_offset
    result:   succeeded — no Y jump; connections visually clean; Z-axis confirmed excellent
    revealed: CP center must never appear as a path endpoint in map.json;
              it is a gate marker for placement only; all paths anchor to actual rail

principle: The CP center is only used to calculate where to place guideways — 1.75m
           perpendicular to the direction of travel. It must never be a vehicle path
           endpoint. Every path endpoint in map.json is on the physical rail centerline.

domain:    SU

## Verification in animation log
stub_out: start=(-12742,-3191,7886)  end=(-10742,-3191,7886)
seg_:     start=(-10742,-3191,7886)  ...
X/Z match; Y continuous — no jump.
