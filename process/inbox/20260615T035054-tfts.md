# TFTS — 2026-06-15T03:50:54Z

problem:   gw_c_* connector arcs animate as straight-line chords — 1m bezier mandate violated
           for gw_c_0_1 / gw_c_1_2 / gw_c_2_3 / gw_c_3_0 despite prior session fixes

arc:
  - try:      Synthesize arcs in jpod_vehicle_anim.rb at animation start (synthesize_circle_arcs!),
              fitting ring center from map.json chord endpoints.
    result:   Partial fix — gw_c_* long arcs (N_N) upgraded; connector arcs (N_M) excluded
              because chord < ARC_MIN_CHORD_MM (968mm < 1000mm). gw_uturn still wrong
              (map.json endpoints are physical beam positions, not mathematical arc endpoints).
    revealed: Math synthesis at animation start was the wrong layer. Endpoints from map.json
              diverge from path.json mathematical topology by metres at every gw_* boundary.
              The TFTS principle (20260614T191539) already stated this:
              "RULE: never measure connectivity from map.json pts. Use path.json."

  - try:      Trace the geometry data pipeline: lines.json → extract → geometry.json → export
              → path.json → animation.
    result:   Found root cause: geometry.json for traffic_circle7 had 2-pt chords for the
              4 connector arcs (gw_c_0_1, gw_c_1_2, gw_c_2_3, gw_c_3_0). generate_geometry_json
              copied them as-is ("All other tracks: copy as-is" branch). export() uses
              geometry.json directly with NO corrections (geometry.json path bypasses the
              extracted.json correction pipeline, including the ring arc fix).
    revealed: The ring arc correction in export() only runs for the extracted.json legacy path.
              When geometry.json exists, export() applies only world_xf — no ring correction.
              generate_geometry_json must produce correct pts for ALL tracks, including 2-pt
              connector arcs. The N_N long arcs (13 pts) were correct; the N_M connectors were not.

  - try:      Fix generate_geometry_json: after main loop, read pass_chains.circle from
              lines.json, fit ring center from any N_N long arc (5+ pts) via circumscribed
              circle fit, then use _arc_pts_from_center_mm to synthesize dense pts for
              2-pt connector arcs. Also fix traffic_circle7/geometry.json directly.
    result:   Connector arcs: 2-pt chord → 5-pt arc (max seg 242mm). Long arcs unchanged
              at 13 pts (max seg 933mm). All gw_c_* arcs within 1m mandate. ✓
    revealed: The data pipeline is correct when geometry.json has proper pts. No math
              synthesis needed at animation start — the correct fix is upstream.

principle: geometry.json is the authoritative source for intra-station arc geometry.
           export() uses it directly with NO corrections — corrections must happen at
           generate_geometry_json time. The extracted.json legacy path HAS runtime corrections;
           the geometry.json path does NOT. This asymmetry means:
             Rule 1: generate_geometry_json must produce correct pts for ALL tracks.
             Rule 2: For ring (traffic_circle) formations, synthesize dense arc pts for 2-pt
                     connector arcs using ring center fit from N_N long arcs + _arc_pts_from_center_mm.
             Rule 3: Never use map.json endpoints for gw_* arc geometry — they are physical
                     beam positions, not mathematical topology. (TFTS 20260614T191539)
           lines.json pass_chains.circle identifies which tracks belong to the ring and their
           sequence. N_N tracks (gw_c_0_0 etc.) are long arcs; N_M (gw_c_0_1 etc.) are
           short connectors. Both must be dense in geometry.json.

domain:    SU
files:     jpod_path_json.rb: generate_geometry_json — ring connector arc synthesis pass
           templates/track_formations/traffic_circle7/geometry.json: direct fix
commit:    bc023fa
