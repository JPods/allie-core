# TFTS — 2026-05-25T19:59:53Z

problem:   routing_chains in JPods_station_parking/line.json had wrong direction for platform_in1
fault_ref: (no fault file — caught from user visual inspection of map.json)

arc:
  - try:      ["gw_platform_out1", "gw_platform_in1"], ["gw_platform_in1", "gw_uturn_0"]
    result:   failed — platform_in1 → uturn_0 is backward; platform unreachable from near_main
    revealed: platform_in1 has dx=+0.9848 (goes EAST) while the station flows WEST. It connects
              the branch point (30020,-2789) back TO the main entry (39945,-4539). It should be
              approached FROM the main entry side, not departed toward it.

  - try:      uturn_0 → platform_in1 → platform_in2 → platform → platform_out1 → far_main
    result:   succeeded — correct vector alignment; near_main and uturn_0 are predecessors
    revealed: platform_in1 and platform_in2 share a start point (the branch point at x≈30m).
              The chain platform_in1 → platform_in2 bridges them logically (vehicle "arrives"
              at the branch from the main line, then takes the northwest approach to platform).

principle: When two segments share a start point and diverge, the one with the "wrong" vector
           direction (going away from destination) is the APPROACH segment, not the exit.
           The routing chain bridges end-of-approach to start-of-continue via the shared
           branch point. Always verify: does the segment's dx/dy point TOWARD or AWAY from
           the next destination?

domain: SU
