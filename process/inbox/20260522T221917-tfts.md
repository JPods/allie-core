# TFTS — 2026-05-22T22:19:17Z

problem:   Natalie parking cycle — vehicles not compacting to highest available slots
fault_ref:
domain:    SU

arc:
  - try:      Scan gw.entities for parked vehicles in compact_platform_static
    result:   Found nothing. No compaction happened.
    revealed: place_vehicle calls host_model.entities.add_instance — vehicles live in
              model.entities root, NOT in guideway groups. Every scan of gw.entities
              for vehicle instances returns empty.

  - try:      Fix scan to model.entities; add parking cycle timer. slot_count capped at
              STANDARD_TEST_MAX_PARKING_SPACES_PER_PLATFORM=7 in both compact and cycle.
    result:   Cycle moved vehicles, but stopped at slot 7 even on a 23.9m platform that
              physically fits 9 slots. Log confirmed: "slot 1→7 target=16.25m".
    revealed: Physical path length (arc-length of gw_platform pts) is the authoritative
              slot count — not STANDARD_TEST_MAX_PARKING_SPACES_PER_PLATFORM, and not
              platform['length_m'] from followme.json which can differ. The cap is a
              5V-test scaffolding limit, not a physical constraint.

  - try:      Remove cap from _natalie_parking_move_one; sort compact ascending (lowest
              slot moves first into higher target).
    result:   Pod moved only one space. "Pod 2 moved pod 1 one space instead of the
              highest # space."
    revealed: When the lowest-slot pod moves first, it moves into the slot the next pod
              needs, then that pod has nowhere to go. Highest-slot pod must move first
              so it vacates a slot for the pod below it.

  - try:      Sort descending (highest-slot pod first), assign target_slots descending,
              skip if new_slot <= old_slot. Remove cap from compact_platform_static too.
    result:   Single vehicle placed correctly — moved to slot 9 on first parking cycle.
              Multiple vehicles (1–8 on S001): each vehicle placed at slot 1, parking
              cycle moves it to the next highest available slot (9, 8, 7, …, 2). Works.
              9th vehicle: overlap detector fires with "2.5m apart, minimum is 2.5m" —
              placement blocked even though NORA_0008 at slot 2 and new vehicle at slot 1
              are exactly slot_spacing apart (correct spacing).
    revealed: overlap threshold was `< 2.5`. Physical slot spacing is
              STANDARD_TEST_PERSONAL_SPACE_M * 1000/25.4 inches → exactly 2.5m when
              converted back. Floating-point arithmetic produces distance_m = 2.4999...
              which is < 2.5, triggering a false conflict for legitimately-spaced vehicles.

  - try:      Change overlap threshold from `< 2.5` to `< 2.49` (1 cm tolerance).
              Remove STANDARD_TEST_MAX_PARKING_SPACES_PER_PLATFORM cap from
              compact_platform_static to match the uncapped parking cycle.
    result:   Vehicles 1–8 placed on S001 correctly. Each triggers parking cycle, moves
              to next highest slot. 9th vehicle fits at slot 1 without false conflict.
              Platform compacts cleanly on each addition.

principle: Three independent rules exposed:
           1. Vehicles live in model.entities root — never in gw.entities. Every vehicle
              scan must iterate model.entities checking is_a?(ComponentInstance).
           2. Slot count = floor(arc_length / slot_spacing) from the actual gw_platform
              beam path. Test scaffolding caps (STANDARD_TEST_MAX_PARKING_SPACES_PER_PLATFORM)
              are only valid for placement ordering, not for compact or parking cycle logic.
              All three must use the same source or they disagree on what "full" means.
           3. Overlap thresholds that equal physical spacing always fail on floating-point
              boundaries. Leave a small tolerance (1 cm) so exactly-spaced vehicles pass.
