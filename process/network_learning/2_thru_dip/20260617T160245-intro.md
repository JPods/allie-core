# Introspection — 2_thru_dip — 2026-06-17T16:02:45Z

Pre-run risk assessment and forecasts before first animation run under the Agent Experience Framework.
Network: 2_thru_dip (station_thru_dip template × 2 stations, seg_ inter-station tracks)
Context: gw_ animation fix just applied (TFTS 20260617T150949); platform data embedded in network.json (TFTS 20260617T144801)

---

## Noelle — Risk Introspection

**Responsibilities at risk this run:**
- Track geometry in network.json is authoritative for gw_ — I wrote it at Build using inst_xf * inner_xf. If that transform is wrong for any station, gw_ animation will be wrong.
- Platform data embedded at Build — detect_platform_guideways_on_structure scanned the live model. If the scan missed a platform or returned a wrong world position, vehicle placement will be wrong.
- build.log.json was eliminated this session — if any downstream consumer still expects it, that is a silent failure I will not detect at runtime.

**Predicted findings:**
- gw_ animation will be substantially better than before the fix. The inst_xf * inner_xf transform is the same as Show Track — it should be correct.
- The highest-risk junction is where seg_ hands off to gw_ (the CP boundary). seg_ uses path.json bezier pts; gw_ uses network.json pts. If these two coordinate systems do not meet exactly at the CP, there will be a visible position gap or heading kink at that boundary.
- Platform detection: station_thru_dip has one platform per station. Predict: 2 platforms total in network.json, correctly positioned. Risk: spawn_t = 0.12 is a guess — vehicle may spawn visually wrong relative to the actual platform geometry.

**Instructions I am least confident about:**
- `formation_template:station_thru_dip` eps[] completeness — I validate that expected eps[] are present, but I do not validate that gw_ track pts are smooth. Nora sees that; I do not.
- The `network.json wins for gw_` merge rule in animation init — I wrote the rule; I have not seen it execute on this network yet.

---

## Natalie — Risk Introspection

**Responsibilities at risk this run:**
- Trip plans are generated from network.json routing graph. If gw_ tracks in the graph are disconnected or mis-directed (wrong inbound/outbound), BFS will find no valid route or will find a wrong one.
- The routing graph was built from the same network.json that Noelle wrote. If Noelle's track list is incomplete, my graph has holes.
- gw_platform clip logic: Nora clips gw_platform at runtime (departure trims forward from slot; arrival trims to slot). My trip plan includes gw_platform at both ends. If Nora's clip math is wrong, she will either overshoot or undershoot the platform.

**Predicted findings:**
- seg_ routing: high confidence — this path worked before and is unchanged.
- gw_ routing within station: moderate confidence — the graph should be correct but I have not run it since the gw_ fix.
- Timing: my planned trip duration will be wrong. The 40s/20s constants were estimated, not measured on this network. Predict actual will be 10–30% different from planned. Direction unknown.
- Ezone wait: 2_thru_dip is a small network. Predict zero ezone waits — not enough pods to conflict.

**Instructions I am least confident about:**
- `timing:station_entry_40s` — this is a placeholder. Real entry/exit time on 2_thru_dip depends on gw_ track length and animation speed, which I have not measured.
- `routing:direction_constraint` — I enforce inbound→outbound CP pairs. If any gw_ track in the new network.json has direction reversed, BFS will silently fail to find a path.

---

## Nora — Risk Introspection

**Responsibilities at risk this run:**
- I will execute the animation on whatever pts the merged lookup table provides. If the lookup is wrong, I follow it wrong — I do not validate pts before executing.
- TrajectoryObserver is not yet implemented in code. My observations this run will be manual (Bill's eyes), not automated. This means the physical.json will not be written after this run.
- Z profile: hanger_z = BEAM_DEPTH = 500mm is subtracted from all track pts to convert beam_top → vehicle path Z. If this subtraction is not applied uniformly across gw_ and seg_ tracks, Z jumps will appear at junctions even if the geometry is otherwise correct.

**Predicted findings:**
- gw_ animation will be recognizably better than before. The coord system fix should eliminate the gross position errors.
- Expect residual heading kinks at the seg_→gw_ junction (the CP boundary). This is where path.json bezier exits and network.json gw_ pts begin. These two were generated by different code paths and may not meet with heading continuity.
- Expect possible Z discontinuity on the turnback tracks — the inner arc at the far end of the station where the vehicle reverses direction. These tracks have the most complex geometry and the most opportunity for a Z mismatch.
- chord_cut risk: any gw_ track with only 2 pts (generated from a short arc) will produce a chord. Predict at least 1 chord_cut on the inner station arcs.

**Instructions I am least confident about:**
- `geometry:junction_continuity` at seg_/gw_ boundary — I have never seen this junction measured. It is the one I most want to observe after this run.
- `geometry:z_profile` at gw_ inner arc endpoints — the Z value on the first and last pt of each inner arc gw_ track is critical. One bad Z propagates through the whole station.

---

## Alice — Risk Introspection

**Responsibilities at risk this run:**
No live transactions on 2_thru_dip — this is a design model, not deployed. Alice has no direct observations to make on this run.

**What Alice watches instead:**
- The Small-Stings framework is designed but not integrated with the build pipeline. If Build errors were occurring silently before the gw_ fix, those would have triggered Small-Stings in a deployed network — customers would have noticed before the system did.
- Timing accuracy: if Natalie's planned trip time is >30% off actual, Alice's fare quote to a customer would be wrong. Predict: timing error is real and Alice cannot quote accurate arrival times on this network until Natalie's constants are measured.

**Forecast for deployment readiness:**
- 2_thru_dip is not ready for a fare quote. Animation geometry must be confirmed correct first. Predict: after this session's fixes, it will be ready for a first fare quote test.

---

## Claude Code — Forecast

Highest confidence predictions:

1. **gw_ animation will show clear improvement** — the inst_xf * inner_xf fix was the right root cause. The gross position error should be gone.

2. **The seg_→gw_ junction is the highest-risk remaining point.** path.json bezier pts and network.json gw_ pts were generated by independent code paths. I predict a visible heading kink or small position gap at that boundary. Severity: minor to moderate. If it is severe, it means the CP connection point coordinates differ between the two systems.

3. **Z continuity at gw_ inner arc endpoints.** The 2_thru_dip template has a dip — the track descends into the station and returns. The Z values at the entry and exit points of that dip are the most likely place for a residual Z mismatch. I predict one Z jump ≥ 50mm somewhere in the gw_ sequence.

4. **Platform spawn position will be approximately right but visually imprecise.** spawn_t = 0.12 is a first guess. The vehicle will appear on the platform but may not be at the center of the slot.

5. **No routing failure** — BFS will find a path. The routing graph should be correct. The defects will be geometric, not topological.

6. **Timing will be off.** The animation speed constant and the 40s station entry estimate are not calibrated to this network. Expect Natalie's plan vs. actual discrepancy to be visible.

What I am least confident about: whether the Z profile on the dip section is smooth or has a step. The dip geometry is the most complex part of station_thru_dip and has had the most history of Z issues (TFTS from 2026-06-12 session).

---

## Allie — Forecast

*(Written by Claude Code as Allie's proxy — Allie's nightly synthesis will incorporate the actual run results)*

From accumulated TFTS history and Understanding entries:

**Pattern recognition:**
- The Z_CONTINUITY saga (session 2026-06-12, multiple TFTS) showed that coordinate system errors in this codebase tend to have multiple manifestations. The gw_ fix addressed the primary one. I predict at least one secondary manifestation will appear in this run — a different junction type or a different Z boundary.
- U-SK-* entries consistently show that fixes to one part of the pipeline reveal the next bottleneck downstream. The gw_ coordinate fix will expose whatever the next constraint is.
- The frame-of-reference discipline feedback (memory entry) says: every coordinate must declare its frame; conversions must be explicit. The seg_→gw_ boundary is a frame-of-reference transition. That is where I expect the next find.

**What I want to know after this run:**
- Did any gw_ track produce a heading kink at the seg_ boundary? If yes: is it consistent (geometry rule) or inconsistent (data issue)?
- Is the Z profile on the dip section smooth or stepped? The dip is the defining feature of station_thru_dip. If it is not smooth, the template definition is wrong, not the code.
- Did vehicle placement (spawn_t) place vehicles visually correctly? If not: is the error in the world coordinate of the midpoint_m, or in the spawn_t constant?

**Cross-domain flag:**
If Nora finds a heading kink at the seg_→gw_ junction, that same junction exists in the physical network. The physical guideway has a physical angle at that CP. If the digital model has a kink, the physical beam has a kink too — that is a fabrication spec question, not just an animation question.

---

## Retrospection Placeholder

*(To be filled in after Bill runs the animation and reports observations)*

```
## What Agents Predicted Correctly

## What Agents Did Not Predict

## Bill's Observations

## Delta Analysis (instruction vs. experience)

## Logging Requirement Changes Triggered by This Run

## Next Increment
```
