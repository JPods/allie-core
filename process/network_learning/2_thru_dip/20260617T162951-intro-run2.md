# Introspection — 2_thru_dip — Run 2 — 2026-06-17T16:29:51Z

Pre-run for NEXT test after Compute is re-run on station_thru_dip template.
Expected change: gw_cp_in/out/lead tracks will have dense arc pts instead of 2-pt chords.

---

## Blocking Questions — Must Be Answered Before Run

---

### Nora [BLOCKING] → Natalie
What is my complete track sequence through 2_thru_dip, start to finish?
I predicted a jerk at the seg_→gw_ boundary in Run 1 but could not name the specific track.

**Natalie [ANSWERED by Claude, from routing_graph in network.json]:**

**Departure (s006 → s007):**
```
s006.gw_platform_out
→ s006.gw_cp_out_lead_1       ← HIGHEST RISK: lead curve, was 2-pt chord in Run 1
→ s006.gw_cp_out_1            ← high risk: junction to inter-station seg
→ seg_s006_cp1_s007_cp1       ← inter-station beam (seg_)
→ s007.gw_cp_in_1             ← high risk: first gw_ track after seg
→ s007.gw_cp_in_lead_1        ← HIGHEST RISK: lead curve, was 2-pt chord in Run 1
→ s007.gw_far_main            ← dip section
→ (turnaround via s007.gw_uturn or gw_near_main)
→ s007.gw_cp_out_lead_0
→ s007.gw_cp_out_0
→ seg_s007_cp0_s006_cp0
→ s006.gw_cp_in_0
→ s006.gw_cp_in_lead_0
→ s006.gw_near_main_1 → s006.gw_near_main_2
→ (return to platform)
```

**Nora's forecast is now specific:**
- Predict `heading_kink` at `s006.gw_cp_out_lead_1 → s006.gw_cp_out_1` (confidence: high if Compute fix works; severe if it doesn't)
- Predict `heading_kink` at `s006.gw_cp_out_1 → seg_s006_cp1_s007_cp1` (moderate — seg/gw boundary direction match unknown)
- Predict `heading_kink` at `seg_s006_cp1_s007_cp1 → s007.gw_cp_in_1` (moderate — same boundary, return direction)
- Predict `heading_kink` at `s007.gw_cp_in_1 → s007.gw_cp_in_lead_1` (high if Compute fix works; severe if it doesn't)
- Predict smooth at `s007.gw_far_main` — this track already had multiple pts in Run 1 (`gw_lift_in` had 25 pts; far_main is similar)

---

### Noelle [BLOCKING] → Claude/Bill
Before this run, was the Compute step re-run on station_thru_dip?
How many pts does `gw_cp_in_lead_0` have in lines.computed.json now?
I cannot predict whether the chord cuts are fixed until I see the new pt count.

**[RESOLVED 2026-06-17 — noelle.rb commit b17ef96]**

The live edge scan that was breaking Compute is removed. Compute now runs cleanly.
New behavior: cp_marker math pins gw_cp_in/out outer endpoints to live cp_marker positions.

IMPORTANT FINDING: gw_cp_in_lead and gw_cp_out_lead in station_thru_dip are STRAIGHT
horizontal segments (constant Y and Z). They have 2 pts each — this is CORRECT, not a
chord cut. The prior "4 chord cuts" report was a false positive from the logging pattern.
The Noelle logging is now fixed to not flag axis-aligned 2-pt _lead tracks.

Bill: reload su_jpods in SketchUp, open station_thru_dip, run Compute again.
Expected console output: NO chord cut warnings for gw_cp_*_lead tracks.
Then: Rebuild 2_thru_dip. Then run animation (Run 2).

---

### Natalie [BLOCKING] → Noelle
Does the exit tangent of `seg_s006_cp1_s007_cp1` at the s007 end match the entry direction of `s007.gw_cp_in_1`?
If the seg_ bezier arrives at s007 from a direction that doesn't align with gw_cp_in_1's first chord, Nora will jerk there regardless of chord count.

**[PARTIALLY RESOLVED 2026-06-17]**

From lines.computed.json: gw_cp_in_1 first pt is at [-2459.966, 1250.87, 9683.942].
The direction into the station is +X (from -2460 toward 40). The seg_s006_cp1_s007_cp1 bezier
arrives at this outer endpoint from outside the station in -X direction — the same heading.
So the seg_ exit tangent should match gw_cp_in_1 entry direction. Angle mismatch is low risk.
Confirm by checking path.json for the seg_ after Rebuild.

---

### Nora [BLOCKING] → Noelle
After Compute, does `gw_far_main` still have the same pt count as `gw_lift_in` (25 pts)?
In Run 1 the dip section looked smoother — I want to confirm its pts were not affected by the Compute re-run.

**[RESOLVED 2026-06-17]**

gw_far_main in current lines.computed.json: 2 pts, Z=9683.9 — a straight horizontal segment.
It was not affected by the Compute confusion. The 25-pt tracks (gw_lift_in, gw_platform_in,
gw_platform_out) and 7-pt gw_uturn_0/1 are the curved tracks and they remain intact.

---

## Forecasts (conditional on Compute being run)

### Noelle — Forecast for Run 2

**If Compute was run and gw_cp_lead tracks now have ≥ 5 pts:**
- Animation at gw_cp_in/out/lead will match Show Track
- No chord-cut heading kinks at those junctions
- Prediction: smooth departure and arrival at both stations
- Confidence: high

**If Compute was NOT run (UNRESOLVED above):**
- Same ragged animation as Run 1
- No point running Run 2 — precondition not met

**What I will check in network.json after Rebuild:**
- `s006.gw_cp_in_lead_0` pt count — must be > 2
- `s006.gw_cp_out_lead_1` pt count — must be > 2 (this is the actual departure track)
- `s007.gw_cp_in_lead_1` pt count — must be > 2 (this is the actual arrival track)

**New risk I didn't forecast in Run 1:**
- The routing uses the `_1` suffix tracks (gw_cp_out_lead_1, gw_cp_in_lead_1), not the `_0` tracks. Both suffix families must be fixed. If Compute only fixes `_0`, the animation will still be ragged on `_1`.

### Natalie — Forecast for Run 2

**Route confidence:** High — routing graph is correct. Same route as Run 1.

**Timing prediction:** Still no calibrated constants. Predict 20–40% timing error. Direction: probably faster than estimated (the gw_ tracks are short; the 40s station entry estimate is too high for a 2-station test network).

**New risk:** The turnaround path at s007 goes through `gw_far_main` then back via `gw_cp_out_lead_0` → `gw_cp_out_0` → `seg_s007_cp0_s006_cp0`. The `_0` outbound tracks at s007 may also have chord-cut issues if Compute didn't fix them.

### Nora — Forecast for Run 2

**If chord cuts are fixed:**
- Expect smooth animation at gw_cp_out_lead_1 and gw_cp_in_lead_1
- Remaining risk: heading kink at `seg → gw_cp_in` junction (seg_ bezier exit angle vs. gw_ entry chord)
- This junction is the one place where path.json (seg_) meets network.json (gw_) — the coordinate systems are different sources and may not produce matching tangent vectors

**Specific predictions with track names:**

| Junction | Prediction | Confidence | Instruction_ref |
|---------|-----------|-----------|----------------|
| gw_platform_out → gw_cp_out_lead_1 | smooth (if Compute worked) | medium | geometry:min_pts_per_track |
| gw_cp_out_lead_1 → gw_cp_out_1 | smooth (if Compute worked) | medium | axiom:junction_continuity |
| gw_cp_out_1 → seg_s006_cp1_s007_cp1 | possible heading kink | medium | axiom:junction_continuity |
| seg_s006_cp1_s007_cp1 → s007.gw_cp_in_1 | possible heading kink | medium | axiom:junction_continuity |
| s007.gw_cp_in_1 → s007.gw_cp_in_lead_1 | smooth (if Compute worked) | medium | geometry:min_pts_per_track |
| s007.gw_cp_in_lead_1 → s007.gw_far_main | smooth (was already multi-pt) | high | geometry:min_pts_per_track |

**What Nora wants measured after this run:**
If Bill sees remaining jerk after Compute fix: it is at the seg_/gw_ boundary angle, not at the lead tracks. That is a different problem — the seg_ bezier exit tangent may not match the gw_cp_in entry chord.

### Claude — Forecast for Run 2

**Highest confidence:** If Compute fixes the lead track pt counts, the most visible ragged animation from Run 1 will be gone. The sharp left jerks at gw_cp_in/out/lead will resolve.

**New prediction for Run 2:** The seg_→gw_ boundary angle mismatch is now the most likely remaining defect. The seg_ bezier arrives at the CP from a specific world-space direction. The gw_cp_in_1 track starts at the CP in whatever direction its first chord points. These were generated by different tools (bezier fitter vs. template Compute). If they don't match, there will be a visible kink specifically at those two junctions: `gw_cp_out_1 → seg_s006_cp1` and `seg_s006_cp1_s007_cp1 → s007.gw_cp_in_1`.

**What I need from Natalie to test this prediction:** The tangent vector at the endpoint of seg_s006_cp1_s007_cp1 and the first chord of s007.gw_cp_in_1. If their angle exceeds ~10°, the kink will be visible.

**Confidence:** If the seg_/gw_ angle mismatch is ≤ 10°, Run 2 will look good. If it is 20°+, there will be a remaining kink that is not fixed by Compute.

### Allie — Forecast for Run 2

The pattern from the Z_CONTINUITY arcs (multiple sessions) suggests that fixes applied to template-level data produce cleaner improvements than fixes applied in code, because they affect all instances of the template simultaneously. The Compute fix is a template-level fix — I predict it will be clean and complete for the chord cut problem.

What I'm watching for: whether the seg_→gw_ boundary angle emerges as the next systematic defect (appears on both CPs of both stations in consistent form), or whether it's instance-specific (varies by station placement). If systematic → design rule; if instance-specific → connection tool behavior.

**Question for Bill (not blocking, but I want the answer):**
When Show Track draws gw_cp_in_lead, does it draw a smooth curve or a straight segment? If Show Track shows a straight segment there, the 2-pt chord is geometrically correct and the jerk is from angle mismatch, not from chord cut. If Show Track shows a curve, the chord cut is confirmed as the defect.

**[ANSWERED by Bill 2026-06-17]:** Show Track is perfect to spec — gw_ tracks are all perfect in their connections, uturn is a smooth arc. gw_cp_in_lead draws as a smooth curve. **Chord cut confirmed as the defect. Angle mismatch theory is ruled out. Compute fix is correct.**

---

## Precondition Checklist Before Running

- [ ] Compute run on station_thru_dip template in SketchUp
- [ ] Verified: `lines.computed.json` gw_cp_in_lead_0 pt count > 2
- [ ] Verified: `lines.computed.json` gw_cp_out_lead_1 pt count > 2 (the actual departure track)
- [ ] Rebuild 2_thru_dip after Compute (so network.json picks up new pts)
- [ ] Verified: network.json `s006.gw_cp_out_lead_1` pt count > 2

Only then: run animation and observe.
