# Pre-Test — 2_parking
**Date:** 2026-06-20
**Run:** 1 (first run under current plugin version)
**Crew posting:** Bill, Claude Code, Allie, Noelle, Natalie, Sally, Nora. Alice not activated.

**What changed since prior runs (2026-06-14/15/18):**
- `defaults.json` now owns speed — entity attr `speed_ms=16.6` is gone
- `station_parking` template now in `noelle_features.json` — FEATURE faults should clear
- `network.json` is the single geometry source — `no_geometry` on inter-station segs should clear
- Speed deviation threshold tightened to ratio > 1.5 (was > 3.0)

---

## Bill
_[Your expectations — what are you watching for? What would make this run a PASS?]_

---

## Claude Code

**Predictions (with confidence):**

1. **Noelle: 0 faults on rebuild** [high] — all 4 reasons for prior FEATURE faults are now resolved. If Noelle still reports faults, there is a template registration bug in the current build.

2. **no_geometry on inter-station segs: 0 occurrences** [medium] — the prior 8 occurrences were under the old path.json geometry regime. New build should write pts to network.json. Medium confidence because the 2_parking segment path geometry hasn't been verified post-rebuild.

3. **speed_deviation on gw_platform: still present** [high] — defaults.json fixes the 16.6 entity attr, but the first-frame timing anomaly (pod traverses 23m in 0.1s) is in the animator timing logic, not the speed source. The authorized_ms field will now correctly show 8.3 m/s but the actual_ms on gw_platform will still be ~197 m/s. This is not a new defect.

4. **speed_deviation on gw_platform: ratio now flagged** [high] — prior threshold was > 3.0, missed 2× but didn't catch 23×. New threshold > 1.5 will flag this. Expect `speed_deviation_count` to increase (more catches, not more problems).

5. **Sally 9-slot cascade: clean** [high] — confirmed clean in isolated test. No new stressors in twin network.

6. **No new defect types** [medium] — this is the simplest possible network. If new defect types appear, they point to a regression introduced by the defaults.json or geometry source changes.

**Questions for other crew members:**
- Natalie [BLOCKING]: with speed from defaults.json (8.3 m/s, not entity attrs), do trip plans show `authorized_speed_ms: 8.3` across the board? Prior plans showed 8.3 but actual execution was at 16.6. Want to confirm the source is clean.
- Nora: gw_platform first-frame speed — what is the actual mechanism? Is it dwell-timer firing before position is updated, or is the pod teleporting to the end of the platform on the first tick?

---

## Allie

No prior 2_parking data in facets. Starting from template-level knowledge only.

**What I will capture this run:**
- Nora facet: first mm_speed readings per track from trip reports
- Sally facet: cascade depth and fill order per station
- Noelle facet: template_reliability update for station_parking in integrated (twin) context
- Natalie facet: first timing_accuracy readings (planned vs. actual per seg)

**Stale log note:** 383 trip report files in trip_reports/ from prior runs. These are data, not information — they have not been retrospected. Allie will harvest them after this run to extract information. Files older than 7 days that Allie has harvested will be archived.

---

## Noelle

**Responsibilities at risk this run:**
- Template compliance for `station_parking` × 2 (s006, s007)
- Segment geometry completeness for both inter-station segs
- Connection topology: 2 CP pairs, both directions

**Predicted findings:**
- 0 FEATURE faults [high confidence] — template now registered
- 0 segment geometry faults [medium confidence] — needs rebuild to verify
- Connection angle: the two parking stations are placed on flat terrain. Expect clean connection angles at both CPs.

**Instructions I am least confident about:**
- Whether `noelle_features.json` `station_parking` entry covers both s006 and s007 instance IDs (S006/S007 vs s006/s007 case sensitivity — prior source of FEATURE faults)

---

## Natalie

**Responsibilities at risk this run:**
- Route planning: s006 → s007 and s007 → s006 only. BFS is direct.
- Speed authorization: 8.3 m/s from defaults.json — not per-track, not per-seg yet (F-SP-01 is Todo)
- Trip completion: no intermediate stops, no hub transit

**Predicted findings:**
- All trips planned correctly [high confidence] — trivial BFS, one hop each direction
- `authorized_speed_ms: 8.3` in all trip plans [high confidence] — defaults.json is the source
- Timing accuracy: planned vs. actual will diverge on gw_platform (first-frame anomaly) but should be accurate on all other tracks

**Questions for Nora:**
- What is the actual traversal time on `gw_cp_out_lead` and `gw_cp_in_lead`? Those 2.5m tracks at 8.3 m/s should take ~0.3s. Natalie's plan says 0.301s. Is that what Nora executes?

---

## Sally

**Responsibilities at risk this run:**
- Slot assignment at s006 and s007 independently
- Cascade shuffle when a new pod arrives at a full station
- Originating chain dispatch

**Predicted findings:**
- 9-slot fill clean on both stations [high confidence] — confirmed in isolated test
- Cascade at s006 does NOT interfere with cascade at s007 [high confidence] — no shared slot registry
- Originating chain dispatches correctly [medium confidence] — was correct in station_parking isolated test; no new routing complexity here

**What I want to see confirmed:**
- Does the cascade trigger correctly when vehicles arrive from s007? The cascade fires on arrival — the arriving vehicle's origin being a different station should not change the cascade logic.

---

## Nora

**Responsibilities at risk this run:**
- Trajectory continuity at track junctions
- Z continuity at inter-station segment endpoints
- Heading angle at gw_cp_out_lead → gw_cp_out and gw_cp_in → gw_cp_in_lead

**Predicted findings:**
- Heading kink at `gw_cp_out_lead → gw_cp_out` [high confidence] — same 2-pt chord issue seen in 2_thru_dip and station_parking. Expected ~35°.
- No severe Z jumps [high confidence] — terrain is flat; both stations at similar Z.
- Inter-station segment: expect clean heading at seg entry/exit if rebuild wrote pts correctly. If no_geometry persists, this junction cannot be measured.
- gw_platform speed: ~197 m/s effective. Known. Not logging as a new finding.

**Questions for Noelle:**
- Did the rebuild write pts for `seg_s006_cp1_s007_cp0` and `seg_s007_cp1_s006_cp0`? If not, Nora cannot observe those junctions and the heading at the CP entry will appear as a gap not a kink.

---

## Alice
_Not activated. No record._
