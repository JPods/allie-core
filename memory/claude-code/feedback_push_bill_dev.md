---
name: Push to bill_dev
description: Always push to bill_dev branch as standard practice; push to main when explicitly requested
type: feedback
---

As a rule, push to bill_dev branch. Bill requested this as the standard push target for WebClerk development work.

**Why:** bill_dev is the working branch; main gets updates when explicitly requested or when merging.

**How to apply:** After committing, push to bill_dev by default. Push to main only when Bill says to (as he did this session — "push to git bill_dev and main").
